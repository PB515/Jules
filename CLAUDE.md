# CLAUDE.md — Synergy

*The context anchor for this site. Read first every session. Keep Status and the Build Log current at the end of every phase.*

---

## What this is

**Synergy** (formerly "Jules") is a mobile-first Progressive Web App that is the shared engagement platform for every club in the Adani University FMS/Infra Management MBA program (12-13 clubs, one shared system — not a single-club app). It turns club participation (meetings, expert sessions, volunteer tasks, live quizzes) into a gamified engagement system — "Joules" earned via QR check-in and quiz ("Surge") participation, standing tiers (Ember → Volt → Current → Plasma), and a permanent archive ("Catalyst Records"). **Three parts**: the student "Node" experience (mobile-installed-PWA only, decision 29 — never a laptop browser, by explicit professor requirement), the admin "Reactor Command Center" (role-scoped — being restructured into Student/Committee Member/Professor per decision 45, still Owner/Officer/Volunteer as of Phase 0), and a public "General" site (club info, event calendar, PWA install page, Event Reports — renamed from Afterglow, decision 45 — Gallery — no login, works on any device). This is an **authenticated app with private per-user data** for two of the three parts — the portal golden path applies, security-first.

**Naming note**: the app and its underlying code/folders are still named "Jules" in the repo path and a handful of internal identifiers (`lib/jules/*`, the `jules` git repo itself) — only user-visible copy and the product name were rebranded to Synergy in Phase 0 (decision 45). Do not rename internal folders/variables as part of later phases unless specifically asked; it's cosmetic-only churn with real regression risk for no user-facing benefit.

Full concept + already-decided product/technical detail: [`docs/project-spec.md`](docs/project-spec.md) (the canonical, merged V3+V4 spec — treat as both the substance/strategy authority; there is no separate competitor-research doc since this isn't a commercial business). Historical: [`docs/project-spec-v4-addendum.md`](docs/project-spec-v4-addendum.md) (superseded by project-spec.md, kept for reference only).

## Current status

- **★ Most recent work (read this first)**: the 3-tier RBAC (Professor platform-wide) was reworked into a 4-tier model — `super_admin` (platform-wide) / `professor` (now club-scoped, one per club) / `committee_member` (club-scoped, event creation + Event Report writing, no QR/Live Round access) — after the user's own real-device Professor/Committee-Member walkthrough. See decision 78 and the newest Build Log entry ("Real-device Professor/Committee-Member walkthrough → 4-tier RBAC rework...") for the full detail, including 6 real bugs found and fixed from that same live-testing pass (a cross-club data leak in Event Reports, a missed club-scoping gap in the System Ledger, a mobile admin-nav gap, and a Server Action body-size crash on real photo uploads, among others). This is the most current architecture — anything below describing the old 3-tier model is historical.
- **Phase:** Platform pivot **complete — all five approved phases (0 through 4) are done and live-verified** (referred to below as "Pivot Phase 0/1/2/…" to distinguish from the original spec's own "Phase 1 of 6" build-phase numbering, which is now historical). After a live professor review of the original "Jules" build (well received), the direction changed fundamentally — see decision 45 and `docs/project-spec.md`'s successor context below. See the five newest Build Log entries: Phase 0 (avatar removal + rebrand to Synergy), Phase 1 (multi-club data model + 3-tier RBAC), Phase 2 (event registration + attendance-based streak), Phase 3 (universal leaderboard — every student ranked, no cutoff), Phase 4 (group/individual quiz registration with pooled scoring, negative marking, and time-based marking — the largest single mechanic in the pivot). Everything below this line up through "Goal 2" describes the pre-pivot "Jules" single-club build — still historically accurate (that work happened, was live-verified, and much of the underlying code survives the rebrand), but superseded in product direction by decision 45. **What's next is genuinely open** — the pivot as scoped by the professor's 11-point feedback is now fully built; further work needs fresh direction from the user (real content: the 12-13 club roster, real audio files, real photos; or picking back up Goal 3/Goal 4 from before the pivot; or new feedback from another professor review).
- **Last completed:** Full security-first build → hosted Supabase → GitHub → Vercel, all live-verified (see prior entries below). Then, at the user's request (a professor compared the app to Kahoot), built **"Live Round"** — a second, host-paced quiz mode alongside the original self-paced async Surge Mode (decision 24). Live-testing Live Round surfaced **three real, previously-undiscovered bugs**, one of them critical and system-wide:
  1. **Critical: Joules have never actually been awarded to a real student, for ANY correct answer, anywhere in the app, since it was built** (QR scans, async Surge, and the new Live Round all go through `_bump_streak()`, which every single time silently failed and rolled back the whole transaction — decision 27). Caught only by submitting a real answer and checking `joule_transactions`, invisible to typecheck/build/lint/inspection.
  2. `start_surge()` — the original async Surge Mode's own entry point — has never actually worked (an ambiguous-column bug, decision 25). Never exercised live until this session.
  3. `live_round_question()` had the same ambiguous-column bug (decision 25), plus the team screen had a cold-load gap where the scoreboard never fetched on a fresh page load into reveal/leaderboard (decision 26).
  All four are fixed, migrated to the live DB, and re-verified live (see Build Log). **This is exactly why "build passes" was never treated as "verified" in this project — live testing keeps finding the bugs that matter most.**
- **Then, at the user's request, added a third site part and a hard access rule**: a public "General" site (homepage, `/events` calendar auto-fed from real event data, `/get-app` PWA install page, `/afterglow` written event recaps linked to real events, `/gallery` photos — migration 0014), plus a two-layer **mobile-PWA-only gate** for every student route (decision 29) per the professor's requirement that quizzes only ever run on an installed phone app, never a laptop browser. Root `/` is now the real public homepage, not an auth-redirect.
- Live-testing this phase found **two more real bugs**, both fixed and re-verified: a hydration mismatch from locale/timezone-dependent date formatting in a client-side dropdown (decision 30) — present in the *original* Grid Station's event picker too, latent since that was built, never triggered until now; and a design correction mid-build (a "Reports" file-upload page was the wrong shape entirely — the user redirected to "Afterglow," a written recap linked to a real event with its real stats, before any code was written for it).
- **Then, at the user's explicit request for a harsh security retrospective on Goal 1** (not a build request — an adversarial re-read of every migration and admin action with fresh eyes), found and fixed **four more real issues, one of them a live, actively-exploitable CRITICAL vulnerability in production**:
  1. **CRITICAL (fixed): `qr_secret()` had no grant/revoke statements at all — unlike every other internal helper — so it carried Postgres's default PUBLIC execute grant and was callable by a fully anonymous, unauthenticated caller, returning the raw HMAC secret behind the whole QR rotation system.** Confirmed live against production (`rpc('qr_secret')` as anon returned the real secret) before it was fixed. With that secret, anyone could compute a valid "current" QR token for any event without ever being present, defeating the entire anti-abuse mechanism spec §9 describes. Fixed by revoking all access (decision 31); re-verified the legitimate internal call path (`current_qr_token`, used by the real Grid Station) still works unaffected.
  2. **Moderate (fixed): `submit_live_answer()` never verified the submitted question was the round's *current* one** — only that it belonged to the same surge. A team could deliberately skip an early question, watch the host reveal its correct answer to the whole room, then submit that now-known answer during any *later* question's live window, and it would be accepted as a normal on-time correct answer. Fixed by scoping the lookup to `order_index = question_index` (decision 31); live-verified both that the exploit is now rejected ("that is not the current question") and the legitimate flow is unaffected.
  3. **Moderate (fixed): Force Reset never verified its target was actually a student.** The Auth Admin API it uses operates on any `auth.users` row, admins included — unlike `admin_adjust_joules`/`admin_set_student_status`, which are naturally scoped by a foreign key to `students`, Force Reset had no equivalent guard. An Owner could have passed another admin's (even another Owner's) id as "studentId" and silently reset their password, with no distinct audit trail from a real student reset. Fixed with an explicit `students` table check before the Auth Admin API call (decision 32).
  4. **Low (fixed): the core RBAC helpers (`is_admin`, `admin_role`, `is_owner`, `is_officer_or_owner`, `is_volunteer_for_event`) had no grant/revoke statements either** — inconsistent with the project's own `has_role.sql` reference pattern and the "internal helper, not exposed" stated intent elsewhere in the same file. Low real risk (all self-referential to `auth.uid()` — calling them reveals nothing about *other* users), but real needless attack surface. Fixed the same way `has_role.sql` does it: revoke from `public`, re-grant to `authenticated` specifically (**not** also revoking from `authenticated` — RLS policies across nearly every table call these directly in their `USING` clauses, evaluated as the querying role, not the function owner; getting that wrong would have broken RLS everywhere). Re-verified live that Owner/Officer flows depending on these policies still work.
  All four fixes are migrated to the live DB (0015, 0016) and re-verified live, not just reasoned through. **Remaining findings from the same pass, disclosed but not yet fixed** (see Known open items): `events.geofence_lat/geofence_lng` are readable by any authenticated student via the broad `events` SELECT policy, even though no client code actually needs to read them (the redeem flow sends the student's own coordinates to the server, which does its own internal comparison) — lower severity since geofence is already documented as a soft flag, not a hard block, but a real over-exposure worth closing; and the login/signup rate limiter (`lib/security.ts`) is explicitly in-memory, single-process — its own docstring already discloses this resets per instance on serverless, meaning it's weaker than it looks in this Vercel deployment specifically.
- **Then moved to Goal 2 (frontend feel/impact)**, starting with the user's two concrete ideas: a once-per-day launch-splash sequence (logo jolt → moto → logo return → dashboard reveal) for the installed PWA, and an escalating drama curve across a quiz (quiet per-question feedback, then a suspense beat + bigger reveal + confetti/sound/vibration "Winner Declaration" on the true final result). Added `motion` and `canvas-confetti` as new dependencies (confirmed with the user first, decision 33). Built and live-verified via a temporary throwaway route (see Build Log) since the real routes are behind the mobile-PWA gate, which a desktop preview browser's User-Agent can't pass. **This slice shipped live** — pushed, deployed, and confirmed Ready/reachable on `jules-delta-nine.vercel.app` (one of this project's several Vercel aliases, all pointing at the same production deployment).
- **Then the user asked a genuinely important question mid-session: are we actually following the established moodboard, and is the Joule/Shakti conceptual foundation being left behind?** Verified directly against `docs/project-spec.md` §2/§3 rather than just reassuring — the palette (solar blue/kumkum/gold) and the Node/Spark/Surge/Grid naming philosophy are both already followed exactly as written. This led to a second Goal-2 slice, going further than the original two ideas: fixing two real hardcoded-hex violations of the project's own "tokens only" convention (decision 35), a tier-up celebration (decision 36, finishing a feature spec §6 already called for but nobody built), two "deeper Shakti" touches from the spec's own Phase-4 list pulled forward now — a rotating Vishwambhari Stuti verse on the splash (verbatim text supplied by the user, never fabricated/translated by me) and "the first spark" naming for a student's first-ever logged activity (decision 37) — an empty-state consistency sweep (decision 38), and giving Live Round a kumkum-leaning chrome identity distinct from async Surge's gold-forward one (decision 39).
- **Then a third Goal-2 slice, closing out "give major features their own color identity" and the consistency pass**: a full color-usage audit (research agent) across every Node/Admin/General screen found the palette was already applied consistently almost everywhere, with one real, concrete gap — neither the admin sidebar nor the public site's nav had ANY active-route highlighting, every link rendered identically regardless of section. Fixed both (decision 40), reusing kumkum's existing "you are here" meaning rather than inventing per-section colors (confirmed with the user: active-nav highlighting only, not a rainbow of feature colors). Also fixed one small real drift (Vault's search box border token) and documented two audit findings that turned out to be deliberate, not bugs (`engagement-chart.tsx`'s categorical chart palette, `winner-burst.tsx`'s white confetti sparkle) with one-line comments so they aren't re-flagged later.
- **Then the user laid out an explicit sequencing plan for the rest of Goal 2**: asset generation first, then the deferred mobile-responsiveness pass, then final polish, then the next goal. Delivered an audio spec sheet (type/length/style/trigger for all 6 sound files) for the user to source. Confirmed the static-art approach: no Midjourney/GPT-Image tool is available in this session, so static art gets hand-coded as real SVG (confirmed with the user) rather than left unaddressed or faked. Built two pieces (decision 41): a real app icon/logo (`public/icons/icon.svg` — replaced the bare lightning-bolt placeholder with an atom-orbit + spark mark, feeds the PWA manifest icons, favicon, and the launch splash's logo beat) and a Gallery placeholder illustration (`lib/components/gallery-placeholder.tsx` — an abstract "energy grid" node scatter, deliberately NOT a mockup pretending to be a real event photo) for the public Gallery page, since the club has no real photos yet.
- **Then, inspired by a reference site's animated hero, the user asked for an interactive homepage hero** — scoped down through direct discussion to: public homepage only (explicitly not the mobile PWA, which is deliberately distraction-free for quiz integrity, and not admin, which needs to stay fast for daily operational use). Built (decision 42): `lib/components/energy-field.tsx`, a full-page ambient particle-network background (deterministic nodes/lines, same visual language as the Gallery placeholder, pure CSS drift, idle everywhere), and `lib/components/hero-atom.tsx`, a bigger animated version of the new app icon's atom-orbit mark with a slow auto-rotation and pointer/touch parallax, scoped to just the hero section on `app/(general)/page.tsx`. **A real, instructive bug found live**: `motion/react`'s `animate`/`style` props silently did nothing on nested SVG `<g>`/`<svg>` elements in this case (no error, just no transform ever applied) — switched to plain CSS `@keyframes` for the rotation and plain React state + a CSS transition for the pointer parallax, the same low-tech approach already proven reliable elsewhere in this codebase, rather than continuing to fight the library.
- **Then closed the last open QR item**: added `react-qr-code` (decision 43, confirmed with the user first) and wired a real scannable QR into all three places it was wanted — the Grid Station check-in code, the Live Round host lobby's room code (with `/live` now also accepting a `?code=` query param to skip typing it, same Suspense-wrapped `useSearchParams()` pattern as the auth pages), and `/get-app`. Kept the QR itself standard black-on-white rather than theme-colored, since real-world scan reliability across random phone cameras matters more here than matching the dark UI.
- **Next up:** Real photos for the Gallery, a real college domain + season calendar, a real club moto/mission review, the six real audio files (spec delivered to the user this session), a real trusted-input confirmation that the hero's pointer/touch parallax actually reacts on a real device (this session's tooling couldn't dispatch a trusted synthetic pointer event React would pick up — see Known open items). Then, per the user's own sequencing: the deferred mobile-responsiveness pass (General site header nav — still explicitly not started, do not build a hamburger menu until that pass begins), then Goal 3 (load/concurrency simulation) or Goal 4 (11-club replication) — both still fully unstarted. **Two real-device verifications the user personally ran and confirmed working**: the mobile-PWA gate (phone installs and works, laptop correctly blocked) and a full 20-question Live Round completed live across two real separate devices (laptop host, phone team) over production — both were previously the biggest remaining unknowns and are now closed.
- **Last commit:** Pivot Phase 4 (group/individual quiz scoring — the final approved pivot phase) — see the newest Build Log entry. Phases 3 (leaderboard), 2 (registration + streak), 1 (multi-club + RBAC), and 0 (avatar removal + rebrand) are the four commits before it.
- **Then, a real pre-deployment testing pass**: the user shared a 13-screenshot feedback document from actually using the deployed site, which surfaced real bugs, a fundamental auth-model change (no self-signup — the institution bulk-provisions all student accounts and hands out credentials via CSV), and a long feature backlog. Confirmed with the user there's time before the next deployment, so the full backlog was attempted, not just the blocking bugs, staged as Phase A (bugs) → Phase B (bulk provisioning) → Phase C (events/club/leaderboard presentation) → Phase D (analytics dashboards) → Phase E (security/performance/UX polish), each independently verified and committed. **All five phases (A-E) are now complete** — see the Build Log entries. Mid-Phase-A, the user asked for a dev-only bypass of the mobile-PWA gate so rendered student-PWA content could actually be seen within this session for the first time in the project's history (see the dedicated Build Log entry) — a real, standing capability now, not a one-off. **Phase C live-verification caught a real bug**: `/clubs` and `/leaderboard` (this phase's own new public routes) were both missing from `proxy.ts`'s `PUBLIC_PATHS`, so both silently redirected to `/login` — fixed before commit.
- **Then the user's own real-device test of the deployed PWA surfaced the single most consequential finding yet**: 9 annotated Android screenshots showed several small UI items, plus one critical gap — Live Round's join screen had no group-formation UI at all. After the user clarified that **only Live Round will actually be used in practice** (not async Surge Mode), the group/pooled-scoring mechanic from decision 49 was rearchitected into Live Round's real-time model itself (decision 60) — new `live_round_team_members` table, `create_live_team`/`join_live_team`/`leave_live_team` RPCs, deferred Joule crediting via a new `complete_live_round()`, a new student team-formation screen, and host-lobby member counts. Live-testing this surfaced two more real findings: a genuine pre-existing bug in the shared `reveal-scoreboard.tsx` component (a cold-load-into-`complete`-phase gap, unrelated to this feature but exposed by its testing) and a previously-undocumented local-dev-only trap where the service worker can serve stale `next dev` chunks indefinitely (Turbopack dev filenames aren't content-hashed the way a production build's are) — both are now fixed/documented. **This closes out the Live Round team-scoring rearchitecture** — the next step is still the joint PWA check with the user (Chrome DevTools' real device-toolbar mode, or the dev-mobile-bypass built earlier, are both immediate ways to actually see the real student PWA on a real `User-Agent`).
- **"Season Opener Surge"** exists in the live DB (draft status, 20 real questions from `docs/starter-question-bank.csv`). All test data created during verification (rounds/teams/answers/students, plus a test event/Afterglow post/Gallery photo from this phase) has been cleaned up — the live DB is back to just the Owner account + seed data.
- **Resume note:** The user interrupted the planned doc-gen step (docs 01–11 + App PRD + Data Model & Security) and asked instead to just build the whole app, using sound judgment on anything ambiguous rather than stopping to ask, and to build everything locally even without real secrets/Docker so values could be pasted in afterward. That's what happened — no docs 01–11 exist, only this file + the two runbooks. If you want the doc set after all, it can still be generated from `docs/project-spec.md` per `docs/doc-gen-master.md`. **What changed since the first pass: everything below the code was reviewed-but-unverified (no Docker, no live DB). It's now actually verified** — real hosted Postgres, real migrations applied, real signup/login exercised in a browser, not just reasoned through. The `pgcrypto` `hmac()` call in decision 16 needed a fix once it hit a real Postgres (text args need explicit `bytea` casts, and `extensions` schema needed adding to the function's `search_path` — hosted Supabase installs `pgcrypto` there, not `public`). A local `.env.local` (gitignored) holds the real project's keys + a direct Postgres connection string; nobody should need Docker for this project going forward since there's already a shared hosted dev project.
- **Then a real test-simulation harness got built**, motivated by real bugs having repeatedly reached live demos despite hand-verification every session: Playwright (UI layer, `template/tests/jules/e2e/`) + Vitest+`pg` (DB layer, `template/tests/jules/*.test.ts`), 5 scenarios covering Live Round reconnect, stale-service-worker eviction, PWA installability + standalone-gate dual-check, cross-tenant RLS enumeration, and a concurrent registration/QR-scan race. **Every scenario had its underlying logic deliberately broken once, confirmed the test actually failed, then reverted** — the answer to "how do we know this method is good on an app that's already been hand-tested so much there's nothing left to newly discover." Two of those breaks (a live authorization function, a live schema constraint) needed a human running the SQL by hand in Supabase's SQL Editor, since Claude Code's own auto-mode classifier hard-blocks scripted mutations to security-relevant live-DB objects even with explicit in-chat authorization. Full detail + the tool-choice history (pgTAP considered, then superseded by Vitest+`pg` once it was actually built and worked) lives in the saved plan file, not duplicated here. New standing runbook: `docs/runbooks/testing-lessons.md` — real gotchas hit building/using the harness (a realtime-subscription race needing a catch-up refetch on `SUBSCRIBED`, shared-test-fixture races under Playwright's default parallelism, `storageState` silently carrying a dev-only bypass cookie into unrelated tests, etc.) — read it before writing a new scenario.
- **Then, using that harness for real, immediately**: three related asks from the user — a professor wants to watch event registrations live from their phone; a Committee Member should be able to write an Event Report from their phone (turned out to be zero new access-model work, just a mobile-usability pass, since "student coordinator" is the existing Committee Member role, decision 46); and the Event Report `.docx` export should carry real images, a registered-students roster, and an attendance summary. Scoped and built as 4 phases (Phase 0 first, added mid-planning): **Phase 0** — a second, distinct installable PWA identity for Professor/Committee Member (`/admin/manifest.webmanifest`, a plain Route Handler — Next's `manifest.ts` special-file convention only picks up one manifest at the app root, a real build confirmed a nested one silently produces nothing), student PWA completely untouched. **Phase 1** — a real-time registrations dashboard (`app/admin/(protected)/grid/[eventId]/registrations/`), then a follow-on visual pass (stat cards, a sparkline, CSV export) once the user's real motivation surfaced directly: this is a personal project meant to make a strong impression on faculty for MBA committee standing, so *how it feels* matters as much as whether it works. **Phase 2** — a mobile-usability verify-then-fix pass (two real touch-target fixes found by reading the code first, a `?event=<id>` deep link added) verified with real device emulation (`devices['iPhone 13']`, WebKit), not a resized desktop tab. **Phase 3** — the `.docx` export gained a real requireAdmin() gate (posture change: the file now carries student contact info, where it previously held only already-public text), a Registered Students table and Attendance Summary table (generated as real OOXML via the `docx` package, then spliced into the real professor-supplied template), and real embedded attachment images.
  - **Real bugs found and fixed at every phase, not just the first one** (full detail in the plan file): a realtime-subscription race in the registrations dashboard (closed with the same `SUBSCRIBED` catch-up-refetch pattern documented in `testing-lessons.md`); two Playwright test-timing bugs (a `waitForURL` matching an intermediate redirect before the real one finished; a strict-mode locator ambiguity); and, in Phase 3, a genuine third-party library quirk in `docxtemplater-image-module-free` — its `render()` branches on `typeof tagValue === 'object'` to detect its own already-resolved internal shape, but a raw image `Buffer`/`Uint8Array` is *also* `typeof === 'object'`, so passing image bytes directly collided with that branch and crashed; fixed by passing base64 strings and decoding inside `getImage()` (found via a standalone reproduction script, far faster to iterate on than the full HTTP/Playwright cycle each time). Also: the Playwright `request` fixture silently inherits a project's `storageState`, so an "unauthenticated request is rejected" test was actually authenticated the whole time — its initial pass had been masked by an unrelated render crash that happened to also return non-200 for every caller. No LibreOffice/`pdftoppm` available in this sandbox, so the docx skill's own visual PDF-render check wasn't possible; substituted the strongest available check instead (embedded image opens correctly via PIL at its real dimensions, real `<w:drawing>` OOXML generated, zero unresolved template tags in the output).
  - Every phase verified via a real Playwright test (not manual browser clicking, per the user's own explicit correction mid-session), `npm run typecheck`/`lint`/`build` clean throughout. Commits: Phase 0 `482e684`, Phase 1 `733a491` + dashboard follow-on `91ce776`, Phase 2 `76291af`, Phase 3 `3b4a565`.

## Stack

Next.js 16 App Router (Turbopack) · Tailwind v4 + tokens · Supabase (4-client split + RLS) · Vercel. PWA scaffold (`app/manifest.ts`, `public/sw.js`, `lib/pwa/register-sw.tsx`) wired to real Synergy branding/colors (decision 45).

## Conventions

Tokens only — no hardcoded hex · secrets in `.env.local` only · no new/upgraded deps without asking · git per phase, branch per phase · changing a frozen doc is a separate logged step. Full list: `docs/conventions.md`.

## Decisions made (do not revisit)

*Decisions 1–10 are pulled directly from `docs/project-spec.md`'s own "Decisions Resolved" (§9) plus earlier sections that already fixed a shape. Decisions 11+ are judgment calls made during the build where the spec was silent or self-contradictory, since the user asked for the build to proceed without stopping to ask — each is logged here, in code comments at the point of decision, and is a deliberate, loggable action to revisit if wrong.*

1. **Visual direction = "solar blue"** — full hex token table in spec §3. Set in `template/app/globals.css` (includes tier colors + the motion keyframes from §3).
2. **Tier system** — Ember (0–299) / Volt (300–599) / Current (600–999) / Plasma (1,000+), computed from `season_joules`, never stored. Spec §1/§4.
3. **Joule lifecycle** — `lifetime_joules` never resets; `season_joules` drives tier + leaderboard and resets on season end. Season cadence is admin-configurable (`Season` record: `start_date`/`end_date`/`cadence`), not hardcoded. Spec §2/§5.
4. **RBAC** — Owner/President (everything incl. Force Reset + Student Data Vault), Officer (Grid Management + Surge Builder, not the Vault), Event Volunteer (QR Scan Station only, scoped to one event, access auto-expires when it ends). Spec §7.
5. **JouleTransaction is the ledger of record** — season/lifetime totals are derived by summing it (also the audit trail), never a mutated balance column. Spec §5/§11.
6. **CSV question-import schema is final**: `question,option_a,option_b,option_c,option_d,correct_option,time_limit_seconds,tag`. Starter bank at `docs/starter-question-bank.csv` — not yet wired into a seed migration (it's real content, not a fixture, so import it through the Surge Builder's own CSV importer once there's a real Surge to attach it to).
7. **QR anti-abuse defaults** — token rotates every 90s during a live event; scan window is 15min before/after the scheduled time; geofence 150m as a **soft flag for admin review**, not a hard block; one scan per student per event. Spec §9. **Hardened beyond the spec's literal words**: spec says the repeat-scan check is "rejected client-side" — this build also enforces it server-side (a partial unique index on `joule_transactions`), since client-side-only would be a real hole in a security-first app.
8. **Concurrency plan for Surges** — pre-fetch the full question set once per Surge entry (no per-question polling); write answers to an append-only queue, not a locking balance update; compute the Surge Matrix reveal as one aggregation pass after the Surge closes, not live per-answer; rate-limit per student. Sized for 500 concurrent joiners within a 10s window (not load-tested — Postgres + RPC design supports it, but this environment can't spin up 500 concurrent clients to prove it). Spec §11.
9. **★ Season calendar — PLACEHOLDER, not real.** Seeded in `0007_jules_seed.sql` as two clearly-labeled placeholder rows (Even/Winter 2025-26, Odd/Monsoon 2026) so "today" always lands inside one. Replace via Institution Settings before the first real season.
10. **★ College email domain — PLACEHOLDER, not real.** `allowed_domains` (text array on `institution_settings`, singleton row), regex-equivalent-checked via `is_email_domain_allowed()`, editable from Institution Settings. Seeded to `yourcollege.edu`.
11. **`season_joules`/`lifetime_joules` are computed, not stored columns** — spec §5 lists them as Student fields, but §5/§11 also insists the ledger is the only source of truth ("never a mutated balance column" — decision 5). Those two statements conflict if taken as "store AND derive"; the stronger, more explicit rule wins. Resolved as SQL functions (`student_season_joules`, `student_lifetime_joules`) computed on read. Logged as a self-consistency fix, not a silent pick.
12. **Surge scoring is a flat `points_per_question` per Surge (default 20), not speed-weighted.** The spec never assigns a number to a correct Surge answer, only to event scans (10/25/50). A flat value was chosen because the spec's own Phase-2 tiebreaker ("average answer time, then earliest completion") only makes sense if raw totals tie often — which requires flat, not speed-scaled, scoring. Tunable per Surge at creation.
13. **Streak rule** (spec has `streak_days`/`last_active_date` fields but never defines the mechanic): increments once per calendar day on any Joule-earning action (event scan or correct Surge answer); no action for a full day resets it to 1 on the next action (not to 0 — it restarts the count rather than zeroing until next earn). Implemented in `_bump_streak()`.
14. **RBAC granularity the spec doesn't spell out**: `admin_manual_adjustment` and `role_change` (both named in the `AuditLogEntry.action` enum) are Owner-only, matching the Vault being Owner-exclusive. Officers cannot adjust Joules or change roles.
15. **A real cross-user data leak was found and fixed during the build**, not after: `student_season_joules()`/`student_lifetime_joules()` were initially left with Postgres's default PUBLIC execute grant, meaning any authenticated client could call them via RPC with an arbitrary student UUID and read someone else's private Joule totals. Fixed by revoking public execute on both and making `my_totals()` (the only legitimate caller for a student's own data) `SECURITY DEFINER` so it can still reach them internally while direct client calls are denied. This is exactly the class of bug the golden path's cross-user-denial gate exists to catch — logged here since it was caught by re-reading the migrations, not by running the gate against a live app (that gate still needs to be run for real once Docker is available).
16. **QR token is a deterministic HMAC over a 90-second time epoch, not a stored/mutable column.** Spec §5 lists `qr_code_token`/`qr_token_expires_at` as Event fields; this build computes the current token from `hmac(event_id || epoch, secret)` instead, so there's no cron to rotate it and no write contention across serverless instances — strictly stronger than the literal field design. `pgcrypto` + a server-only `app_secrets` table back it.
17. **Account recovery uses Supabase's built-in `resetPasswordForEmail` (an email magic-link) instead of a hand-rolled 6-digit OTP table.** Same user-facing shape the spec describes ("Lost connection?" → email → new password), fewer moving parts to get wrong.
18. **No QR-scanning camera library and no QR-image-rendering library were added** (the one-dependency rule: no new deps without asking). Instead: the QR the Grid Station displays encodes a deep link (`/scan?e=<event>&t=<token>`) that a student's native camera app opens directly — no in-app decoder needed — and the Station shows the current code as large text + a copyable link rather than a rendered QR image. Fully functional and testable end-to-end; wiring an actual scannable QR image is a flagged follow-up needing a one-line dependency decision (either vendor a small QR encoder or add a package).
19. **First Owner admin is bootstrapped via a standalone script** (`scripts/bootstrap-owner.ts`, run once via `npm run bootstrap:owner <email> <name>`), since the in-app admin-creation RPC is itself Owner-gated — a deliberate chicken-and-egg break, not an oversight. See `docs/runbooks/jules-setup.md`.
20. **`/admin/login` must never sit inside the auth-guarded admin layout** — it originally did (`app/admin/layout.tsx` wrapped every route under `app/admin/*`, including `login/`), so an unauthenticated visit to `/admin/login` hit `requireAdmin()`, got redirected back to `/admin/login`, and looped forever (`ERR_TOO_MANY_REDIRECTS`, caught live while verifying against the real DB, not by inspection). Fixed by moving every guarded route into `app/admin/(protected)/*` (a route group, same URLs) and leaving `app/admin/login/page.tsx` as an unguarded sibling. Applies generally: an auth guard's own login page can never live inside the layout that guard protects.
21. **Vercel project's team-wide "Standard Protection" (Vercel Authentication / SSO wall) was disabled for this project specifically**, via `PATCH /v9/projects/jules {"ssoProtection": null}` (no CLI command exposes this toggle). It was on by default (`all_except_custom_domains`), which would have put every deployment — including production — behind a Vercel login, meaning no student could ever reach the app. Confirmed via a live `curl`, which returned a `vercel.com/sso-api` redirect before the fix and a real `200`/`307` after. Revisit if the account's other projects rely on this protection for a reason.
22. **Vercel project's Framework Preset was stuck on "Other," not "Next.js,"** because it was created via `vercel project add` (empty project) rather than an auto-detecting first deploy. This made Vercel serve the app as a static site straight out of `public/` — the build itself succeeded (real Next.js build logs, correct route manifest) but nothing routed through it, so every real page 404'd even though the deploy reported "Ready." Fixed via `PATCH /v9/projects/jules {"framework": "nextjs"}` + a forced redeploy. **Lesson: "Ready" + a clean build log is not proof of a working deployment — a live route on the actual public URL is the only real gate**, exactly the golden path's own "works locally ≠ works live" rule (`docs/playbook.md` PART 5), just rediscovered at the infra layer instead of the app layer.
23. **Git-based auto-deploy-on-push is wired up and proven.** Bare `vercel git connect` failed because `template/` (the Vercel project root) isn't itself a git repository — it's a subfolder of the `jules/` repo — but `vercel git connect <explicit-https-url>` works around that, and `rootDirectory: "template"` was set via `PATCH /v9/projects/jules` (same API used for decisions 21/22; no CLI flag exposes it). Confirmed for real: a subsequent `git push` auto-triggered a production build that went `Building → Ready` in under a minute and aliased correctly.
24. **"Live Round" — a second, host-paced Kahoot-style quiz mode, alongside (not replacing) the original self-paced async Surge Mode.** Added after the user showed the app to a professor who asked for Kahoot-style group play with a live projector scoreboard. New tables (`live_rounds`, `live_round_teams`, `live_round_answers` — migration 0010) + RPCs (`host_create_round`, `join_live_round`, `host_advance_round`, `submit_live_answer`, `live_round_scoreboard`, `live_round_question` — migrations 0010/0011). Confirmed decisions from the user: **one phone per team** (no team-roster data model — one student hosts the team entry under their own login, Joules credit that student), **new mode alongside** the existing async one (different concurrency shape: host-paced/synchronous vs. self-paced/asynchronous), **live reveal after every question** (classic Kahoot), **one device for both host control and the projector display** (v1 scope — a separate anonymous spectator screen is a clean v2 add-on). Real-time is Supabase Realtime (`postgres_changes` on `live_rounds`/`live_round_teams`/`live_round_answers`, explicitly added to the `supabase_realtime` publication in 0010 — easy to forget, since RLS alone doesn't make a table broadcast). Routes: `/admin/live/new` + `/admin/live/[roundId]` (host), `/live` + `/live/[roundId]` (team).
25. **A real, previously-latent bug class: `RETURNS TABLE (id uuid, ...)` implicitly declares every output column (here, `id`) as a PL/pgSQL variable in scope for the WHOLE function body**, so a later unqualified `where id = ...` against a real table that also has an `id` column is genuinely ambiguous to Postgres. Found in `live_round_question()` (the Live Round team screen went blank after every phase change) — and, once every other `returns table` function was audited for the same shape, also in `start_surge()`, meaning **the original async Surge Mode has never been able to start a single Surge since it was built**; nobody had exercised it live until this session. Fixed by qualifying the table reference explicitly in both (migration 0012). Every other `returns table` function was checked and is safe — either no output column is literally named `id`, or every reference in the body was already alias-qualified (e.g. `admin_student_totals()` has an `id` output column but was always written as `s.id`, so it was never at risk).
26. **The Live Round team screen had a cold-load gap**: the scoreboard only re-fetched inside the Realtime UPDATE handler, so arriving directly at (or reloading into) the `reveal`/`leaderboard`/`complete` phase — not just transitioning into it live — rendered an empty scoreboard. Fixed by also fetching it once on mount when the initial phase is already past `question`. The host screen didn't have this gap (it already fetched unconditionally on mount).
27. **The most important bug found this session, and a system-wide one**: `_bump_streak()` (0006) sets `jules.trusted_write = on` before updating a student's own `streak_days`/`last_active_date`, and its comment claims the students self-update-restriction trigger (0005) honors that flag — but the trigger body only ever checked `is_owner()`, never the flag. Since `_bump_streak` runs as the student (not the Owner) on every correct answer, every single call raised "only name and phone are self-editable" and **rolled back the entire calling function's transaction** — meaning `redeem_event_scan`, `submit_surge_answer`, and `submit_live_answer` have never actually awarded a single real Joule since this app was built, for any student, through any path. Caught only by actually submitting a live answer and checking `joule_transactions` was empty; invisible to typecheck, lint, build, or code review of the RPCs in isolation (the bug is in a *different* migration file, 0005, than any of the three broken call sites). Fixed by making the trigger also check `current_setting('jules.trusted_write', true) = 'on'` (migration 0013). Re-verified live after the fix: a real answer now correctly inserts into `joule_transactions`, bumps the streak, and shows up on `/dashboard`.
28. **Owner-vs-student session isolation matters even when testing manually**: during verification, submitting the student signup form while a Supabase Auth session for a *different* user (the Owner) was still active in the same browser produced a `students` row keyed to the **Owner's own auth id** rather than a new user (name "Test Student" under the Owner's email) — a real footgun for manual live-testing, not an app bug (RLS correctly scopes `id = auth.uid()`; the confusion is upstream, in not fully logging out between role-switches in the same browser tab). Cleaned up; worth remembering for the next testing session: always start a fresh/logged-out state before testing a different role.
29. **A two-layer mobile-PWA-only gate for every student route**, per the professor's explicit requirement that quizzes only ever run on an installed phone app, never a laptop browser (a laptop gives a student other tabs/windows during a live quiz, defeating the whole point of Live Round's single-focus device). **Layer 1** (`proxy.ts`): a coarse mobile-OS user-agent check, scoped only to Node paths (`/dashboard`, `/scan`, `/live`, `/surge`, `/catalyst`, `/profile`) — a non-mobile UA redirects server-side to `/mobile-required` before any bundle loads. **Layer 2** (`app/(node)/pwa-gate.tsx`): since no server-side signal reliably says "installed as a PWA" across iOS/Android, this runs client-side, checking `display-mode: standalone` (plus `navigator.standalone` for older iOS) — fails closed (blocks by default; an effect flips it to allowed only once actually confirmed standalone), so nothing ever paints before the check resolves, and there's no hydration-mismatch risk since the very first client render matches what SSR already sent. **Explicitly a soft gate, not a security boundary** — same posture as the QR geofence check: a user-agent and a CSS media feature are both self-reported, and a determined person could fake either from dev tools. It stops the ordinary case (a student opens the link on a laptop) cold, and that's the whole job. Admin and General routes are completely untouched by either layer.
30. **A real hydration-mismatch bug, and a duplicate of it already latent in the codebase.** `new Date(x).toLocaleDateString()` inside a Client Component picks up whatever locale/timezone the *runtime* has — the server's during SSR, the visitor's browser during hydration — so the same call can render two different strings and React discards the tree. Found in the new Afterglow admin form's event dropdown; once found, the *exact same pattern* turned up in the original Grid Station's `event-picker.tsx`, present since that was built, never triggered/noticed before. Fixed both with one shared utility, `lib/jules/format-date.ts`, which pins both locale and `timeZone: 'UTC'` so server and client always agree. Server Components never had this risk (they don't re-render client-side), so this only ever applies to `'use client'` files.
31. **`qr_secret()` had zero grant/revoke statements and was live-confirmed callable by an anonymous caller, returning the raw QR HMAC secret** — a critical, actively-exploitable vulnerability found during a deliberate security retrospective on Goal 1, not a normal build-and-test pass. Fixed by revoking all access (migration 0015); the internal call chain (`qr_token_for_epoch` → `qr_secret`) is unaffected since SECURITY DEFINER functions execute as their owner regardless of the caller's own grants — re-verified live that `current_qr_token()` still works for a real admin after the fix. Same retrospective also fixed `submit_live_answer()` never checking the submitted question was the round's *current* one (a team could bank a correct answer for an earlier, already-revealed question during any later question's window — live-verified both the exploit is now rejected and the legitimate path still works), and closed the RBAC helpers' (`is_admin`/`is_owner`/etc.) own missing grant/revoke pair the same way `has_role.sql` does it (revoke from `public`, re-grant to `authenticated` specifically — not also `authenticated`, since RLS policies across nearly every table evaluate these as the querying role). All three in migration 0016.
32. **Force Reset never verified its target was actually a student** — the Auth Admin API it uses operates on any `auth.users` row, so an Owner could pass another admin's id as "studentId" and silently reset their password, unlike `admin_adjust_joules`/`admin_set_student_status` which are naturally FK-scoped to `students`. Fixed with an explicit existence check before the Auth Admin API call.
33. **Launch splash is purely cosmetic and fails OPEN, unlike the (security-relevant) PWA/mobile gates, which fail closed.** `lib/components/launch-splash.tsx` always renders its children immediately; the animated overlay is only ever added on top by a client effect, so a slow-JS or no-JS student never sees a blank screen — this is deliberately the opposite failure mode from `pwa-gate.tsx` (decision 29), because getting this one wrong just costs a missed animation, not a security hole. Plays once per calendar day (`localStorage` date-gated) and is skipped outright under `prefers-reduced-motion`. `motion` (Framer Motion's successor) and `canvas-confetti` were added as new dependencies for this and the escalating-drama work below — confirmed with the user first (the project's own one-dependency-rule convention).
34. **The true "Winner Declaration" moment is the round's actual completion state (Surge Matrix's rank-1 row, Live Round's `complete` phase), not every intermediate leaderboard.** Live Round's `leaderboard` phase (shown after non-final questions, and even after the final question before the host clicks "Finish round") stays exactly as understated as before — no confetti, no sound — specifically so the big beat only ever fires once per round, on the one moment it's supposed to mean something. A shared `lib/components/winner-burst.tsx` (confetti + `playSound('winner')` + a vibration pattern + a bigger `motion` spring pop) is reused by all three call sites (Surge Matrix, Live Round host, Live Round team) instead of copy-pasted, and a shared `lib/components/reveal-scoreboard.tsx` reuses the Surge Matrix's existing worst-to-best countdown-reveal technique for Live Round's final standings. The final question's `reveal` phase gets its own ~1.5s local-only suspense beat (dimmed/pulsing hold + drumroll sound + a building vibration pattern) before showing the same reveal data every other question shows instantly — a client-side pacing effect only, never a new DB phase, kept in sync between the host screen and every team's own device by using the identical duration and vibration pattern in both.
35. **Two real "tokens only" convention violations, found by grepping for hardcoded hex outside `globals.css`, both fixed.** `lib/components/energy-bar.tsx`'s countdown color had an invented `'#e0a020'` for its mid-decay amber stage — replaced with the existing Ember tier token (`var(--tier-ember-text)`), an established color already in the system rather than a new invention. `lib/components/winner-burst.tsx` hardcoded the same hex values `--gold`/`--accent` already define as a `CONFETTI_COLORS` array — replaced with `getComputedStyle` reads of the real CSS custom properties at fire-time, and generalized to accept optional `colors`/`sound`/`vibration` props so the same one confetti/sound/haptics implementation could be reused for the tier-up celebration (decision 36) with tier-specific colors and a gentler pattern, instead of a second copy-pasted implementation.
36. **A tier-up celebration finishes a feature `docs/project-spec.md` §6 already specifies but was never built** ("Crossing a tier boundary fires a bump animation and a toast"). Since `tier_for_joules()` only ever returns the current tier (no before/after pair anywhere server-side), the only place to detect a change is client-side: `lib/components/tier-up-celebration.tsx` compares the current tier against a `localStorage`-remembered last-seen tier on each dashboard visit (same fail-open, cosmetic-only posture as the launch splash, decision 33) — never celebrates the first-ever visit (nothing to compare against yet) or a downgrade (only a manual admin adjustment could cause one). Wired into `app/(node)/dashboard/page.tsx` next to the existing `TierBadge`. Live-verified all three paths (promotion fires with the right tier's confetti color; first-ever visit and same-tier reload both correctly stay silent) via a temporary throwaway harness.
37. **Two "deeper Shakti" touches from the spec's own Phase-4 list (`project-spec-v4-addendum.md`) pulled forward now, at the user's explicit request, after they asked directly whether the Joule/Shakti conceptual foundation was being left behind.** (1) `app/(node)/dashboard/page.tsx`'s Recent Activity feed now labels a student's chronologically-first-ever `joule_transactions` row "Your first spark ⚡" instead of the generic type label — found via one extra cheap query (`order by created_at asc limit 1`), since the *latest-10* feed alone can't tell you if a given row is truly the first once a student has more than 10 transactions total. (2) `lib/components/launch-splash.tsx`'s moto beat now rotates between the existing English tagline and the Vishwambhari Stuti verse the user supplied verbatim (day-of-month parity decides which, no new dependency) — **the Gujarati text was used exactly as given, never fabricated, paraphrased, or translated**, since it's a real religious text and getting it wrong would misrepresent it.
38. **Empty-state consistency sweep**: migrated ~8 hand-rolled "no data" blocks (found by a research-agent audit) to the existing shared `lib/patterns/empty-state.tsx` (already used in 17 places) — `(general)/page.tsx`, admin Ledger's engagement chart and event timeline, Vault, Settings' seasons section, Grid Station's recent scans, the Surge Builder's question list, and the Surge Matrix's no-answers state. Filled genuine gaps that previously showed nothing at all: the Scan page's empty event dropdown, Institution Settings' roster section with zero admins. Also replaced Afterglow's inconsistent `notFound()`/generic-404 case with the same inline-`EmptyState`-return idiom every other "not found" case in the app already uses (matching `surge/[surgeId]/page.tsx`'s own pattern), rather than introducing a new `not-found.tsx` boundary-file convention. Deliberately **not** touched: `lib/patterns/chip-list-editor.tsx` (Domains editor) — it's an always-editable input widget whose placeholder text already communicates emptiness, not a passive list; and Live Round's zero-teams lobby count, which already renders as a clear "0 teams joined," not a blank void.
39. **Live Round now reads as visibly distinct from async Surge Mode within the shared palette** — both previously used identical, fully neutral chrome (confirmed by reading both client components, not an intentional design being overridden). Live Round's host lobby room-code border and the host/team screens' small eyebrow/label text now lean kumkum (`var(--accent)`) instead of gold, while the shared correct/incorrect meaning (gold=correct, kumkum=incorrect) is untouched everywhere in both modes — kumkum never means two different things on the same screen. Async Surge Mode itself got no changes, staying gold-forward/calm to match its own spec-described "distraction-free" framing. Live-verified directly (admin routes aren't behind the mobile-PWA gate, decision 29, so this one didn't need the throwaway-route workaround): the room code border renders kumkum, the code text and CTA stay gold.
40. **A full color-usage audit (research agent, across every Node/Admin/General screen) found the palette already applied consistently almost everywhere — the real, concrete gap was that neither the admin sidebar (`app/admin/(protected)/layout.tsx`) nor the public site's nav (`app/(general)/layout.tsx`) had ANY active-route highlighting at all.** Fixed both by reusing kumkum's existing "you are here / current emphasis" meaning (the same one Live Round's identity already established, decision 39) — confirmed with the user this should be scoped to active-nav highlighting only, not a distinct accent color per major feature, to avoid diluting what gold/kumkum mean everywhere else. **A real bug found via live verification, not just reasoned through**: the admin sidebar's first implementation attempt passed pre-built nav items (including Lucide icon component references) as a prop from the Server Component layout into a new Client Component — Next.js threw "Only plain objects can be passed to Client Components from Server Components" at runtime, since a component reference isn't plain-serializable data, unlike a rendered React element. TypeScript's own type system doesn't know about this constraint (`LucideIcon` typechecks fine as a prop type), so this was invisible to `typecheck`/`build`/`lint` — only caught by actually loading the page in a browser. Fixed by having the Client Component (`admin-nav-client.tsx`) own the full nav array (icons included) itself, and passing down only the plain `role: string` for filtering, never a component reference across the boundary. Also fixed a small real drift (Vault's search input used `border-border-muted`, the token the spec reserves for onboarding-style inputs, instead of the `border-border` every other admin input uses) and documented two audit findings confirmed as deliberate exceptions rather than bugs: `engagement-chart.tsx` repurposing gold/accent/success as a 4-category chart palette, and `winner-burst.tsx`'s literal `#ffffff` confetti sparkle (no true-white token exists; off-white `--foreground` would look worse in confetti).
41. **No Midjourney/GPT-Image tool is available in this session, so static art gets hand-coded as real SVG rather than AI-generated** — confirmed with the user directly rather than assumed. `public/icons/icon.svg` (feeds the PWA manifest icons, browser favicon, and the launch splash's logo beat) replaced the placeholder lightning bolt with an atom-orbit + spark mark (two crossed ellipses in gold + a bolt at center + one kumkum accent dot at the orbit intersection) — built from the same "atom generating a spark" concept already in `site.tagline`, kept within the maskable-icon safe zone (all content within roughly the center 80% of the 512×512 canvas) and using thick, high-contrast strokes deliberately so it survives downscaling to favicon size. `lib/components/gallery-placeholder.tsx` — a hand-coded abstract "energy grid" node-and-line illustration for the public Gallery's empty state, replacing the generic `ImageIcon`, since the club has no real event photos yet. Deliberately NOT a mockup pretending to be a real photo — an illustration can't be mistaken for actual documentation of an event that didn't happen, which a faked "photo-style" placeholder could be.
42. **The homepage got an interactive hero, scoped deliberately narrow after direct discussion with the user** (inspired by a reference site's animated hero) — public homepage only, never the mobile PWA (deliberately distraction-free for quiz integrity, per the professor's own requirement behind decision 29) or admin (needs to stay fast/glanceable for daily operational use, not a landing-page moment). `lib/components/energy-field.tsx` is a full-page, `position: fixed` ambient particle-network background — deterministic nodes/lines (same visual language as `gallery-placeholder.tsx`, decision 41), pure-CSS idle drift, kept deliberately low-opacity so body text on every section stays fully readable (confirmed live: body text renders at full `opacity: 1`, entirely independent of the background layer beneath it). `lib/components/hero-atom.tsx` is a bigger, animated version of the new app icon's atom-orbit mark (decision 41), scoped to just the hero section, with a slow auto-rotation and a subtle pointer/touch parallax. **A real, instructive bug found live, not just reasoned through**: the first implementation used `motion/react`'s `animate` prop and `style={{x, y}}` on nested SVG `<g>`/`<motion.svg>` elements — no error was thrown, no console warning, the code typechecked and built clean, but the transform silently never applied (confirmed via `getComputedStyle` showing `transform: none` after the animation should have progressed). Rather than keep debugging `motion`'s SVG-specific transform handling, rewrote both pieces using the same plain, already-proven techniques used elsewhere in this codebase: a plain CSS `@keyframes` rotation (same pattern as `energy-field.tsx`'s node drift) and plain React state + a CSS `transition` for the pointer parallax, checking `prefers-reduced-motion` via raw `window.matchMedia` (same pattern as `count-up.tsx`/`matrix-client.tsx`) instead of `motion`'s own hook. **A genuine testing-tooling limitation, disclosed rather than glossed over**: a synthetic, script-dispatched `PointerEvent` reaches a plain `addEventListener` listener (confirmed) but does not reliably trigger React's synthetic event handling for `onPointerMove` in this session's browser tooling — the parallax logic itself was verified correct by careful code review (the same confidence level as other reduced-motion-path verifications this session), but a live human touch/mouse test is still the real remaining proof.
43. **Added `react-qr-code` to close the QR-image gap flagged since decision 18** — confirmed with the user first (the project's own no-new-deps-without-asking convention). A small, zero-dependency, actively-maintained library that renders a real QR as an SVG (not canvas/raster), which composes cleanly with this project's token system and matches the "hand-coded SVG" posture already established this session. Wired into all three places it was wanted: the Grid Station's check-in code (`station-client.tsx`, alongside the existing text/copy-link, which stays as the accessible fallback), the Live Round host lobby's room code (`host-client.tsx`'s `LobbyView`, encoding `${site.url}/live?code=<roomCode>`), and `/get-app` (encoding the bare site URL). Also extended `/live`'s join page to read a `?code=` query param and pre-fill the room-code field (same Suspense-wrapped `useSearchParams()` pattern as the auth pages), so scanning the host lobby's QR actually skips typing the code, not just links to the join page. **Deliberately kept the QR itself standard black-on-white, not theme-colored** — real-world scan reliability across random phone cameras and lighting matters more here than matching the dark UI, and an inverted/tinted code risks scanning worse in practice. Verified live: `react-qr-code` renders correctly as a Server Component (no `'use client'` needed, `/get-app` still prerenders statically) and the SVG path data was confirmed to contain real encoded module geometry, not an empty/broken render.
44. **Goal 2, Stage 1 of the solarpunk avatar expansion**: a 3D character on Profile that visibly gains cosmetics as Joules accumulate, plus a Dashboard teaser — decided after a full design-system pass (an Artifact-based token/typography/imagery reference) and dedicated wireframes for Profile, Dashboard, the quiz-answer button redesign, and the cosmetic-unlock celebration storyboard, all confirmed with the user before any code. `three`, `@react-three/fiber`, `@react-three/drei`, `gsap`, `@gsap/react` added as new dependencies (confirmed in the approved plan). Migration 0017 (`avatar_items` table + `avatar_items_catalog()` RPC, same deny-by-default/narrow-RPC posture as `public_events()`) applied live — seed colors deliberately diversified across two new tokens, `--cosmetic-rare` (`#C98A3E`) and `--cosmetic-epic` (`#2E8B7A`), fixing a real finding from the design-system pass where four different items shared an identical gold hex. Unlock state is computed client-side from `lifetime_joules` vs. each item's `joule_threshold`, same "computed, never stored" philosophy as tiers (decision 11) — no per-student unlock-state table. Built: `avatar-scene.tsx` (the R3F scene, placeholder icosahedron standing in for the real character until a sourced/generated model lands, `frameloop="demand"`, capped `dpr`, low-power/no-AA GL, `OrbitControls` scoped to the canvas), `avatar-backdrop.tsx` (pure SVG/CSS, reusing `hero-atom.tsx`'s orbit rings and the site's particle-network language — no extra runtime cost), `collection-tree.tsx` ("Your Collection," a threshold-ordered tech-tree with a GSAP `useGSAP()` unlock reveal — glow → scale-in → label swap — scoped to its own container), `dashboard-teaser.tsx` (a static teaser card with a one-time GSAP ScrollTrigger reveal, `once: true`, never `scrub`). **Two real bugs found only by building and running it, invisible to typecheck/lint/build**: (1) `next/dynamic(..., { ssr: false })` cannot be called from a Server Component in Next 16 — `profile/page.tsx` needed a tiny Client Component wrapper (`avatar-scene-loader.tsx`) to own the dynamic import, keeping the page itself a plain async Server Component. (2) React Three Fiber's own `<Canvas>` only initializes at all (calls `createRoot`/`onCreated`) once its internal `react-use-measure`-based resize detection reports a non-zero container size — traced directly in R3F's source. **Disclosed, not silently worked around**: in this session's sandboxed browser tool specifically, that internal measurement never resolves, even for a bare minimal `<Canvas>` in a plain fixed-pixel div with no aspect-ratio CSS involved — confirmed by testing the simplest possible case, ruling out anything specific to this project's component. A raw, manually-attached `ResizeObserver` on the identical DOM node *does* fire correctly in the same browser, so `resize={{ polyfill: ResizeObserver }}` (R3F's own documented escape hatch for exactly this situation) is applied as the correct fix regardless — but this session's tooling could not independently confirm the canvas paints at the right size and aspect ratio, since even that fix didn't move the needle inside this specific sandbox. Everything else was verified directly: clean `typecheck`/`lint`/`build`, no console errors, correct data flow (unlock math, threshold ordering, item colors) confirmed via real rendered DOM text, and confirmed via `grep` that the Three.js/R3F bundle is imported only by `/profile`, never by `/dashboard` or any other route. **A real phone check of the live 3D canvas is the next real proof**, same disclosure posture as this project's other real-device gaps.
45. **Platform pivot to "Synergy," after a live professor review of the single-club "Jules" build (well received) surfaced 11 pieces of feedback that add up to a fundamentally different product**: not an app for one energy club, but the shared engagement platform for all 12-13 clubs in the Adani University FMS/Infra Management MBA program, with per-student registration/attendance-based streaks, a full universal leaderboard (every student ranked, not top-10), group-vs-individual quiz registration with pooled/negative/time-based scoring, a 3-tier RBAC (Student/Committee Member/Professor), and a templated Event Report builder. Name and visual direction were resolved through direct rounds with the user, not invented: **name = "Synergy"** (the one candidate reflecting all three things the professor asked for — Progress, Club, Student — chosen specifically because "Jules" is itself a real club's name in the program, which would confuse students about what the app even is), **colors = direction "A"**, an Adani-inspired corporate palette (Honolulu Blue background family, X11 Maroon `#BD3861` as the new brand/CTA color replacing gold, and a distinct warm orange-red `#E0521F` for danger/incorrect — deliberately different hues, ~340° vs ~14°, so "wrong answer" and "brand identity" never visually collide), since the platform now sits inside, and should visually belong to, Adani University specifically. The 3D avatar/cosmetic-unlock system from decision 44 is removed entirely — built for the single-club version, not asked for in the pivot. The full build is staged (Phase 0 rebrand/cleanup, then multi-club data model + RBAC, then registration/streak, then leaderboard, then quiz scoring) rather than attempted in one pass, since it touches nearly every layer of the app; each phase is independently live-verified before the next starts, same discipline as every other phase in this project. **Phase 0 and Phase 1 are both complete** (see the newest Build Log entries) — Phases 2–4 are approved (full plan on file) but not yet started.
46. **Pivot Phase 1 — the multi-club data model + the 3-tier RBAC itself, not just the rename.** New `clubs` table (`id, name, slug, description, created_at`); `events` and `surges` each get a required `club_id` FK — students get no club FK at all (open participation, confirmed with the user: any student can register for any club's activities). `admins.role` collapses from `owner`/`officer`/`volunteer` to `professor`/`committee_member` — migration 0020 checked the live data first (3 admin rows, all `owner`; 0 events; 3 surges) rather than assuming, so the backfill risk was known before writing a line of SQL. A Committee Member is scoped to exactly one club via `admins.club_id`; a Professor is platform-wide (`club_id` is null) and is the only role that can call `admin_adjust_joules` (manual point adjustment stays the one power nothing else gets, per the professor's own point 11). The old fine-grained, auto-expiring Volunteer role is dropped entirely (a deliberate simplification — the professor named exactly 3 tiers, not 4) — flagged as easy to reintroduce later as a Committee-Member sub-tier if a club actually wants shift-based helpers. A ★ placeholder "General Club" is seeded (same convention as decisions 9/10) since the real 12-13 club roster doesn't exist yet; a new "Clubs" section in Institution Settings (Professor-only) lets clubs be added for real.
    - **Every RLS policy and RPC that used to check `is_owner()`/`is_officer_or_owner()` was re-audited, not just renamed** — this was the explicit ask, and the exact class of bug this project's own retrospectives (decisions 15, 27, 31) have repeatedly found hiding in exactly this kind of "should be a simple rename" work. New helpers: `is_professor()`, `is_committee_member()`, `is_committee_member_or_professor()`, and the scoping primitive `can_manage_club(club_id)` (a Professor can manage any club; a Committee Member only their own) plus `can_manage_event(id)`/`can_manage_surge(id)` built on it. **Writes** to events/surges/questions are now club-scoped (a Committee Member can only create/edit their own club's rows; only a Professor can delete an event); `log_csv_import`, `host_create_round`, and `host_advance_round` are scoped the same way via the surge's club. **Reads stay platform-wide for staff** (the System Ledger reports, the full Joule ledger, Live Round content) — this was Phase 1's explicit scope (write-path scoping), not a full reporting audit; flagged as a follow-up in Known Open Items, not a silent gap. Admin list pages (Grid Station's event picker, the Surges list) additionally filter to a Committee Member's own club at the query level, so the "can't view another club's stuff" verification requirement holds at the UI layer too, even though the underlying `events`/`surges` SELECT policies stay broad (needed for the public calendar and, in a later phase, student registration).
    - **A real bug caught before it ever reached the live DB**: the first draft of the migration ran the role-value UPDATE statements (`'owner' → 'professor'`, etc.) *before* dropping the old check constraint — Postgres rejected every row write since `'professor'` wasn't yet a legal value under the still-live `('owner','officer','volunteer')` constraint. Caught immediately by the migration tool's own transactional rollback (`migrate up` failed cleanly, no partial state), not by careful reading beforehand — fixed by moving the constraint drop before the data writes.
    - **A second real bug, a genuine Postgres constraint not obvious from the docs**: `CREATE OR REPLACE FUNCTION` refuses to rename an input parameter even when the type signature is unchanged (`admin_create_admin`/`admin_set_role`'s `p_volunteer_event_id` → `p_club_id`), raising "cannot change name of input parameter" — fixed with an explicit `DROP FUNCTION` before the `CREATE OR REPLACE`, and the migration's `down` path was checked to make sure it actually recreates the *original*-signature functions rather than just dropping them (an easy thing to get wrong in a migration this size, and the first draft did get it wrong before a second pass caught it).
    - **Live-verified directly, not just reasoned through**: with the migration applied, ran a real cross-club RLS test — a Committee Member seeded onto the placeholder club, a second real club created as a test fixture, a surge attached to that second club. Confirmed, via `SET LOCAL request.jwt.claims`/`SET LOCAL ROLE authenticated` (the same technique Supabase's own PostgREST layer uses per-request): the Committee Member's `UPDATE` on the other club's surge affected 0 rows (denied), their `UPDATE` on their own club's surge succeeded, `can_manage_surge()` returned `false`/`true` correctly for the two cases, a Professor's `UPDATE` on the other club's surge succeeded (platform-wide), and `log_csv_import` correctly raised "not authorized" for a cross-club surge id. All test fixtures (the second club, the test surge, the temporary Committee Member admin row) were cleaned up immediately after — the live DB is back to exactly its pre-test state (1 placeholder club, 3 Professor admins, the same 3 surges). `npm run typecheck`/`lint`/`build` all passed clean throughout.
47. **Pivot Phase 2 — event registration + a fully redefined, attendance-based streak.** New `event_registrations` table (`event_id, student_id, registered_at, attended_at`). A student registers ahead of time via `register_for_event()` (rejects surge-type events, already-ended events, and double-registration); `unregister_from_event()` lets them cancel any time before they've actually attended, but never after (same "ledger, not a mutable balance" discipline as `joule_transactions`, decision 5) — once `attended_at` is stamped it's a real record, not theirs to delete. Attendance is credited at the exact same QR scan students already do (`redeem_event_scan`, mechanism otherwise unchanged): a matching registration gets `attended_at` stamped alongside the existing Joule credit; a walk-in scan with no prior registration still credits Joules exactly as before, it just never touches the streak. Shown on the Dashboard as two new sections — "Upcoming events" (with inline Register/Cancel buttons) and "My registered events" (Registered / Attended / Missed status per row) — per the plan's own stated UI location.
    - **The streak itself is a full redefinition, not a rename**: it used to mean "opened the app today" (`_bump_streak()`, decision 13, bumped on every correct quiz answer or QR scan); it now means "how reliably has this student shown up for what they registered for." Computed on read via `student_attendance_streak()` — same "computed, never stored" philosophy as tiers/season_joules/lifetime_joules (decision 11) — by walking a student's already-concluded registrations backward from most-recent, counting consecutive attended ones until the first no-show. A no-show doesn't freeze the count, it resets it to 0 immediately; the *next* successful attendance starts counting from 1 again. A stored running counter would get this subtly wrong the moment two events conclude out of registration order; a query recomputed fresh every time cannot drift. `_bump_streak()` and its call sites in `submit_surge_answer`/`submit_live_answer` are removed entirely — quizzes no longer move the streak, only registered-event attendance does. The now-dead `students.streak_days`/`last_active_date` columns are dropped, and the field itself is renamed `streak_days` → `streak` everywhere it's returned (`my_totals`, `admin_student_totals`, and every frontend display) — it no longer counts calendar days, and keeping the old name would misrepresent what the number means now.
    - **Two real bugs, both the same class already seen in decision 46's migration** (Postgres's `CREATE OR REPLACE FUNCTION` refusing certain signature-adjacent changes) — `cannot change return type of existing function` when trying to rename `streak_days` → `streak` in `my_totals()`/`admin_student_totals()`'s return tables. Fixed the same way as before: explicit `DROP FUNCTION` first, and the migration's `down` path double-checked to make sure it drops-then-recreates in both directions rather than erroring the same way in reverse.
    - **Live-verified the actual logic, not just the schema**: since the Node routes are gated behind the mobile-PWA gate (decision 29) and this session's desktop browser tooling can't pass it, verification for this phase leaned on direct RPC/SQL tests against production instead of a screenshot — arguably the stronger proof for a business-logic feature like this one. Confirmed via `SET LOCAL request.jwt.claims`: `register_for_event` succeeds for a live not-yet-ended event and correctly rejects a second registration, an already-ended event, and (implicitly, via the `type <> 'surge'` check) a quiz. Confirmed `redeem_event_scan` both credits Joules and stamps the matching registration's `attended_at` in the same call. Confirmed `unregister_from_event` deletes an unattended registration but leaves an attended one alone. Confirmed the streak formula itself with a constructed attend→attend→**miss**→attend→attend→attend sequence: computed streak was `1` right after the miss (only the most recent attendance counted, the two before the miss did not), then `3` once two more consecutive attendances were added on top — exactly the "reset to 0, then recount from the next success" behavior the plan called for. All test fixtures (events, registrations) were created and cleaned up directly against the live DB; confirmed clean afterward. `npm run typecheck`/`lint`/`build` all passed clean throughout (one real lint catch: two inline `Date.now()` calls inside the Dashboard's render body tripped the project's own `react-hooks/purity` rule — fixed with a standalone `hasConcluded()` helper, the same established pattern this file's own `daysUntil()` helper already uses).
48. **Pivot Phase 4 (professor point 6): "all student will get a rank even if there are 400 then all 400 will be on points and there will be leader board."** `season_leaderboard()` (0008) turned out to already have no top-N cutoff — it was never capped at 10 — but it silently excluded any student with zero Joules for the season (`having ... > 0`), which is exactly the gap the professor called out: a 0-point student still needs to see their rank. Fixed **in place** rather than adding a redundant parallel `platform_leaderboard()` RPC as the plan's own draft wording suggested, since Joules were never club-scoped (decision 46's "points stay universal ledger") — `season_leaderboard()` already *is* the platform-wide leaderboard the plan describes, it just had a filtering bug, not a naming problem. Added real pagination (`p_limit`/`p_offset`, default page size 50) so a 400-student roster doesn't have to render as one unbounded list; `total_count` rides along on every row via `count(*) over ()`, which — because window functions evaluate before `LIMIT`/`OFFSET` in SQL's logical processing order — always reflects the true total regardless of which page was requested, not just the current page's row count. Catalyst Records (the existing season-browsable screen, reused rather than building a new one, per the plan's own instruction) gained a `page` query param and Previous/Next links plus an "X–Y of N students" caption; the empty state changed from "Nobody had earned Joules in this season yet" to "No students yet," since the only way the list is now empty is if there are literally no students in the system.
    - **A third instance of the same Postgres migration lesson from decisions 46/47**: adding new trailing parameters (`p_limit`, `p_offset`) to an existing function changes its call signature/identity for Postgres's own overload resolution — `CREATE OR REPLACE FUNCTION` would have created a *second*, separately-callable overload alongside the old argument-count-mismatched one rather than truly replacing it. An explicit `DROP FUNCTION public.season_leaderboard(uuid)` first avoided that. Three phases in a row have now hit a variant of this same restriction (parameter rename, return-column rename, parameter count change) — treated as a standing rule going forward: any `CREATE OR REPLACE FUNCTION` that changes anything about a function's declared shape, not just its body, gets an explicit `DROP FUNCTION` first, no longer discovered by trial and error each time.
    - **Live-verified directly against production**: applied the migration, then queried the real (small, 4-student) live roster's active season — confirmed all 4 students appear ranked, including the 3 with zero Joules (correctly tied at rank 2, standard SQL `rank()` semantics — ties share a rank and the next distinct value skips accordingly). Confirmed pagination windows correctly across two pages (limit 2/offset 0, then limit 2/offset 2) with `total_count` staying `4` on every row of both pages, proving the window function computes over the whole result set before `LIMIT` trims it, not just the returned page. `npm run typecheck`/`lint`/`build` all passed clean. The actual `/catalyst` page itself wasn't directly browser-loaded — same mobile-PWA-gate limitation as decision 47 — but the RPC's correctness (the real risk in this phase) was proven at the database layer directly.
49. **Pivot Phase 4 — the last approved pivot phase, and the largest single mechanic in the whole pivot: group/individual quiz registration with pooled scoring, plus negative and time-based marking for every quiz answer.** Four design questions were explicitly open per the plan and were resolved with the user via `AskUserQuestion` before any code (all four "Recommended" options picked): pooled points that don't divide evenly round down, with the leftover distributed one point at a time to the fastest answerers in the group; the time bonus is a continuous formula (`base_points × remaining_time / time_limit`), not tiered brackets; negative marking is a flat, per-Surge-configurable penalty per wrong answer (0 = off); and Committee Members can configure all of this themselves, same access as any other Surge — no Professor-only carve-out.
    - **Scope, resolved by re-reading the plan's own contrast against Live Round rather than assumed**: group registration and pooled scoring apply *only* to async Surge Mode — every member answers individually on their own device, and the group's combined result is redistributed once the Surge closes. Live Round keeps its existing "one device per team" real-time model entirely (decision 24) untouched; it only picks up the new scoring *formula* (time-weighted correct answers, and now an actual ledger row for a wrong answer when negative marking is configured — previously wrong answers silently credited nothing at all).
    - **Built**: migration 0023 — `quiz_groups`/`quiz_group_members` (the latter denormalizes `surge_id` specifically so a real `unique(surge_id, student_id)` constraint guarantees a student can never belong to two groups for the same Surge, closing a race condition an RPC-level check alone wouldn't fully prevent); two new `surges` columns (`participation_points_per_question`, `negative_points_per_wrong_answer`); a shared `surge_answer_points()` formula function used by both the instant per-answer preview and the batch finalization pass, so the math exists in exactly one place; `create_quiz_group()`/`join_quiz_group()`/`leave_quiz_group()` (locked to `status = 'draft'` — a group's roster is fixed before the quiz starts, matching the existing prefetch-once architecture, decision 8); and `complete_surge()`, a new finalization RPC that replaces a raw status update for the "mark complete" transition — it computes each group's participation and earned pools from every REGISTERED member's contribution (not just those who happened to answer, so a member who joined but never played still counts toward the divisor, exactly as a real classroom group quiz would treat it), splits both pools via the confirmed floor-plus-remainder-to-fastest rule, and credits the ledger in one atomic pass. `submit_surge_answer()` no longer writes `joule_transactions` at all — Joule credit for async Surge Mode is now fully deferred to `complete_surge()`; it still returns an instant correct/incorrect + computed-points preview for the quiz UI, which just no longer implies the ledger write already happened.
    - **A real, necessary rename**: `joule_transactions.type` value `'surge_correct_answer'` is renamed to `'surge_earned'` (it no longer means literally "one row per correct answer" — it's now a single pooled amount for async Surge Mode, though Live Round still credits it per-answer) and a new `'surge_participation'` type is added; `surge_leaderboard()` updated to sum both. Two new partial unique indexes (`uq_one_surge_earned_pool_per_student`, `uq_one_surge_participation_pool_per_student`) are defense-in-depth against `complete_surge()` ever double-crediting the same student for the same Surge, on top of the row lock (`select ... for update`) inside the function itself that serializes concurrent calls.
    - **The exact same Postgres migration-ordering bug from decision 46's migration, hit again**: the data-migration `UPDATE` renaming existing rows to `'surge_earned'` ran before the old check constraint was dropped, so the write violated the still-live `('surge_correct_answer', ...)` constraint. This is now the *second* time this exact ordering mistake has been made in this project's migrations — logged here explicitly as a checklist item for any future migration that renames an enum-like check-constraint value: drop the constraint, THEN write the data, THEN add the new constraint, never the reverse.
    - **Verified far more rigorously than a hand-checked example, on purpose**: rather than hand-computing expected pooled amounts (real risk of arithmetic error in a review), the same `surge_answer_points()` formula was independently reimplemented in a plain JS test script, which then drove the *real* RPCs (`submit_surge_answer` for a constructed 4-student scenario — a 3-person group with mixed correct/wrong/timed answers and one member who skipped a question, plus one solo player — followed by `complete_surge()`) and diffed the actual database output against the JS-computed expectation. All 8 resulting ledger rows matched exactly, including the correct fastest-two-members-get-the-remainder split and the solo player receiving their full undivided pool. Separately confirmed: `submit_surge_answer` writes zero `joule_transactions` rows before `complete_surge()` runs (proving the deferred-crediting design); every group-formation guard (one group per student per surge, no forming/joining/leaving once the Surge is live); and Live Round's `submit_live_answer` now correctly inserts a real `-5` ledger row for a wrong answer under negative marking, where before it credited nothing. All test fixtures (surges, questions, groups, live rounds) were created and fully cleaned up directly against the live DB. `npm run typecheck`/`lint`/`build` all passed clean. The admin-facing New Surge form (not gated behind the mobile-PWA wall) was directly browser-verified showing all three new scoring fields; the student-facing group-registration screen sits behind the same gate as every other Node route this pivot has hit, so it relies on the RPC-level proof above instead of a screenshot.
50. **Event Report template rebuilt to match the real Adani University "Event Completion Report" — a genuine reference document the user shared, not a spec description.** The Phase 0 template (decision 45: Title/Highlights list/Summary paragraph) was a reasonable guess at "everything is described in a pattern, just need to fill gaps" (professor's point 10), but the real institutional format is meaningfully different and more detailed: Event Name/Date/Time/Venue/Organised by/Coordinator, then Introduction (paragraph), Objectives (bullets), Event Highlights (a *paragraph*, not a bullet list — the Phase 0 guess had this backwards), Outcomes (bullets, didn't exist before), Conclusion (its own paragraph, previously conflated into one "Summary"), and an Attachments checklist. Confirmed with the user before rebuilding: Date/Time/Venue/Organised-by-club auto-fill from the linked event, read-only, rather than being retyped — the event row is already the source of truth for those fields elsewhere in the app, and this is exactly the "fill the gaps" philosophy the professor asked for. `title` is kept as its own column but now auto-derived from the event's own name at creation (no longer a separate user-typed field), so a report's identity stays stable even if the event is later renamed.
    - **Built**: migration 0024 restructures `event_reports` (drops `summary`/`highlights`, adds `coordinator_name`, `introduction`, `objectives text[]`, `event_highlights`, `outcomes text[]`, `conclusion`, and four `attachment_*` booleans — zero live rows existed, confirmed before writing the migration, so this was a clean restructure, not a data migration) and extends `public_events()` with the organising club's name (an explicit `DROP FUNCTION` first, applying the now-standing rule from decisions 46–48 rather than hitting the same Postgres return-type-change error a fourth time). The admin form and public detail page were both rewritten to match the real template's exact section order and shape.
    - **A real, previously-latent bug caught during this exact verification pass**: the public detail page's event date/time display used `new Date(...).toLocaleDateString/toLocaleTimeString(undefined, ...)` in a Server Component — with no explicit locale/timezone pin, this renders in whatever timezone the *deploying server* happens to run in, not the event's actual intended time. Live-verified concretely: an event stored as 11:00 AM UTC rendered as "4:30 p.m." (the sandbox's local UTC+5:30 offset) before the fix. Switched to the project's existing `formatDateUTC()`/`formatTimeUTC()` helpers (`lib/jules/format-date.ts`, originally built for decision 30's client-hydration-mismatch fix) — pinning to UTC also happens to solve this different-but-related problem (deployment-timezone-dependent display), even on a Server Component with no hydration risk at all. `formatTimeUTC()` is new, added alongside the existing `formatDateUTC()`.
    - **Live-verified end-to-end with the actual real-world example**: created a real test event ("Bid to Build — Constructing Future Infrastructure," Cityscape Club, 8 Dec 2025, 11:00 AM, Classroom 2) and filled out the real admin form using the exact content from the user's supplied filled-example document. Confirmed the auto-fill correctly pulled Date ("Dec 8, 2025"), Time ("11:00 AM"), Venue, and Organised-by-club the moment the event was selected, with no manual retyping. Confirmed the created report's data matches exactly what was entered (coordinator name, introduction, objectives array, event highlights, outcomes array, conclusion, and the two checked attachment booleans) via a direct database query. Confirmed the public detail page renders every section in the template's real order, including the objectives/outcomes bullet lists and the attachments checklist showing only the checked items. All test fixtures (the event, the club, the report) were cleaned up immediately after, confirmed clean via a follow-up query. `npm run typecheck`/`lint`/`build` all passed clean throughout.
51. **Event Report .docx download — the real institutional format is now downloadable, not just viewable on the public page.** Confirmed with the user via `AskUserQuestion`: Word (.docx) only, one small new dependency accepted (`docx` npm package) — the print-friendly-PDF-view and PDF-only alternatives were both explicitly declined. Built a Route Handler (`app/api/event-reports/[id]/docx/route.ts`) that assembles the exact same section order as the real AU template (label lines, Introduction, Objectives, Event Highlights, Outcomes, Conclusion, Attachments) using the `docx` package's `Document`/`Paragraph`/`TextRun` API, served with `Content-Disposition: attachment`. No auth check on the route itself, by design — `event_reports` already has a public "anyone reads" RLS policy and the same content is already fully rendered on the public detail page with no login required, so a downloadable copy needs no additional access control.
    - **A real bug found during live verification, not a coding mistake caught by lint/typecheck**: the download link worked from a logged-in browser session but returned a redirect to `/login` for an unauthenticated request (confirmed via `curl`). The route handler itself was correctly built with no auth check — the redirect was coming from `proxy.ts`'s global middleware, whose `PUBLIC_PATHS` list included the page route `/event-reports` but not the API route `/api/event-reports`, so an anonymous request never reached the handler at all. Fixed by adding `/api/event-reports` to `PUBLIC_PATHS`. This is a reminder that a page being public and its supporting API route being public are two separate things that both need declaring — this project's route-protection logic is table-driven exactly so this kind of gap is a one-line fix once found, but it isn't automatic.
    - **Live-verified past the point of "the response looked right"**: after the middleware fix, fetched the actual generated file via `curl` (200 OK, correct `Content-Type`/`Content-Disposition` with a sanitized filename), then unzipped it directly and grepped `word/document.xml` for the `<w:t>` text nodes — confirmed every field (event name, date, time, venue, club, coordinator, introduction, objectives, event highlights, outcomes, conclusion, attachments) round-trips correctly into the real document body, not just that a response came back. `npm run typecheck`/`lint`/`build` all passed clean. Test fixtures (event, club, report) created for this pass were cleaned up directly against the live DB afterward.
52. **The initial .docx (decision 51) matched the real template's *content* but not its *visual layout* — the user pointed at the actual reference document (a bordered masthead table with the real Adani logo, lettered Objectives/Outcomes, numbered Coordinator/Attachments) and asked why the download didn't look like that.** Extracted the real logo directly from the two reference `.docx` files the user originally supplied (`word/media/image1.jpeg` inside the zip, 330×176) rather than asking for a fresh asset — it was already sitting inside documents already shared. Rebuilt the Route Handler's `Document` tree as a real bordered `Table` (logo cell vertically merged down 3 rows via `VerticalMergeType.RESTART`/`CONTINUE`, title row spanning both columns, Event Name/Date/Time/Venue as labeled cells) followed by Organised-by/Coordinator/Introduction/Objectives/Event Highlights/Outcomes/Conclusion/Attachments — Objectives and Outcomes now use a real `lowerLetter` numbering format (a, b, c…) and Coordinator/Attachments a real `decimal` format (1, 2, 3…), each as an independent `numbering.config` reference so every list restarts fresh rather than sharing one running counter.
    - **Confirmed with the user via `AskUserQuestion` where the real logo should additionally appear on the site** (separate from the .docx): nav headers only (general public site + admin panel), not the PWA app icon — the custom "Synergy" icon (decision 41) was a deliberate earlier choice and stays untouched. The extracted logo's JPEG had a solid white background (JPEG can't hold alpha); since both nav headers use the dark `--card` token (`#0d2436`), the raw file would have shown an ugly white box. Used `sharp` (already present as a transitive dependency, confirmed before reaching for a new package) to key near-white pixels to transparent and composited the result against the actual dark header color to verify it reads cleanly before wiring it in — two separate asset files now exist under `template/public/brand/`: the transparent PNG for on-screen nav use, and the original opaque JPEG for the .docx (Word pages are white, so the pristine source is the safer embed there, no keying-artifact risk).
    - **Verified the table/numbering XML directly, not just that the file downloads**: unzipped the regenerated `.docx` and confirmed the embedded media file byte-matches the source logo, exactly one `<w:tbl>` exists, and `word/numbering.xml`'s `<w:num>`→`<w:abstractNumId>` mapping shows 4 independent abstract numbering definitions (2 `lowerLetter`, 2 `decimal`) rather than one shared instance — the concrete risk with reusing a single numbering reference across two lists is the second list silently continuing the first's count instead of restarting at "a"/"1". Confirmed the logo `<img>` tag and its `/_next/image` optimizer endpoint both resolve with a real request via `curl` (the in-app Browser pane was down with an unrelated infra error during this pass, so verification leaned on `curl`+direct XML inspection instead of a screenshot). `npm run typecheck`/`lint`/`build` all passed clean; test fixtures cleaned up.
53. **Two quick corrections to decision 52, both requested directly by the user right after seeing it live**: (a) the professor wants a **light** background, not the dark navy canvas every screen has used since decision 97 — flipped `globals.css`'s token *values* only (background/card/border/foreground/muted/tertiary, plus the tier-badge colors, which were subtle dark tints tuned for the old dark canvas and would've read as jarring solid boxes on a light one) to a warm-ivory light palette (direction "C" of three mood-boarded options), keeping the maroon brand accent (`--gold`) untouched since it already reads correctly on light — same "tokens only, value swap not rearchitecture" approach decision 97 itself used. PWA `theme_color`/`manifest.ts`/`appleWebApp.statusBarStyle` updated to match (was `black-translucent`, now `default`, since that combination assumes a dark status bar). (b) The Adani logo moved **out of both nav headers entirely** and into the general public site's footer only — the admin panel has no footer, so it was just removed there, not relocated.
54. **Event registration via an external Google/Microsoft Form, in-app reminders, and QR-confirmed attendance/streak — built on top of the registration/attendance backbone Phase 2 (decision 47) already shipped, not a rebuild.** The professor's actual workflow: a club attaches its own external form to an event for whatever data it wants to collect (team size, dietary needs, etc.); a student registering in the app should both be recorded in our own system AND land on that external form, see their registered events in the PWA, get reminded the day before (and if the venue changes last-minute), and have attendance/streak still confirmed by the existing QR scan — registration is an "I'm coming" signal, not proof of attendance. Two real design decisions were resolved with the user via `AskUserQuestion` before any code, both "Recommended" picked: registering records our own database row AND opens the external form in the same click (the form is the club's own supplementary collection, not our source of truth for "is this student registered" — no fragile Google-Apps-Script-webhook integration); and reminders are in-app only, computed live and shown in a Dashboard section, not real Web Push (no VAPID keys/subscription table/scheduled job — confirmed as a materially bigger, unrequested lift, and this project has zero push infrastructure today).
    - **Built**: migration 0025 adds `events.registration_form_url` (nullable) and `event_registrations.location_at_registration` (a snapshot of the event's location at the moment of registration, populated by `register_for_event()` — the simplest way to later detect "this venue changed since I signed up" without a separate edit-history/audit table); migration 0026 extends `public_events()` with `end_date`/`joule_value` (`DROP FUNCTION` first, decisions 46-48's standing rule). New admin capability that didn't exist before at all: an event **edit** page (`admin/grid/[eventId]/edit`) + `editEventAction`, club-scoped the same way event creation already is. New student-facing event detail page (`(general)/events/[eventId]`) with full info and a Register/Cancel action; Dashboard's event rows became links into it instead of carrying their own inline buttons, consolidating the action to one place. A live "Reminders" section on the Dashboard surfaces both "tomorrow" (UTC day-boundary comparison, not local time, so it doesn't flicker depending on server timezone — same reasoning as `formatDateUTC`/`formatTimeUTC`) and "venue changed" (comparing the snapshot to the event's current location).
    - **A real bug found during live verification, not caught by typecheck/lint**: the new public event detail page did a raw `.from('events').select(...)` — `events` only has an RLS policy for `authenticated` reads (0005), not `anon`, so a logged-out visitor's request silently returned nothing and 404'd. This is exactly why `public_events()` exists as a security-definer RPC in the first place (decisions behind 0014/0018/50/52) — the fix was routing the new page through it (hence migration 0026's new columns) rather than opening RLS on the raw table. Live-verified concretely: the page 404'd before the fix, rendered every field correctly after.
    - **Live-verified past the RPC layer, through real UI**: created a real test event/registration directly against the DB (faking `auth.uid()` via `request.jwt.claims` to call the actual `register_for_event`/`redeem_event_scan` RPCs, not just raw table writes), confirmed the location snapshot, simulated a venue change, confirmed the reminder-detection query returns `venue_changed: true`, and ran the real QR-scan flow end-to-end (computed a genuine token via `qr_token_for_epoch`, called `redeem_event_scan`, confirmed `attended_at` stamped and Joules credited). Then logged into the actual admin UI in a browser, opened the new edit page, confirmed every field — including the registration form URL — was genuinely prefilled (not just the placeholder), changed the venue through the real form, and confirmed the database reflected it. All test fixtures cleaned up. `npm run typecheck`/`lint`/`build` all passed clean (a stale `.next` cache from the still-running dev server caused one transient Windows `EPERM` build error — the project's known "stale `.next`" gotcha — resolved by clearing it, not a real code issue).
55. **The PWA "Install app" button (an existing, correctly-written `beforeinstallprompt` listener) had a real, silent root cause for why it could fail to appear at all: the manifest only ever listed SVG icons (`sizes: 'any'`), never a concrete-sized PNG.** This was an already-documented TBD in the manifest's own comment ("swap for real 192/512/maskable PNGs before launch") — Chrome/Android's installability heuristics have inconsistent real-world support for SVG-only manifest icons across versions, and a browser that doesn't consider the site installable never fires `beforeinstallprompt` at all, so the button correctly never rendered — not a bug in the button's own code, which was already correct. Generated real `icon-192.png`/`icon-512.png`/`icon-512-maskable.png` from the existing source SVG using `sharp` (already a transitive dependency, not a new one), with the maskable variant rendered at 70% scale onto a background-filled 512×512 canvas so Android's circular/squircle icon masks don't clip the mark — added all three to `manifest.ts` alongside the existing SVG entry. Also added `metadata.icons.apple` (pointing at the 192 PNG) to `app/layout.tsx`, which was entirely missing — without it, a student manually doing "Add to Home Screen" on iPhone (the only path there; `beforeinstallprompt` never exists on iOS at all, a hard Apple platform limitation no code can bypass) would get a screenshot of the page as their home-screen icon instead of the actual logo.
    - **Verified via `curl`, not the Browser pane**: the in-app Browser preview was fully unresponsive this session (`navigate` failing even on a brand-new blank tab, not just a stale one) — confirmed real fix another way: fetched `/manifest.webmanifest` directly and confirmed all four icon entries are present with the right `sizes`/`type`/`purpose`, then fetched each new PNG directly and confirmed `200`/`image/png`. Separately hit the exact same "stale `.next` cache" gotcha decision 54 just documented — deleted `.next` and restarted the dev server, which is the correct fix, not a real app bug. `npm run typecheck`/`build` passed clean.
56. **A real in-app camera QR scanner for `/scan`, added because the existing camera-free design (point the phone's own OS camera app at the QR, which opens a deep link) wasn't matching how the user expected "tap to scan" to behave.** Confirmed with the user via `AskUserQuestion` before adding anything: yes to one new small dependency (`jsqr`) rather than trying to make do without a decoder — no reliable dependency-free path exists, since the newer native `BarcodeDetector` API doesn't exist on Safari/iOS at all, and a real share of students are on iPhone. Built `app/(node)/scan/camera-scanner.tsx`: opens `getUserMedia({ video: { facingMode: 'environment' } })`, decodes each frame with `jsQR` via an offscreen canvas on a `requestAnimationFrame` loop, and on a successful decode parses the same absolute `${site.url}/scan?e=&t=` link the admin's displayed QR already encodes (`station-client.tsx`) — it hands `e`/`t` straight to the *same* `redeem()` the existing manual/deep-link paths already use, rather than re-deriving any redemption logic of its own. A "Scan with camera" button on the existing Scan page opens this view; camera permission denial/no-camera get friendly inline messages instead of a silent failure, and the media stream's tracks are explicitly stopped on unmount/success so the camera light doesn't stay on.
    - **Verification is necessarily code-level, not a live phone test, and that's disclosed rather than glossed over**: `/scan` sits behind the same server-side mobile-PWA gate every other Node route in this project has always been (proxy.ts checks the real `User-Agent` header) — resizing the Browser pane's viewport to a mobile preset does not change that header, confirmed directly (the page still redirected to "Use your phone" after resizing). Camera access itself also needs a real device with a real camera, which this environment doesn't have either way. The mitigating factor: this feature introduces zero new backend logic — it reuses the exact `redeem()` call path (and therefore the exact `redeem_event_scan` RPC) already proven correct in earlier phases; the only new surface is getting `e`/`t` into that same function from a camera frame instead of a URL param or typed input, which was verified by careful code reading rather than a live run. `npm run typecheck`/`lint`/`build` all passed clean.
57. **Four real, previously-unreported bugs, found from a genuine pre-deployment testing pass (a feedback doc with 13 annotated screenshots), each grounded directly in the code before being called a "bug" rather than guessed at.** All four fixed in one pass since they're independent and each small:
    - **Homepage "Upcoming events" wasn't clickable at all** — `app/(general)/page.tsx` rendered plain `<li>` rows with no `<Link>`, confirmed by reading the file; the separate `/events` list page already links its rows correctly, so the homepage's preview list was just made consistent with that already-working pattern.
    - **Typing an admin's credentials into the *student* login form silently redirected into a dead end** (the screenshot showed "Use your phone" appearing after a laptop login). Root cause: `loginAction` in `app/(auth)/actions.ts` called `signInWithPassword` and redirected on any successful auth, with no check that the authenticated user actually has a `students` row — `adminLoginAction` right below it already had this exact check. Fixed by mirroring that pattern: sign back out and return a clear error if the account isn't a student, instead of proceeding into `/dashboard` (a Node path, which is what was hitting the mobile-only gate).
    - **The service worker could serve an old deployed version forever, explaining "old Jules site still opening" reports.** `public/sw.js` was pure cache-first for every GET request, and its `CACHE = 'site-shell-v1'` constant had never been bumped across any deploy this project has ever made — so a cached page response stayed cached indefinitely regardless of what shipped afterward, with no way to fix it short of remembering to hand-edit that string on every future deploy (which nothing had, until now, ever done). Rebuilt as a Route Handler (`app/sw.js/route.ts`, replacing the static `public/sw.js` file entirely) so the cache name derives from `process.env.VERCEL_GIT_COMMIT_SHA` — a real value Vercel sets automatically per deployment, so it changes without anyone needing to remember anything. Also changed the actual caching *strategy*, not just the busting: network-first for page navigations (always fetch fresh; cache is only an offline fallback) and cache-first only for `/_next/static/*` — those paths are already content-hashed by Next.js, so caching them forever is genuinely safe.
    - **The Grid Station's event picker would become unusable once the real 50-60 event calendar exists** — filtered `app/admin/(protected)/grid/page.tsx`'s query to exclude already-concluded events from the picker (same `hasConcluded` comparison the student Dashboard already uses), without touching the underlying data at all.
    - **Live-verified**: confirmed via `curl` that the homepage now renders a real `<a href="/events/...">` for each event, and that `/sw.js` serves from the new route with the derived cache name (`site-shell-dev` locally, since `VERCEL_GIT_COMMIT_SHA` is a Vercel-only env var — will be the real commit SHA in production). The Grid Station filter was verified against real data already live in the database: `Test_1` (event_date just minutes in the past at verification time) and `Test_2` (several days out) are exactly the concluded/upcoming split needed to exercise the filter, confirmed directly via the Supabase REST API rather than assumed. The login fix was verified by code review against the already-proven `adminLoginAction` pattern it mirrors, not a live login attempt (would require a real, disposable student/admin credential pair to safely exercise via HTTP directly). `npm run typecheck`/`lint`/`build` all passed clean.
58. **A dev-only bypass for both layers of the mobile-PWA gate — the single biggest recurring limitation across this entire project's history, where every Node-route feature had to be verified via direct RPC calls instead of an actual look, because nothing in this session's tooling can satisfy either gate layer.** Explicitly confirmed with the user before building (it touches a security-relevant gate): `proxy.ts` gets an `isDevMobileBypass()` check (query param `?devMobileBypass=1` OR a `dev_mobile_bypass` cookie it sets on first use, so it survives normal in-app navigation) that's double-gated on `process.env.NODE_ENV !== 'production'` — since any real `next build`/`next start` (which is all Vercel ever runs) always sets `NODE_ENV` to `'production'` regardless of any other env var, this branch is structurally unreachable outside local `next dev`, not just conditionally disabled. `app/(node)/pwa-gate.tsx` (Layer 2, the client-side standalone-display-mode check) reads the same cookie the same way, so both layers open together with one query param on first navigation.
    - **A real gotcha hit live, not a code bug**: the first attempt to use the bypass still showed the "Use your phone" blocking screen even though `document.cookie` genuinely contained `dev_mobile_bypass=1` (confirmed directly) — React Fast Refresh had preserved the `PwaGate` component's already-computed `standalone = false` state from *before* the edit was hot-swapped in, since the `useEffect` that computes it doesn't necessarily re-run on every Fast Refresh. A full page reload (not just navigating) fixed it immediately. Worth remembering: Fast Refresh can silently mask whether a `useEffect`-computed value actually reflects the current code.
    - **Live-verified for real, the first genuine look at rendered student PWA content this whole project has had**: logged in as the `jules.demo.volt@gmail.com` seed account through the actual login form and reached the real Dashboard — Season Joules, Ember tier, Power Grid, Upcoming Events (`Test_2` with a working "View & register" link), and a real Recent Activity feed populated with actual seeded transaction history, all rendered correctly, not inferred from RPC output. `npm run typecheck`/`build` passed clean.
59. **Bulk student account provisioning — the real process change behind "no self-signup," confirmed with the user via `AskUserQuestion`: credentials go out as a CSV the user sends themselves (email/WhatsApp), not an automated email-sending integration.** Reused `createAdminAction`'s own exact pattern (`app/admin/(protected)/settings/actions.ts`) rather than inventing a new one: service-role `auth.admin.createUser()` per row, then a direct `students` row insert via the same service-role client (bypasses RLS by design, same trusted-server-context posture as Force Reset) — not `complete_onboarding()`, which only ever works in the new user's own `auth.uid()` session and has no path for an admin acting on someone else's behalf. New `BulkStudentsSection` in Institution Settings: paste "Name, email" one per line (up to 500), each row validated against the same `is_email_domain_allowed()` RPC self-signup already used, partial failures reported per-row rather than failing the whole batch, and a client-side CSV built straight from the actual creation results (no separate download endpoint needed). Also added genuine self-service password change to Profile (`changePasswordAction`, mirroring the existing email-link `resetPasswordAction`'s validation) — the other half of the deal: students don't create their own account, but they do still own their own credential afterward. The public `/signup` page's route was kept working but its link removed from the login page, replaced with "Your account is created by your club" — de-emphasized, not deleted, so nothing that still points at it breaks.
    - **Live-verified genuinely end-to-end, including a real login with a real (if disposable) account** — not just checked at the API layer: created 3 test rows through the actual admin UI (2 valid, 1 deliberately malformed) and confirmed "2 of 3 accounts created" with the malformed row cleanly rejected with a clear per-row error, not blocking the other two. Confirmed both real rows actually exist in `students` via direct query. **Did not read back the real generated temp passwords or the exported CSV's contents** — attempting to capture the CSV `Blob` for inspection was correctly blocked by the safety classifier as credential materialization (echoing real generated secrets into the transcript), so verification of "does the created account actually work" instead used a disposable, self-chosen test password set directly via the service-role Admin API (not the real generated one), then logged in with it through the real form via the dev-mobile-bypass from decision 58 and reached a genuine Dashboard. Separately verified self-service password change the same way: changed the password from Profile, logged out, and logged back in with the new value through the real form. All test fixtures (both accounts, cascading via `on delete cascade` on `students.id → auth.users.id`) deleted afterward, confirmed clean via a follow-up query. `npm run typecheck`/`lint`/`build` all passed clean.
60. **Live Round rearchitected from "one phone per team" into real multi-student teams with pooled/split scoring — the exact same mechanic as async Surge Mode's group registration (decision 49), moved/mirrored into Live Round because the user, after real-device testing the installed PWA, explicitly clarified that only Live Round will actually be used in practice** ("frankly only live round is going to utilize that, other one is not going to utilize"). Confirmed via a full `EnterPlanMode`/`ExitPlanMode` plan before any code, given the size of the rearchitecture (schema changes, RPC rewrites, a new client join flow).
    - **Built**: migration 0032 — `live_round_teams.student_id` renamed to `created_by` (drops the old one-student-per-team constraint); new `live_round_team_members` table (`team_id`/`round_id`/`student_id`, `primary key (team_id, student_id)`, `unique(round_id, student_id)` — mirrors `quiz_group_members` exactly, added to the `supabase_realtime` publication); `live_round_answers` gains a `student_id` column and a new `unique(team_id, student_id, question_id)` constraint (each member now answers individually, replacing the old one-answer-per-team constraint). New RPCs `create_live_team`/`join_live_team`/`leave_live_team` (mirror `create_quiz_group`/`join_quiz_group`/`leave_quiz_group`, gated on `phase = 'lobby'`); `join_live_round` is removed. `submit_live_answer` now looks up team membership via `live_round_team_members`, records `student_id` on the answer, and **stops crediting `joule_transactions` immediately** — deferred to a new `complete_live_round(p_round_id)` RPC, which mirrors `complete_surge()`'s pooled/split CTE almost verbatim (participation pool + earned pool via `surge_answer_points()`, floor-division with the remainder going one point at a time to the fastest-responding members) and is called automatically from inside `host_advance_round`'s existing `leaderboard → complete` transition — no new manual "finalize" step for the host. `live_round_scoreboard` and `live_round_question` were both rewritten to aggregate/check membership through `live_round_team_members` instead of the old single-student team row.
    - **Client-side**: `/live` is now room-code-only (team name moved out of this step); a new `TeamFormationClient` (`app/(node)/live/[roundId]/team-formation-client.tsx`, a close structural mirror of async Surge's `GroupRegistrationClient`) shows open teams with live member counts and Create/Join/Leave actions once a student resolves a room code; the host lobby (`host-client.tsx`) now shows both team count and total member count ("3 teams, 9 students joined") via a new realtime subscription on `live_round_team_members`, and "Start round" is disabled on zero members rather than zero teams.
    - **A real, previously-latent bug in shared code, found and fixed while testing this feature (not introduced by it)**: `lib/components/reveal-scoreboard.tsx`'s reveal-animation `useEffect` had an empty `[]` dependency array, so a cold page load/reload landing directly on Live Round's `complete` phase — before the parent `HostClient`'s async `refreshScoreboard()` call resolves — permanently froze the animation at `visibleCount = 0` once `rows` later went from empty to populated, showing an empty "Final standings" list forever. Invisible in ordinary continuous play because the preceding `leaderboard` phase's own earlier fetch already populates the state well before the terminal transition; only a genuine cold load into `complete` exposes it. Fixed by changing the dependency array to `[rows.length]`.
    - **A real, previously-undocumented dev-only tooling gotcha, discovered while chasing what first looked like the fix not working**: the service worker (`app/sw.js/route.ts`, decision 57) treats every `/_next/static/*` path as safely cache-first forever, on the stated assumption that Next.js content-hashes those filenames — true for a real production build, but **not** true for `next dev` with Turbopack, where dev chunk filenames are stable identifiers that do not change when the file's content changes. A browser tab that had the service worker installed from before this fix kept being served the *pre-fix* compiled chunk for `reveal-scoreboard.tsx` indefinitely, even after a full dev-server restart with `.next` cleared — confirmed by fetching the same chunk URL directly via `curl` (bypassing the browser entirely) and finding it *did* contain the fix, proving the server was serving current code and the browser's own SW cache was the sole problem. Unregistering the service worker and clearing Cache Storage in that tab immediately fixed it. **This only matters for local `next dev` testing in this project, not production** (a real Vercel build does content-hash `/_next/static/*`, so decision 57's assumption holds there) — but it's a real trap for any future live-browser verification in this repo: if an already-open tab's tests don't reflect a just-made edit even after `.next` is cleared and the dev server restarted, check for a lingering service worker/Cache Storage entry before concluding the fix is broken.
    - **Verified the pooled-scoring math itself with an independently-reimplemented JS formula, the same bar as decision 49**: a throwaway script drove the real RPCs end-to-end for a constructed 2-person-team-plus-solo-player scenario (mixed correct/wrong/timed answers) via `create_live_team`/`join_live_team`/`host_advance_round`/`submit_live_answer`/`complete_live_round`, and diffed actual `joule_transactions` against hand-computed expectations — exact match for all three students. All test fixtures from that script were cleaned up and the script itself deleted immediately after.
    - **Live-verified in a real browser after the SW discovery above**: a genuinely fresh tab (SW cleared, dev server restarted with `.next` wiped) loaded the `complete`-phase round and rendered the real "Team A / 0 J" scoreboard row correctly (0 J because this particular round was advanced through its phases via a direct RPC script without any real answers submitted — expected, not a bug). `npm run typecheck`/`lint`/`build` all passed clean. Cleanup: the one leftover test round (room code `FDE9`, its team, and its two `live_round_team_members` rows) was deleted via its own row (cascades through FKs) — the pre-existing "Example" test surge those rounds belonged to was deliberately left alone, since it's a shared, already-existing test fixture from earlier sessions, not something created for this task, and removing it was out of this task's stated scope. The leftover `advance-round.mjs` verification script was deleted.
    - **A testing-environment limitation reconfirmed, not new**: all tabs in this session's Browser pane share one cookie jar, making true simultaneous multi-role (host + multiple real students) UI testing impractical — testing here was sequential with explicit re-logins/role-switches between steps, same limitation already disclosed for earlier phases.
61. **Three of the smaller real-device screenshot findings (from the same annotated-PWA feedback behind decision 60) triaged and fixed, after the user picked specific approaches for each rather than accepting generic fixes.** All three grounded in the actual current code/data before writing anything, not just re-described from the original annotation.
    - **Season picker**: Catalyst Records' `<select>` (`catalyst/season-picker.tsx`) let a student switch between seasons even though, confirmed via a direct query, only one of the two existing (still-placeholder, decision 9) seasons has ever had any real `joule_transactions` activity — the other has zero. Fixed by only rendering the picker once 2+ seasons each have at least one real transaction in their date range; otherwise the page just shows the one season that matters, with no dead dropdown. Default season selection also now genuinely prefers whichever season's date range contains "now," rather than relying on `seasons[0]` happening to sort correctly.
    - **Event poster placeholder**: a cover-less event card/detail page (`events/page.tsx`, `events/[eventId]/page.tsx`) showed a bare generic `BookOpen` icon — since cover-image upload is a very recent feature (Phase C), nearly every real event still shows this, reading as a broken image rather than an intentional placeholder. Replaced with a new hand-coded SVG illustration, `lib/components/event-cover-placeholder.tsx` (a spark-burst motif, same "atom generating a spark" visual language as the app icon, decision 41, and the Gallery's own placeholder, decision 41) — an abstract illustration, not a mockup pretending to be a real photo, same reasoning as decision 41's original Gallery placeholder.
    - **Streak display**: previously a bare flame icon + "N events" on Profile, ambiguous since decision 47 redefined "streak" from "days active" to "consecutive attended registered events" with no UI explanation of which. Presented the user with four concrete visual options (intensity flame, chain-link row, milestone progress bar, bigger-number-plus-subtext); the user picked the chain-link option. Built `lib/components/streak-chain.tsx` — a row of alternating vertical/horizontal rounded-rect rings that overlap to read as an interlocking chain, solid gold/brand-colored when the streak is nonzero (capped visually at 6 rings, the exact count is always spelled out in real text alongside so the number is never conveyed by the visual alone — same "don't convey information by color/shape alone" discipline as the rest of this app), a single dashed/muted ring for a zero streak (a visually "broken" chain, not just a smaller number).
    - **Live-verified all three, not just typechecked**: confirmed via direct DOM/computed-style inspection in a real browser tab that the season picker is genuinely absent from the rendered page when only one season has activity; that the new event placeholder SVG renders with real non-zero dimensions inside the card; and, for the streak chain, both states — created a temporary 3-event attended-streak fixture for a demo account, confirmed the chain rendered exactly 3 solid gold interlocking rings with the real brand color and correct alternating orientation/overlap, then deleted the fixture and confirmed the streak read back to 0 with the correct single dashed ring. `npm run typecheck`/`lint`/`build` all passed clean (one transient Windows `EPERM` build error from the dev server still holding `.next` — this project's own well-documented gotcha, decision 54 — resolved by stopping the dev server and clearing `.next`, not a real code issue).
    - **A credential-hygiene note, not a code issue**: verifying the season-picker/streak fixes required a real student login; the standing demo account `jules.demo.volt@gmail.com` (from the professor-demo kit, see the Live Round Build Log entry) had its password reset to a disposable value for this purpose, since the original password was never known/recorded by this session per this project's standing credential-handling rule. Flagged to the user directly — anyone using that account for a future demo should set a password of their own choosing first.
    - **Deliberately not yet touched**: the fourth item from the same feedback batch (Dashboard list scaling on narrow phones) — code review found no obvious fixed-width/overflow bug, so it's held for the live phone/PWA check rather than guessed at blind.
62. **★ The live DB now carries deliberate, persistent dummy demo content — not test fixtures to be cleaned up, unlike every other throwaway fixture logged elsewhere in this file.** At the user's explicit request ("create dummy data so we can understand the site better"), seeded via `template/seed-demo-data.mjs` (kept in the repo, idempotent/rerunnable, not deleted — a genuine break from this session's usual "verify then delete" test-fixture discipline, since this content is meant to persist): 4 new clubs (Finance & Investment Club, Marketing Society, Operations & Analytics Club, Entrepreneurship Cell) alongside the existing placeholder General Club; 7 past (concluded) events and 4 upcoming events across those clubs, with varied types/locations/Joule values; a hand-designed attendance pattern per each of the 11 `jules.demo.*` seed students producing a realistic tier spread on the leaderboard (Ember through Plasma) and varied streaks (0 through 7, including one deliberately-broken streak and one student, Demo30, left with zero registrations as a "brand new" baseline); a handful of `surge_earned`/`surge_participation`/`admin_manual_adjustment` ledger rows for variety in "Joules by source"; 3 real-shaped Event Reports (fabricated but plausible club-meeting content, clearly fictional coordinator names); and one deliberate venue-change (`Community Outreach Day`) to exercise the Dashboard's Reminders section for Demo31 specifically.
    - **A real ECONNRESET was hit and diagnosed, not just retried blindly, per the user's explicit instruction to stop and assess first**: a one-off diagnostic connection immediately after the reset succeeded cleanly, confirming the pooler itself wasn't down — most likely a transient blip from this session's history of many rapid, short-lived connections to the same Supabase pooler. The seed script's connection step was hardened with a small retry-with-backoff wrapper as a result; the script's own idempotent design (existence checks before every insert) means a real mid-run drop would have been safely resumable anyway.
    - **Live-verified thoroughly, not just eyeballed**: confirmed live in a real browser that `/clubs` shows all 5 clubs, `/events` shows all 11 events with correct club/type labels, `/leaderboard` matches the seeded totals exactly (Demo Watt 1050 → Plasma down to Demo30 at 0), and — logging in as two different real seed students in turn — that Profile's streak chain (decision 61) correctly shows "7 events in a row" for a perfect attender and "No streak yet" for one whose most recent event was a miss, that the Reminders section correctly fires both the "tomorrow" and the "venue changed" cases, and that the Event Attendance/Joules-by-source breakdowns reconcile exactly against the seeded ledger rows.
    - **A pre-existing discrepancy investigated, not assumed to be a bug**: Demo Volt's total came out 184 J higher than hand-calculated. Traced to 10 real `surge_earned` transactions already sitting on that account from **before** this seed script ran, dated 2026-07-10/07-13 — genuine leftover activity from this project's own earlier real-device Live Round/Surge testing (see the Known Open Items entry on real-device verification), not a bug in the new seed data. Left untouched, since it's real historical activity on a real (if disposable) demo account, not something to silently delete.
    - **Credential-hygiene note, same pattern as decision 61**: verifying this required logging in as two different demo students; `jules.demo.volt@gmail.com`'s and `jules.demo.amp6@gmail.com`'s (Demo31) passwords were both reset to a shared disposable value for this session's verification, since neither original password was known. Same caveat as before — reset before using either for a real demo.
63. **The student's own "My activity" breakdown (event attendance bars, Joules-by-source bars, quiz participation split) is now also visible to staff in the admin Student Data Vault, per the user's direct request** — expanding a student's row now shows the exact same section Profile shows the student themselves, plus the streak now renders as the same chain-link visual (decision 61) instead of plain "N events" text. No new RPC was needed: `event_registrations`/`joule_transactions` already carry staff-wide SELECT policies (a committee member or professor already reads the full ledger/registration set platform-wide, not just their own — the same policies decision 46's Known Open Items already flagged as intentionally broad), and `quiz_group_members` is readable by any authenticated user.
    - **Built**: extracted the computation (previously inlined in `profile/page.tsx`) into a shared `lib/jules/student-activity.ts` (`getStudentActivitySummary()` + the `SOURCE_LABEL` map) and the presentational bar into `lib/patterns/bar-row.tsx`, so Profile and the Vault can never silently drift apart on this logic — both call the identical function now. The Vault fetches it lazily per row (a new `getStudentActivityForVaultAction` server action, `requireAdmin(['professor'])`-gated) only when a student is expanded, not for the whole roster up front.
    - **A real, previously-latent data-consistency bug found while verifying this, not a bug in the new feature's own code**: expanding Demo Amp in the Vault showed "No streak yet" where the seeded pattern (decision 62) should have produced 4. Traced to the seed script's `on conflict (event_id, student_id) do nothing` on `event_registrations` — for the two pre-existing events (`Test_1`/`Test_2`, which predate this session), two demo students already had a stale registration row from earlier real testing sessions with `attended_at` still null, and "do nothing" silently preserved that stale value instead of applying the seed script's intended attendance pattern. The `joule_transactions` event_scan credit for the same event went through fine (no conflicting ledger row existed), so the two students' Joule totals were already correct — only their registration/streak state was wrong, a real internal inconsistency between the ledger and the registration table. Fixed both affected rows directly (attended_at backfilled from the existing ledger transaction's own timestamp, so everything lines up), confirmed both streaks recomputed correctly (0 → 4), and changed the seed script's conflict clause to `do update set attended_at = excluded.attended_at` so a future rerun can't reintroduce the same silent-skip.
    - **Live-verified end-to-end**: logged into the real admin Student Data Vault as a demo Professor account, expanded Demo Amp's row, and confirmed the corrected "4 events in a row" streak and the full attendance/Joules-by-source breakdown render identically to what the same student sees on their own Profile. `npm run typecheck`/`lint`/`build` all passed clean.
64. **★ RESOLVED — the real 9-club FMS roster (the long-standing "only one placeholder club" Known Open Item, since decisions 9/46) is now in the live `clubs` table, sourced directly from the university's own "SoP for Students Conduits" document the user supplied.** The 9 real clubs: **Joules Club** (Energy, mentor Dr. Namita Pragya), **Gati (गति) Club** (Transport & logistics, Prof. Rachna Gangwar), **Cityscape Club** (Urban & Real estate, Dr. Karan Radia), **Shastra Club** (Finance/Economics/Policy, Dr. Riya Mehta), **Marketing Mavericks Club** (Marketing & Social Media, Prof. Anindita Chatterjee/Prof. Shamindra Nath Sanyal), **Nexus Club** (Project Management & Operations, Prof. Krishna Murthy Inumula), **StratEdge Club** (Start-up/Innovation/Digital Tech, Dr. Vidya Mohan), **Film Club** (Communication, Dr. Baishali Mitra), **SRISTI Club** (Societal welfare, Dr. Manju Raisinghani). The existing "General Club" placeholder was kept (not deleted) as a legitimate catch-all for whole-of-faculty events not tied to one club (orientation, program-office announcements) — its description was reworded so it no longer reads as an unresolved TODO now that real clubs exist alongside it.
    - **A genuinely delightful, non-coincidental discovery surfaced while reading the source document**: the entire app's point-currency name, "Joules," is not an invented pun — it is directly named after this real **Joules Club** (the Energy-sector club), confirming what decision 45 could previously only infer ("Jules" the app's original pre-pivot name being "a real club's name in the program"). This is the first time that connection has been confirmed against a real institutional source rather than assumed.
    - **The 4 fabricated demo clubs from decision 62** (Finance & Investment Club, Marketing Society, Operations & Analytics Club, Entrepreneurship Cell) were retired now that real, better-fitting clubs exist — confirmed zero remaining `events`/`surges` references before deleting each. The demo events built in decision 62 were remapped onto the real clubs by theme rather than discarded: Stock Pitch Workshop/Case Study Sprint/Investor Panel Night → Shastra Club; Brand Strategy Bootcamp/Leadership Roundtable → Marketing Mavericks Club; Supply Chain Simulation → Nexus Club; Founders' Fireside Chat → StratEdge Club; Community Outreach Day → SRISTI Club (a better real-world fit than the fabricated "Operations & Analytics Club" it was originally under, since it's a volunteer/community event, not an operations one).
    - **One real gap disclosed, not silently filled in**: the source document's own "Details of the Clubs" section (mission paragraph + objectives + activities for each club) covers 8 of the 9 clubs — **SRISTI Club has no detailed write-up in the source**, only its name and faculty mentor from the summary table. Its `clubs.description` was kept honest and short ("Focuses on societal welfare initiatives.") rather than inventing objectives/activities content the source document doesn't actually contain, matching this project's standing discipline (decisions 9/10) of never fabricating placeholder content silently.
    - **Read via `python-docx`, not `pandoc`** (not installed in this environment) — a raw XML-regex extraction attempt first produced corrupted output (nested `<w:tc>`/`<w:tcPr>` table markup leaking into extracted paragraph text) before switching to `python-docx`'s own paragraph/table iteration, which parses the real document structure correctly.
    - **Live-verified directly**: confirmed via a real browser check that `/clubs` renders all 10 clubs with the correct real descriptions/mentors, and `/events` shows every event now attributed to its correct real club. `npm run typecheck` passed clean (no code changed, data-only import); a stray leftover `next dev` process on port 3000 from earlier in the session (not the one this segment had been restarting on port 3004) turned out to be the one actually serving traffic — used for verification once found, not fixed/killed, since it was serving correctly.
    - **General Club itself was removed shortly after, at the user's follow-up request, once the real 9-club roster made it redundant.** Before deleting it, its dependents were checked rather than assumed away: 3 events (Test_1, Test_2, Orientation Mixer) and — critically — 3 surges, one of which (Season Opener Surge) is real content (20 real imported questions) and another (Example) held the Joule totals seeded for 7 demo students earlier this same segment. Flagged this distinction back to the user via `AskUserQuestion` rather than deleting real content on the strength of a broad first instruction — the user chose to reassign the 3 surges onto Joules Club (preserving the real questions and the demo Joule totals intact) while still deleting the 3 generic/test events and all their dependents (`event_registrations`, `joule_transactions`, and Test_1's own `event_reports` row). Verified live afterward that `/clubs` shows exactly the 9 real clubs, `/events` no longer lists the deleted 3, and the leaderboard recalculated correctly (each student's total dropped by exactly the Joules tied to the deleted events, nothing else moved).
65. **An 8-item polish batch, mostly triggered by the real club roster finally existing and by direct product/security feedback**, all built and live-verified in one pass:
    - **`/clubs` got the same card-grid + placeholder-illustration treatment as `/events`** (reusing `EventCoverPlaceholder` — no separate club illustration exists, and none was worth building for a one-off).
    - **The club detail page (`/clubs/[slug]`) now shows every upcoming event** (the old 5-item cap is gone) **and a new horizontally-scrollable "Past events" strip** (`overflow-x-auto`) instead of showing no past events at all, which was the previous behavior. Both sections, plus the homepage's and `/events`' listings, now show event **time** (not just date) rendered in the accent/kumkum color next to the date — using the existing `formatDateUTC`/`formatTimeUTC` helpers, not raw `toLocaleString()`.
    - **The homepage's "The idea" section and its stale "Energy Management Club" eyebrow were rewritten from the real SoP document** (decision 64), replacing the old Shakti/Vishwambhari-Stuti framing (still accurate to `docs/project-spec.md` §2, but never reviewed by the actual club, and now superseded by real institutional language) with copy drawn from the SOP's own stated mission and objectives (professional development, networking, leadership, industry insight, community engagement). The Joule/tier mechanic explanation stayed, since that's the app's own real feature, not spec-derived flavor text.
    - **The homepage's prominent "I'm an admin" button was removed entirely** (flagged by the user as a public-facing security concern — no reason to advertise a staff login button next to the student one) **and replaced with a small "Staff login" link tucked into the footer**, discoverable but not prominent. `/admin/login` also gained a "← Back to site" link, since it previously had no way back to the public site at all once an admin (or a student who mis-clicked) landed there.
    - **The public Event Report page's "Download Word report" link is now admin-gated** (`getAdmin()` check, same helper used throughout the admin routes) rather than shown to every visitor — confirmed live in both directions: absent for an anonymous visitor, present for a logged-in admin viewing the identical `/event-reports/[id]` route (one shared page, not a duplicated admin-only copy).
    - **The public footer was rebuilt to list all clubs** (each linking to its own `/clubs/[slug]` page) **with an Instagram icon+link wherever `clubs.instagram_url` is set** — required splitting `app/(general)/layout.tsx` into a Server Component (fetches `public_clubs()`) that passes plain club data down to a new `general-layout-client.tsx` (owns the nav-highlighting `usePathname()` logic), mirroring the exact Server/Client split this project already used for the admin sidebar (decision 40) — a Client Component cannot itself fetch and render a Server Component's data source directly. **No real Instagram handles were fabricated** — the SOP document doesn't contain any, and the admin's existing Institution Settings → Clubs social-links editor (built in Phase C, decision "Per-club public pages + social links") already lets a professor add them later; the footer/detail-page UI is just wired to display them once set.
    - **A real, previously-latent bug in a completely unrelated file was exposed while verifying this batch, not caused by it**: logging into `/admin/login` in an already-open, previously-edited tab intermittently failed silently (no redirect, no visible error) with a console error — "Can't perform a React state update on a component that hasn't mounted yet" — a Fast-Refresh artifact from the tab having loaded the page before this session's own edit to that same file (adding the back-to-site link). A genuinely fresh tab logged in immediately on the first attempt. Documented here as the same class of Fast-Refresh staleness this project has hit repeatedly before, not a bug in the shipped code.
    - **`npm run typecheck`/`lint`/`build` all passed clean throughout.**
66. **The public footer was redesigned to a 3-column structure after the user shared a reference site's footer** (logo big and prominent on the left, a clearly-labeled "Pages" link column, a third informational column, a bottom bar with legal text + a small deemphasized staff-login link) **— asked to match that shape and quality, not to copy its content.** Left column: the real `icon.svg` app mark at a real visible size + the "Synergy" wordmark, `site.description`, an "Install the App" button (links to the existing `/get-app` page rather than re-implementing the `beforeinstallprompt` flow a second time — that logic already lives in one place, `get-app/install-button.tsx`), and the Adani University logo small underneath (kept, per decision 53, just de-emphasized further under the bigger Synergy mark). Middle column: "Pages" (mirrors the top nav). Right column: "Clubs" (all 9, Instagram icon inline where set). Bottom bar: `site.legalName` (left) · `site.tagline` (center) · "Staff login" (right) — matching the reference's exact 3-part bottom-bar layout.
    - **Verification workflow changed at the user's explicit request, going forward**: this session had repeatedly lost real time to Fast-Refresh/stale-tab artifacts (documented across decisions 60/62/64/65) that looked like real bugs but weren't. Per the user's suggestion, the standing practice now is to stop the running dev server and start a completely fresh one before any live-browser verification, rather than relying on hot-reload of an already-running process — cheap insurance against exactly this class of false alarm. Applied immediately in this same slice: stopped the old server, started fresh, and verified in a brand-new tab.
    - **A live demonstration of exactly why that discipline earns its cost**: even with a fresh server AND a fresh tab, the footer still initially rendered the *old* markup — traced to the dev-mode service worker cache (the same class of gotcha as decision 60, now confirmed to recur), not the new server or the new tab. Confirmed via `curl` (which bypasses the browser entirely) that the server was serving the new footer correctly the whole time; clearing the tab's service worker/Cache Storage immediately fixed it. A leftover second tab's dev-server console error (a hydration-mismatch stack trace referencing stale-vs-new footer class names) was similarly just that old tab's own stale client tree reconciling against fresh server output — not a real bug, confirmed by the fact that the properly-cleared tab showed zero errors.
    - **`npm run typecheck`/`lint`/`build` all passed clean.**
    - **Immediate follow-on from the user, after seeing it live**: the Adani University logo should render large (comparable in size to the whole Synergy brand block, not a small de-emphasized mark) and sit *above* the Synergy icon/wordmark, not below it. Reordered the JSX and sized the logo at `h-20` — confirmed this really is a "stop and ask before assuming" moment, since the user explicitly interrupted mid-instruction to have me confirm understanding before writing any code, which was the right call given decision 53 had specifically de-emphasized this exact logo — a direct user override of an earlier decision, not a guess. **Hit the exact same dev-mode service-worker stale-cache gotcha a third time** (decisions 60, 65) — even with a freshly-restarted server *and* a brand-new tab, the footer still showed the old (small, below) logo; confirmed via `curl` that the server was serving the corrected markup the whole time, and clearing that tab's service worker/Cache Storage fixed it immediately. This is now a firmly established pattern for this project's local dev workflow, not a one-off.
    - **A second immediate follow-on**: at `h-20` the logo read as disproportionately large once actually seen live — the user's original ask was "bigger than before," not "dominate the footer." Sized down to `h-12`, a size clearly bigger than the original de-emphasized version but proportionate against the Synergy block and the rest of the footer's visual weight. Confirmed the exact same service-worker staleness would otherwise recur, so cleared it proactively in the verification tab before checking, rather than discovering it again — worth doing preemptively from now on rather than treating each recurrence as a fresh surprise.
    - **A third follow-on**: the user reported the logo now looked "stretched from side" and asked to decrease its width by 30%. Before touching the code, checked whether this was an actual aspect-ratio bug — opened the real source file with PIL (`Image.open(...).size`) and found it's genuinely 330×176px (ratio 1.875), essentially identical to the 220×118 already hardcoded in the `<Image>` props (ratio 1.864, <1% off) — ruling out a real defect. Treated it as a subjective proportion preference and implemented the literal ask: updated the `<Image>` width/height props to the real 330×176 dimensions (so Next/Image's own aspect-ratio math is correct) and set an explicit fixed Tailwind width, `h-12 w-[63px]` (48px height × 1.875 × 0.7 ≈ 63px), replacing the previous `w-auto`. Verified proactively (fresh server restart, fresh build, fresh tab, service-worker/Cache Storage cleared before checking) — the footer logo now renders visibly narrower at the same height, sitting correctly above the Synergy block.
67. **The installed student PWA's mobile-gate (`PwaGate`, decision 22) was flashing its "Synergy is built for your phone / Get the app" blocking screen on real-device use, even though the app really was installed** — real-phone testing showed this screen for roughly a second on ordinary app opens and even mid-session page moves, never the native Android/Chrome cold-start splash alongside it. That absence was the key clue: if the whole app process/Activity were restarting, the OS splash would reappear too; since it didn't, only this one React component was remounting (most likely something re-triggering a full document reload for in-app navigation, e.g. across the (general)/(node) route-group boundary — see decision 68), not the phone relaunching the app. `PwaGate`'s own design was the rest of the problem: it fails closed by design (correct, since no cross-platform signal proves "installed as a PWA" before mount), but its fail-closed placeholder *was* the real "you need to install this" copy — showing that copy is only actually correct once the check resolves negatively, not while it's still pending. Fixed both parts: added a third `'checking'` state rendered as a bare centered spinner (no copy) so the misleading "get the app" text is never shown as a placeholder, only after a genuine negative result; and cached a confirmed "yes, standalone" result in `sessionStorage` so any later remount within the same tab session (whatever the cause) skips the check and renders real content immediately, with no flash of any kind. Verified live: logged in as `jules.demo.volt@gmail.com` (password reset to a disposable value per this project's standing credential-hygiene practice), confirmed the sessionStorage flag gets set on first resolution, then forced a hard reload of a Node page (a strictly harder case than an in-app remount) and confirmed real content rendered immediately with no gate/spinner visible. `npm run typecheck`/`lint`/`build` all passed clean.
68. **A related real-device finding, surfaced right after decision 67: tapping an event from inside the installed Node app's Dashboard drops the student out of the Node app's own persistent shell (bottom nav) entirely, into the public General site's header/footer chrome** — because the Dashboard's "Upcoming events"/"My registered events" links point straight at `/events/[id]`, a page that lives in the `(general)` route group (shared with public/anonymous visitors), not inside `(node)`. Once there, the general site's own hamburger menu (mobile) offered a way to browse off to unrelated public marketing pages (Home/Events/Clubs/Leaderboard/Gallery) with no clear way back into the app except the phone's back button — not a security bypass (nothing there defeats attendance/quiz integrity), but a jarring mid-task UX gap the user flagged directly from two different real-device screenshots (the homepage and an event page), asking to fix both: hide the escape-hatch nav and add an explicit way back. Implemented in `app/(general)/layout.tsx` (now also calls the existing non-throwing `getStudent()` helper and passes `isStudent: boolean` down) and `general-layout-client.tsx`: when a logged-in student views any general-site page, the header's entire nav/hamburger is replaced by a single "← Back to Grid" link (`/dashboard`) — anonymous/admin visitors see the header completely unchanged. Verified live end-to-end: as the same logged-in demo student, tapped "Leadership Roundtable" from the Dashboard and confirmed the event page showed only the Back-to-Grid link (no nav, no hamburger), clicking it returned to the Dashboard with the Node app's bottom nav intact; logged out and reloaded the same event URL to confirm an anonymous visitor still sees the full nav + hamburger, unchanged. `npm run typecheck`/`lint`/`build` all passed clean.
69. **Two more real-device Dashboard findings, both fixed in the same pass**: (a) **"The Power Grid" (`lib/components/power-grid.tsx`) was never actually measuring what its own docstring claims ("event scan or Surge participation")** — its `litCount` was wired to `activity?.length` in `dashboard/page.tsx`, the length of the *Recent Activity* feed's own capped `.limit(10)` query, which mixes in non-attendance rows (`admin_manual_adjustment`, `surge_earned`/`surge_participation` credit lines) and can never exceed 10 regardless of true history — the user, seeing a real student's grid read "5 logged," described this as being driven by "daily login" (an older, already-retired concept from decision 47's streak rework) rather than real attendance. Fixed by deriving `litCount` from the existing shared `getStudentActivitySummary()` helper (decision 63, already the single source of truth for Profile/Vault's "My activity") instead: `attendance.attended + soloQuizCount + groupQuizCount` — genuine distinct events attended plus distinct quizzes participated in, uncapped. (b) **Added the Streak card (the `StreakChain` chain-of-rings visual, decision 61) to the Dashboard itself**, directly under Lifetime Joules in the Season card — previously only shown on Profile; the user pointed at Profile's Streak card and asked for the same thing on the Grid page. Both reuse `totals.streak` and `getStudentActivitySummary()`, values the Dashboard was already fetching or could fetch via existing shared code — no new RPC or migration needed. Verified live: logged in as a real demo student whose Profile page independently showed "Attended 3 · Missed 2" and "1 solo · 0 group" quiz participation; the Dashboard's Power Grid now reads "4 logged" (3 + 1 + 0), matching exactly, down from the old "5" derived from raw transaction count — and the same "3 events in a row" Streak card now renders on the Grid page too. `npm run typecheck`/`lint`/`build` all passed clean.
70. **Three Live Round quiz-UX gaps, all raised directly by the user in one message, all fixed in the same pass**: (a) **"Go solo" button** — `TeamFormationClient` (`app/(node)/live/[roundId]/team-formation-client.tsx`) previously only offered "+ Start a team" (which still required typing a team name, even for a lone player); added a one-tap "Go solo" button that calls the existing `createLiveTeamAction` with the student's own name (a new `studentName` prop, threaded from `page.tsx`'s already-available `requireStudent()` result) as the auto-generated team name — no architecture change, the "team of one" concept already existed (decision "LiveTeams"), this just removes the pointless manual-naming step for the common case. (b) **Wrong-team recovery** — the `leave_live_team` RPC and its `leaveLiveTeamAction` server action already existed from the original LiveTeams build but were never wired to any button; a student who joined the wrong team by mistake had no way out except waiting for the round to end. Added a "Not your team? Leave" button to `TeamClient`'s lobby-phase view (`team-client.tsx`, only shown pre-round-start, matching the RPC's own `phase = 'lobby'` guard) — required also threading a new `teamId` prop down from `page.tsx` (previously only `teamName` was passed). Calling it correctly bounces the student back to `TeamFormationClient` to pick the right team, verified live. (c) **No "what now" screen after a round ends, on either side** — the student's `complete`-phase final-standings view had no next action (added "View Leaderboard" → `/leaderboard` and "Back to Grid" → `/dashboard` buttons), and the admin host's `FinalView` had no next action either (added "Start another Live Round" → `/admin/live/new`, the closest thing this project has to a Live Round index/list page, since none exists separately). Verified end-to-end live: created a real Live Round from a 1-question test Surge, joined as a demo student via "Go solo" (auto-named "Demo23," the student's own name), used "Not your team? Leave" and confirmed return to team formation, rejoined, then drove the round to `complete` and confirmed both the student's and the admin's new navigation buttons render and link correctly — all test round/team/membership rows deleted afterward. `npm run typecheck`/`lint`/`build` all passed clean.
71. **The "escalating drama" reveal treatment (drumroll + suspense hold, decisions 28-30) previously only fired on the true final question — the user asked why longer quizzes (their example: 5-10+ questions) don't also get a beat at the halfway and three-quarter marks.** Extracted the shared math into a new `lib/jules/quiz-milestones.ts` (`getQuizMilestone(index, total)` → `'half' | 'three-quarter' | 'last' | null`, plus `MILESTONE_LABEL` for the on-screen caption), used by both `host-client.tsx` and `team-client.tsx` in place of their old local `isLast`/`isLastQuestion` booleans — kept as one shared module specifically so the two screens' milestone timing can never drift apart, unlike the suspense *effect* itself (duration, vibration pattern), which the two files still each declare separately per their existing "kept in sync deliberately" convention. Milestone math: the final question always gets the treatment; halfway/three-quarter only kick in for quizzes of 8+ questions (`Math.ceil(total * fraction) - 1`) — below that, the beats would land too close together to read as distinct moments, so a 5-6 question quiz still only dramatizes the final question, exactly as before. Also suppressed the quiet per-question correct/incorrect sound on milestone questions (previously only suppressed on the true last question) so it doesn't layer under the drumroll. Verified: a standalone Node script confirmed the exact milestone question numbers for totals of 5/6/8/9/10/20 questions match expectations (5→q5 only; 8→q4/q6/q8; 20→q10/q15/q20); a real Live Round was parked mid-round on a 20-question Surge (`question_index=9`, the halfway mark) and the reveal screen rendered correctly with no crash, both from a cold page load and from a live realtime phase-flip triggered via direct DB update (the 1.5s suspense window itself was too short to reliably screenshot mid-animation given this session's tool round-trip latency, but the underlying state/render logic was confirmed correct both ways). `npm run typecheck`/`lint`/`build` all passed clean.

72. **The Event Report .docx download now fills the user's real, literal `.docx` reference file via `docxtemplater`/`pizzip` (mail-merge/tag-substitution), replacing the from-scratch `docx`-js table/paragraph generation that decisions 51-52 had built.** The user explicitly corrected the earlier approach mid-session: "we can assume my template is reusable fillable form... i dont want to build new word i want to use my provided form" — the real institutional file itself is now the template, not a look-alike rebuilt in code, so any future visual tweak the professor makes to the actual `.docx` (fonts, spacing, the masthead layout) survives untouched, since the app only ever fills placeholder tags into a copy of that exact file.
    - **Confirmed with the user before tagging the real file** (`AskUserQuestion`): Coordinators became a real list (`coordinators text[]`, migration 0033, replacing the single `coordinator_name text` column — same "computed/derived, not hand-maintained" discipline as this project's other list fields); Event Highlights stays free-text, matching the template's real paragraph section.
    - **The real reference `.docx` (`C:\Users\bpurv\OneDrive\Documents\AU_EVENT COMPLETION REPORT-Template.docx`) was tagged directly, not redrawn** — copied into `template/lib/jules/templates/event-completion-report.docx` with `{event_name}`/`{event_date}`/`{event_time}`/`{venue}`/`{club_name}`/`{introduction}`/`{event_highlights}`/`{conclusion}` simple tags, plus three `{#coordinators}{.}{/coordinators}`/`{#objectives}{.}{/objectives}`/`{#outcomes}{.}{/outcomes}` loops. **Objectives/Outcomes loops reuse the template's own real Word auto-numbering** (`<w:numId>` lettered lists already in the source document) by wrapping the existing numbered paragraph in marker paragraphs (`{#tag}`/`{/tag}` each in their own paragraph, `{.}` alone in the template paragraph between them) so docxtemplater repeats real, separately-numbered Word paragraphs per item rather than concatenating array items inline with no separator — the first tagging attempt got this wrong (all markers crammed into one paragraph, producing glued-together text like "processesBuild"), caught by the user's own screenshot after saying "it worked," then fixed. **Coordinators has no source auto-numbering to reuse**, so the app pre-numbers each string itself (`"1. Name"`, `"2. Name"`) before handing the array to docxtemplater, rather than looping into a numbered Word list — the one place this template's list handling isn't identical between fields, worth remembering if a future coordinator-list bug looks like an off-by-one.
    - **A second, harder bug required switching tooling entirely, not just retrying regex**: a second regex-based tagging attempt (marker paragraphs, but insertion via string-slicing on the raw XML) produced "Closing tag does not match opening tag" from docxtemplater — the user explicitly asked to stop guessing ("brother stop to much work things not working... can we search web where someone has done similar thing which is proven") before a third attempt. Research confirmed docxtemplater itself is the standard, proven tool (already correctly chosen); the bug was in *how the template was authored*, not the library. Fixed by abandoning regex/string-slicing entirely in favor of a `defusedxml.minidom`-based DOM approach — finding real `<w:p>` paragraph element nodes (optionally filtered by descendant `<w:numId>`) and using `insertBefore`/`removeChild` on actual sibling nodes, rather than slicing text ranges. This is a stronger technique than regex for structural XML edits generally, not just a one-off fix for this file.
    - **`template/app/api/event-reports/[id]/docx/route.ts` was rewritten from scratch**, dropping the `docx`-js `Document`/`Table`/`ImageRun` masthead-table construction and the half-built photo-attachment-embedding helpers (`imageTypeFromPath`/`readImageDimensions`/`fetchEmbeddableImage`/`photoTable`) entirely — the real template file already contains the bordered masthead, the logo, and the Attachments checklist section verbatim, so none of that needs regenerating in code anymore. The route now just reads the template file, calls `doc.render({...})` with the report's real data, and streams the filled zip back — a much smaller, more robust route than the one it replaced. **Photo/attachment image embedding into the real template is explicitly deferred, not silently dropped** — the user asked for it once ("so we need to add picture table border") before the pivot to the real-template approach, and it needs to be re-derived for docxtemplater specifically (likely its official image module) rather than reusing the old `docx`-js `ImageRun` helpers, which don't apply to a template-fill approach at all.
    - **Live-verified end-to-end against production**, not just a local sample fill: created a real test event + Event Report directly against the live DB (two coordinators, real objectives/outcomes/highlights/conclusion text), started a fresh dev server, hit the real `/api/event-reports/[id]/docx` route, confirmed a real `200` with the correct filename/content-type, then unzipped and read the actual returned document via `python-docx` — every field (Event Name, Date, Time, Venue, Organised-by-club, both numbered coordinators, Introduction, Objectives, Event Highlights, Outcomes, Conclusion, and the still-present Attachments checklist) rendered correctly, and the embedded logo (`word/media/image1.jpeg`) survived in the output zip untouched. All test fixtures (the event, the report) were deleted from the live DB afterward. `npm run typecheck`/`lint`/`build` all passed clean throughout, and migration 0033 was applied to production.

73. **A real bug found from the user's own real-device testing ahead of the 2026-07-17 live demo: Live Round's team screen gave away correct/incorrect immediately upon a team submitting an answer — sound, vibration, and the "+X J!"/"Not quite." text all fired the instant `submit_live_answer`'s RPC responded, mid-question, well before the host ever clicked "Reveal answer."** The user described this two ways — "without even me clicking reveal answer sound effect is playing," and a 20-second question answered at 15 seconds already showing as wrong "not weighting for final answer" — both symptoms of the same root cause: `team-client.tsx`'s realtime UPDATE handler unconditionally cleared `selected`/`awarded` on *every* round update, including the question→reveal transition itself, so the quiet per-question sound effect (keyed only on `awarded !== null`) necessarily had to fire the moment `awarded` was first set — mid-question — since by the time the phase actually became `'reveal'` the state was already wiped. **Fixed by only clearing `selected`/`awarded` when a genuinely new question starts** (`next.phase === 'question'`), letting them survive the question→reveal transition, and re-gating the sound/vibration effect on `round.phase === 'reveal'` in addition to `awarded !== null` — so the sound, the "+X J!"/"Not quite." text, and the correct/wrong highlighting now all wait for the host's real click. The in-question view now just shows a neutral "Locked in, waiting for the host…" regardless of correctness, matching the intended suspense.
    - **Also fixed in the same pass, from the same message**: "wright answer should come in green accent not red" — the correct-answer highlight in both Live Round screens (`team-client.tsx`'s reveal view, `host-client.tsx`'s `RevealView`) and async Surge Mode's own reveal (`surge-client.tsx`) used `--gold`, which post-pivot (decision 45) is X11 Maroon — a reddish/pink hue easily read as "not green." Switched all three to the existing `--success` token (`#2e8659`, already used elsewhere for confirmation states like the QR scan success screen) — wrong-selected stays `--accent` (the danger orange-red) unchanged in all three, since that half of the complaint was never in question.
    - **Live-verified end-to-end against production, driving the round through real phase transitions while watching the real student screen react over Realtime** — not just code review. Faked a professor session via `SET LOCAL request.jwt.claims` to create/advance a real Live Round from a real Surge; logged into the actual PWA as a real disposable-reset demo student (via the dev-mobile-bypass, decision 58); joined solo; deliberately selected the *wrong* option. Confirmed live: during `'question'` phase, the picked option showed only the neutral "selected" maroon border (not the correctness colors) and the screen read "Locked in, waiting for the host…" with no reveal text at all. After advancing the round to `'reveal'` via the same faked-professor script (simulating the host's actual click), confirmed via `getComputedStyle` that the correct option (`Joule`) rendered with `border-color: rgb(46, 134, 89)` (`--success`) and the wrong pick (`Newton`) rendered `rgb(199, 64, 26)` (`--accent`), and the page now showed "Not quite." — exactly the intended sequencing. All test fixtures (the round, team, team membership, answer) were deleted afterward; the demo student's disposable password reset follows this project's standing credential-hygiene practice. `npm run typecheck`/`lint`/`build` all passed clean.

74. **Club detail pages (`/clubs/[slug]`) now show real per-club content from the "SOP for Students Conduits" document** (`docs/club-detail-pages-spec.md`, drafted entirely in a separate ideation pass before this build), not just the one-line description that was there before. Two new sections, inserted between the header/socials block and the existing Upcoming/Past events: a **card-trio** (Focus / What You Gain / Activities, club-specific) and a **club rules accordion** (6 questions — joining process, membership size/eligibility, mentor's final authority, member expectations, removal grounds, leadership roles — identical wording on every club's page).
    - **The existing `description` column was reused as the Focus card's copy rather than adding a redundant column** — confirmed against actual usage first (it already held a one-sentence activity summary shown on both the `/clubs` grid and the old detail-page header, which is exactly what "Focus" is). The old seeded values had "Faculty mentor: X" appended inline; that's now redundant since `mentor_name` is its own column, so the 9 clubs' `description` values were updated to the spec's drafted Focus copy (mentor suffix dropped) rather than left duplicated.
    - **Migration 0034** adds `mentor_name text`, `gain text[]`, `activities text[]` to `clubs`; `public_clubs()` extended to return them (`DROP FUNCTION` first, this project's standing rule since decisions 46-48, since adding return columns changes the function's shape). The universal rules FAQ is **not** stored in the DB at all — it's a hardcoded constant in `rules-accordion.tsx`, since it never varies per club and isn't something a professor edits, matching this codebase's existing static-label-map convention (`TYPE_LABEL`, `NAV`).
    - **`BulletListField` (originally inlined only in the Event Report form, decision 50) was extracted into `lib/patterns/bullet-list-field.tsx`** since this is the second place it's needed (the admin club editor's new Gain/Activities fields) — the Event Report form now imports the shared version instead of carrying its own copy.
    - **SRISTI Club has no real Objectives/Activities in the source document** (a gap already known from decision 64's club-roster import) — its card-trio shows only the Focus card plus a "More details coming soon" placeholder, never fabricated Gain/Activities content, exactly as the spec called for.
    - **Live-verified directly in a real browser**: Joules Club (full content) renders all three cards correctly with real bullet lists; SRISTI Club correctly shows only the Focus card + placeholder; the rules accordion actually expands/collapses on click, revealing the real answer text; and the admin editor round-trip was proven for real — opened Joules Club's edit panel in Institution Settings, confirmed every field (Focus, mentor, Gain bullets, Activities bullets) was genuinely prefilled from the database (not just showing placeholders), edited the mentor name, saved, and confirmed via a direct database query the new value persisted — then reverted the test edit. `npm run typecheck`/`lint`/`build` all passed clean throughout; migration 0034 applied to production; all 9 real clubs seeded with the spec's drafted content.

75. **`/get-app`'s own QR code encoded the bare site homepage (`site.url`), not a link to the install page itself** — scanning it from a poster/screen dropped a phone onto the general homepage, one extra tap away from the actual install instructions, rather than landing directly where the QR was supposed to send them. Confirmed the *other* two QR call sites (Live Round's room-code join link, Grid Station's check-in link) were already correct — a not-yet-installed phone scanning the Live Round join QR is already caught by the mobile-PWA gate (decision 29) and shown `pwa-required.tsx`'s "Get the app on your phone" screen, which already links to `/get-app` — so this was specifically an install-QR gap, not a gate gap. Fixed by pointing `/get-app`'s QR (and its companion "link to open on your phone" text, kept in sync since both now read from one `link` constant) at `${site.url}/get-app` instead of the bare `site.url`.

76. **A real, previously-latent bug found from the user's own iPhone testing: "Add to Home Screen" installs likely never actually count as standalone on iOS, meaning the PWA gate could reject the just-installed app forever.** `app/layout.tsx`'s `appleWebApp: { capable: true }` was assumed to emit the legacy `<meta name="apple-mobile-web-app-capable" content="yes">` tag — confirmed via `curl` against a real rendered page that **Next.js 16 only emits the modern `<meta name="mobile-web-app-capable">` tag**, not the apple-prefixed one. Older iOS/Safari versions only key off the legacy tag to decide whether a home-screen shortcut opens as a true standalone app (hiding Safari's chrome) versus a bookmark that still opens with the address bar visible — and WebKit's `navigator.standalone` property (one of the two signals `pwa-gate.tsx`, decision 29, checks) is itself tied to that same legacy tag's presence. Fixed by adding `metadata.other['apple-mobile-web-app-capable'] = 'yes'` explicitly, verified via `curl` that both the legacy and modern tags now render together.
    - **Also fixed in the same pass, from the same report**: `/get-app`'s QR code linked to itself correctly (decision 75) but the on-page instructions didn't address the single most common real-world trap for "nothing happens when I try to install on iPhone" — opening the link inside another app's in-app browser (Instagram/Facebook's own WebView, not real Safari, does not expose "Add to Home Screen" in its share sheet at all). Added an explicit callout telling the reader to open the page in their real browser first if they arrived via another app, and clarified the iPhone instructions to say plainly that there is no install button on iPhone by design (matching `install-button.tsx`'s own already-correct behavior of rendering nothing there, since `beforeinstallprompt` never fires on iOS).
    - **A real JSX whitespace bug caught while writing the above copy, not by tooling**: writing `<strong>Share icon</strong> in the bottom toolbar...` with the space on the same source line still rendered with the space silently dropped (confirmed via `element.innerHTML` inspection, not just a screenshot) — fixed by using an explicit `{' '}` expression between the closing tag and the following text, the same pattern already used earlier in the same paragraph for the space before the `<strong>`. Filed as a reminder that adjacent-JSX-child whitespace can't always be trusted to survive compilation just because the source has a literal space character.
    - **Not independently confirmable from this environment**: whether the `apple-mobile-web-app-capable` fix actually resolves the user's real-device symptom requires a real iPhone re-test, which this session's tooling can't perform — the fix is grounded in a directly-observed, real gap (confirmed via `curl` against the actual rendered HTML, not guessed), but the *causal* link to "no PWA is downloading" is the most plausible explanation found, not independently proven end-to-end on real hardware.

77. **Five real bugs found from the actual 2026-07-17 live demo with real students, all fixed and live-verified.**
    - **Host lobby team/member counts drifted upward forever** ("1 team, 3 students joined" when only 2 real students were playing) — `host-client.tsx`'s realtime handlers only listened for `INSERT` on `live_round_teams`/`live_round_team_members` and hand-incremented local counters, so a student leaving (a `DELETE`, e.g. a solo team auto-deleting once empty per decision 60) was invisible to the counter — it could only ever go up. Fixed by switching to `event: '*'` handlers that call the existing `refreshTeamCount()`/`refreshMemberCount()` (a real re-query), not manual arithmetic.
    - **That fix alone wasn't enough** — live-verified it still didn't update without a manual reload, which traced to a second, genuinely separate root cause: both tables default to `REPLICA IDENTITY DEFAULT` (Postgres only includes primary-key columns in a `DELETE`'s old-row replication data), and neither table's primary key includes `round_id` (`live_round_team_members`'s PK is `(team_id, student_id)`; `live_round_teams`' is just `id`) — so a `DELETE`'s old row literally never carried `round_id`, and Supabase Realtime's `round_id=eq.<x>` filter couldn't be evaluated against it, silently dropping the event rather than deliver a possibly-wrong match. Migration 0036 sets `REPLICA IDENTITY FULL` on both tables, which fixed it for real — live-verified end-to-end (a leave+rejoin now updates the host lobby's count with zero reload).
    - **A live-round-only "captain" concept was added**: real-device testing found every team member could answer independently and simultaneously on their own phone, which isn't the intended "one shared team answer" quiz feel. Migration 0035 restricts `submit_live_answer` to only the team's `created_by` (already tracked since decision 60, no new column) — everyone else's phone shows the question read-only with "Your team captain is answering for the team," enforced server-side (not just hidden client-side). The pooled/split Joule crediting itself is untouched, since it already keys off the team roster, not who physically tapped.
    - **"Finish round" appeared to do nothing** — traced to `host-client.tsx`'s `advance()` silently swallowing any RPC error with zero user feedback (`if (!error && data) {...}` — no `else`). Added error display. **Live RPC-level testing then proved `complete_live_round()` itself works correctly with no error** (a full round lifecycle — captain-only answers, one correct one wrong — completed cleanly and credited the ledger correctly), so the real demo-night failure was almost certainly this exact silent-failure class, now visible if it recurs.
    - **"Negative marks is not implemented" turned out to be a configuration gap, not a code bug** — the same live RPC test proved negative marking's math is and always was correct (a wrong answer's `-8` correctly pulled the team's pooled total down before splitting it), but there was no way to *set* `negative_points_per_wrong_answer` on a surge after creating it (only at creation, decision 49). Added a "Scoring" editor to the (draft-only) Surge Builder (`updateSurgeScoringAction`) so this is now actually configurable on an existing surge.
    - **iPhone lock-screen audio lingered until the PWA was uninstalled** — `sound.ts`'s cached `<audio>` elements never told iOS a media session had ended. Added explicit `navigator.mediaSession.playbackState = 'none'` + `metadata = null` on each clip's `ended`/`pause` events. **Disclosed, not glossed over**: this is a best-effort fix grounded in the standard iOS media-session API, not independently re-confirmed on a real iPhone from this session — the next real demo is the actual proof.
    - **A recurring project trap hit again during this exact verification pass**: even with the client-side code fix correct and the DB migration applied, the SAME browser tab kept showing the stale, pre-fix behavior until its service worker was explicitly unregistered and Cache Storage cleared — the now-familiar Turbopack-dev-chunks-aren't-content-hashed trap (decisions 57/60/65/66). Confirmed again here as a standing rule: after any client-code fix, clear the tab's SW/cache before trusting what it shows, don't assume a code fix is broken just because a stale tab still shows the old behavior.
    - **Live-verified thoroughly**: created real rounds/teams via direct RPC calls (`SET LOCAL request.jwt.claims`, the project's standing technique) to prove the non-leader-answer rejection, the negative-marking math, and `complete_live_round()`'s correctness — then separately drove a real browser (fresh tab, SW cleared) through an actual leave+rejoin to confirm the host lobby's count now updates live with zero reload. All test fixtures (rounds, teams, surges, questions, ledger rows) were created and fully cleaned up afterward. `npm run typecheck`/`lint`/`build` all passed clean throughout; migrations 0035 and 0036 both applied to production.

78. **4-tier RBAC rework: `super_admin` (platform-wide) replaces the old platform-wide meaning of `professor`; `professor` becomes club-scoped, same shape `committee_member` already had.** Live-testing the 3-tier RBAC surfaced the real operating model directly from the user: one Professor per club (not platform-wide), Committee Member loses QR/scanner access entirely (event creation + Event Report writing only), and a new top tier holds the platform-wide powers Professor used to have (Institution Settings, full ledger, manual Joule adjustments, admin roster). Migration 0038 redefines `can_manage_club()` (the one scoping primitive `can_manage_event()`/`can_manage_surge()` both delegate to) to treat `professor` the same way `committee_member` already worked, plus a `super_admin` bypass — this single change re-scopes most of the app automatically. Explicit per-site moves: `admin_adjust_joules`/`admin_set_student_status`/`admin_create_admin`/`admin_set_role`/`admin_student_totals`/the Vault and Institution Settings RLS policies all moved from `is_professor()` to `is_super_admin()`. Every `is_committee_member_or_professor()`-as-"any staff, platform-wide" policy (`joule_transactions`, `surge_answers`, `live_round_answers`, `event_reports`, `event_registrations`, `gallery_images`) was club-scoped too — confirmed with the user this should apply everywhere, not just the specific screens asked about, closing the long-standing "Committee Member scoping covers writes, not reads" Known Open Item for good. A new RLS policy, `staff reads registered students for own club events`, was needed alongside this — the old blanket "professor reads all students" policy had silently powered the registrations dashboard's student name/email/phone join, and moving it to `super_admin`-only would have broken that dashboard for a real club Professor; the new policy grants exactly the legitimate need (a student's info is readable by staff who can manage an event that student registered for). The QR scan window also tightened in the same migration, per the user's explicit ask: one-sided, event-scheduled-start → +20 minutes, replacing the old ±15-minute window; `current_qr_token()` (previously display-only, no window check) now returns null outside the window too, so Grid Station can show "opens at X"/"window closed" instead of a token doomed to fail.
    - **Verified live**: 12 cross-club RLS/RPC checks against disposable test fixtures (a club-scoped Professor cannot read/write another club's events or ledger, `can_manage_club()` returns the correct boolean for both clubs, the QR window opens/closes correctly, `admin_adjust_joules` is now `super_admin`-only) — all passing, fixtures fully cleaned up. All 13 pre-existing "professor" admin rows (3 real, 10 disposable test accounts from this session) confirmed correctly auto-promoted to `super_admin`. `npm run typecheck`/`lint`/`build` clean throughout.
    - **A process mistake worth remembering**: Claude Code's auto-mode classifier blocks scripted mutations to security-relevant live-DB objects even mid-session (same restriction hit in decisions 31/46/49) — the user ran each of these migrations by hand in Supabase's SQL Editor. Once, the user pasted and ran BOTH the `-- migrate:up` AND `-- migrate:down` sections back to back, which cleanly reverted the whole migration (a migration file's down-section is a rollback script, not a second step) — caught immediately by re-querying `admins.role`, and the up-section was simply re-run. No data was lost; this is exactly why migrations are idempotent create-or-replace/drop-if-exists statements.
79. **A real gap in decision 78's own audit: three System Ledger RPCs added later (`admin_dashboard_summary`/`admin_tier_distribution`/`admin_club_engagement`, migration 0031, added to the app after the original 0038 audit list was drawn up) were missed entirely and stayed fully platform-wide for any staff tier.** Caught live: a club-scoped Professor's System Ledger showed all 9 clubs' engagement numbers and a platform-wide student/event/Joule count instead of just their own club's. Fixed in migration 0039 by threading the caller's own `club_id` through each function's query (non-`super_admin` callers see only their club's numbers; `students` has no club FK at all — decision 46 — so "students engaged" is scoped via a join through `event_registrations` for events belonging to the caller's club, the same shape as decision 78's new students-read policy). **Migration 0040 was needed immediately after**, fixing a real bug in 0039 itself: `admin_club_engagement()`'s own return signature has a column literally named `club_id`, which collided with the local variable of the same name inside the function body — the exact `RETURNS TABLE` ambiguous-column class already documented in decision 25, caught by testing the migration right after applying it, not by inspection.
    - **Verified live**: the same raw query the Ledger page runs, executed directly as the test Professor fixture, now returns `total_students: 1, total_clubs: 1, total_events: 2` instead of the platform totals (`31/9/10`); `admin_club_engagement()` returns exactly 1 row (their own club) for a club-scoped caller and all 9 for `super_admin`, confirmed both ways.
80. **A second real gap found by the same live-testing pass: `event_reports`' pre-existing "anyone reads event reports" policy (0019, `using (true)`, needed for the legitimate public Event Reports pages) combines permissively (OR) with the newer club-scoped policy from decision 78 — RLS can't distinguish "give me report X" from "give me every report," so the blanket public policy silently defeated the scoped one for every reader, staff included.** Confirmed live: a Cityscape Committee Member's own Event Reports list showed reports belonging to Shastra Club and StratEdge Club. Fixed the same way every other public-facing read in this app already works (`public_events()`, `public_clubs()`, `public_event_stats()`): migration 0042 drops the blanket table policy and adds two narrow RPCs — `public_event_reports()` (the public list page) and `public_event_report(p_id)` (the public detail page) — while the raw table becomes staff-only, properly club-scoped. The admin list page and the `.docx` download route both inherit the fix automatically, since they already read through the RLS-scoped client with no code change needed.
    - **Verified live**: the same query that leaked 3 cross-club reports before now returns 0 rows for the same Cityscape CM (who has none of their own yet); `public_event_reports()` still correctly returns all 3 reports for anonymous visitors, and `public_event_report(p_id)` still resolves individual reports for anonymous callers — the legitimate public feature is unaffected.
81. **Committee Member also loses Live Round hosting access, same reasoning as the QR/scanner restriction (decision 78)** — their job is event creation + Event Report writing, not running a live activity. Nav entry and all three route/action gates (`live/new`, `live/[roundId]`, both `live/actions.ts` functions) narrowed to `['professor', 'super_admin']`.
82. **Real staff accounts should be simple, individually-owned, and genuinely traceable — confirmed directly with the user, not a shared login per club-role.** A shared login (one ID/password the whole WhatsApp group knows) would make "who did this" structurally unanswerable; an individual account per real person, with a simple admin-chosen password (no personal-email requirement — a synthetic ID like `cityscape-cm-priya@synergy.local` works fine, since admin-created accounts are always `email_confirm: true` and nothing is ever sent to the address) preserves both simplicity and real attribution. `createAdminAction` gained an optional `password` form field (falls back to the existing random-generated one when left blank); the roster form's email input now says so explicitly.
    - **The actual traceability didn't exist yet, though** — `audit_log_entries` only ever logged `force_reset`/`manual_joule_adjustment`/`csv_import`/`role_change`, and critically **there was no page anywhere in the app that displayed it at all** — pure write-only, invisible even to a Super Admin. Migration 0041 widens the logged actions to include `event_create`/`event_edit`/`report_create`/`gallery_upload`/`live_round_create`, adds a `log_admin_action()` RPC (`SECURITY DEFINER`, matching the project's own golden-path discipline — `audit_log_entries` has no direct INSERT policy for anyone else), and a shared `lib/jules/audit.ts` helper wired into the four relevant Server Actions. A new `/admin/audit` page (Super Admin only — the platform-wide accountability tool) finally makes the log visible.
    - **Verified live**: 5 real actions logged with correct `admin_id`/action/details for a disposable Professor fixture; a custom (non-random) password confirmed to actually authenticate a real login; an invalid action value rejected by the CHECK constraint; a non-admin (student) confirmed unable to call `log_admin_action` at all. All fixtures cleaned up.
83. **A real, confirmed bug found from an actual real-device submission attempt, not guesswork: Next.js Server Actions default to a 1MB request body limit, and the Event Report form's real phone photo uploads (across 4 attachment categories) were blowing straight past it.** The failure produced a raw, unstyled browser-level error page (not a normal form error) since it happens before the request body is even parsed — meaning zero partial data ever landed (no report row, no uploaded images), which made the symptom genuinely confusing to diagnose from the client side alone. Root-caused via Vercel's actual runtime logs (`npx vercel logs`, after first accidentally creating and having to re-link to the correct existing "jules" project) — the exact error (`Error: Body exceeded 1 MB limit`, digest `1060560556@E394`) matched the digest shown on the real device precisely. Fixed by setting `experimental.serverActions.bodySizeLimit: '25mb'` in `next.config.ts`.
84. **A mobile admin-nav gap, confirmed live-blocking**: the admin shell's sidebar was `hidden ... sm:flex` (desktop-only) and the mobile header had no navigation at all below that breakpoint — just a title and Log Out. This had been flagged once before as a deferred item for the *general public* site specifically, but the *admin* shell had the identical gap and nobody had built it, and it just prevented a real Committee Member from reaching Event Reports on their own phone during live testing. Fixed with `MobileAdminNav` (`admin-nav-client.tsx`) — a hamburger toggle that reuses the exact same `NAV` array and role filter the desktop sidebar already uses, rather than a second nav definition that could drift.

## Where things live

- Product/technical spec (source of truth) → `docs/project-spec.md`
- Starter question content → `docs/starter-question-bank.csv` (not yet imported — see decision 6)
- Golden path (security-first order for this app type) → `docs/golden-paths/portal.md`
- Local setup, in order → `docs/runbooks/jules-setup.md` **(read this first to actually run the app)**
- Tokens → `template/app/globals.css`
- Brand/contact + placeholder constants → `template/lib/site.ts`
- Schema + RLS + RPCs → `template/db/migrations/0003`–`0023` (0001/0002 are IDP template boilerplate, untouched; 0010–0013 are Live Round + the bug fixes, decisions 24–27; 0014 is the General site: Afterglow/Gallery/public_events; 0015–0016 are the security-retrospective fixes, decisions 31–32; 0017 was the avatar system, decision 44 — dropped again by 0018; 0018 drops `avatar_items`/`avatar_items_catalog()` entirely, decision 45; 0019 renames `afterglow_posts` → `event_reports`, adds a `highlights text[]` column, and renames `body` → `summary`, decision 45; 0020 is Pivot Phase 1 — `clubs` table, `club_id` on events/surges, the full RBAC rename + club-scoping rework, decision 46; 0021 is Pivot Phase 2 — `event_registrations`, the computed attendance streak, and dropping `students.streak_days`/`last_active_date`, decision 47; 0022 is Pivot Phase 3 — `season_leaderboard()` fixed to include zero-Joule students + real pagination, decision 48; 0023 is Pivot Phase 4 — `quiz_groups`/`quiz_group_members`, `surge_answer_points()`, `complete_surge()`, the `joule_transactions` type rename, decision 49 — the final approved pivot phase)
- RBAC helpers → `is_professor()`, `is_committee_member()`, `is_committee_member_or_professor()` (replace `is_owner()`/`is_officer_or_owner()`/`is_volunteer_for_event()`, decision 46), plus the scoping primitives `can_manage_club(club_id)`/`can_manage_event(id)`/`can_manage_surge(id)` every club-owned write policy and RPC builds on
- Clubs management (Professor-only) → `template/app/admin/(protected)/settings/clubs-section.tsx` + `createClubAction` in `settings/actions.ts`, wired into `settings/page.tsx`
- Event registration + attendance streak (decision 47) → `event_registrations` table; `register_for_event()`/`unregister_from_event()` RPCs; `student_attendance_streak()` (computed, internal-only, same posture as `student_season_joules`); Dashboard sections in `template/app/(node)/dashboard/page.tsx` ("Upcoming events", "My registered events") + `registration-client.tsx` (Register/Cancel buttons) + `actions.ts` (`registerForEventAction`/`unregisterFromEventAction`)
- Universal leaderboard (decision 48) → `season_leaderboard(p_season_id, p_limit, p_offset)` (same function as before the pivot, fixed in place — no separate `platform_leaderboard`), used only by `template/app/(node)/catalyst/page.tsx` (page-param pagination, Previous/Next links)
- Group/individual quiz scoring (decision 49) → `quiz_groups`/`quiz_group_members` tables; `create_quiz_group()`/`join_quiz_group()`/`leave_quiz_group()` RPCs; the shared `surge_answer_points()` formula function; `complete_surge()` (the finalization RPC — replaces a raw status update in `setSurgeStatusAction` when transitioning to `'complete'`); student UI in `template/app/(node)/surge/[surgeId]/group-registration-client.tsx` + `group-actions.ts`, shown on the draft-status branch of `surge/[surgeId]/page.tsx`; admin scoring config fields on `template/app/admin/(protected)/surges/new/form.tsx`
- Live Round real multi-student teams + pooled/split scoring (decision 60, mirrors decision 49's formula/split logic) → `live_round_team_members` table; `create_live_team()`/`join_live_team()`/`leave_live_team()` RPCs (replace `join_live_round()`); `complete_live_round()` (called automatically from `host_advance_round`'s `leaderboard → complete` transition, not a separate step); student UI in `template/app/(node)/live/[roundId]/team-formation-client.tsx` + `team-actions.ts`, shown when a student has no team yet for a `lobby`-phase round in `live/[roundId]/page.tsx`; `/live/page.tsx` is now room-code-only (`resolveRoomCodeAction` in `live/actions.ts`)
- Generated DB types (hand-written placeholder — see runbook) → `template/lib/supabase/database.types.ts`
- Auth/session helpers → `template/lib/auth/session.ts`
- Route protection → `template/proxy.ts` (Next 16 renamed `middleware.ts` → `proxy.ts`; also Layer 1 of the mobile-PWA gate, decision 29)
- Active-nav highlighting → `template/app/admin/(protected)/admin-nav-client.tsx` (owns the nav array + icons itself, decision 40 — do NOT pass icon components as props from a Server Component, they aren't plain-serializable) and `template/app/(general)/layout.tsx` (now a Client Component for the same reason `usePathname()` is needed)
- App icon/logo → `template/public/icons/icon.svg` (decision 41); Gallery placeholder art → `template/lib/components/gallery-placeholder.tsx`
- Homepage interactive hero → `template/lib/components/energy-field.tsx` (ambient background) + `template/lib/components/hero-atom.tsx` (interactive centerpiece), decision 42, wired into `template/app/(general)/page.tsx` only
- Scannable QR codes → `react-qr-code` (decision 43), used directly in `station-client.tsx`, `host-client.tsx`'s `LobbyView`, and `get-app/page.tsx` — no shared wrapper component, each call site is a one-line `<QRCode value={...} size={...} />`
- In-app camera QR *reading* (decision 56, the reverse direction) → `jsqr`, used only in `app/(node)/scan/camera-scanner.tsx` — decodes the same `${site.url}/scan?e=&t=` link the QR-*generating* code above already produces, then hands off to the existing `redeem()` in `scan-client.tsx`
- **Avatar system (decision 44) was fully removed in decision 45** — `template/lib/components/avatar-3d/*`, `template/lib/jules/avatar-items.ts`, `template/lib/components/demo-controls.tsx` are all deleted; `three`/`@react-three/fiber`/`@react-three/drei`/`gsap`/`@gsap/react` removed from `package.json`; migration 0018 drops the `avatar_items` table/RPC. Historical asset-generation prompts (from before the pivot) still exist at `docs/prompts/asset-generation-prompts.md` for reference only — not current scope.
- Node (student) routes → `template/app/(node)/*` (now includes `live/`; gated by `pwa-gate.tsx`, decision 29; `profile/page.tsx`/`dashboard/page.tsx` are back to a plain listing/season-card, no avatar), auth routes → `template/app/(auth)/*`, admin → `template/app/admin/login` (unguarded) + `template/app/admin/(protected)/*` (guarded, decision 20; now includes `live/`, `event-reports/`, `gallery/` — renamed from `afterglow/`, decision 45), public → `template/app/(general)/*` (now includes `event-reports/`, renamed from `afterglow/`) + `template/app/mobile-required/page.tsx` (no auth guard at all)
- Event Report authoring (renamed from Afterglow, decision 45; rebuilt to match the real AU template, decision 50) → `template/app/admin/(protected)/event-reports/new/form.tsx` — event dropdown with auto-pulled attendance/Joules stats and auto-filled Date/Time/Venue/Organised-by-club (read-only, from the linked event); Coordinators (repeatable list, decision 72); Introduction; Objectives (bullets); Event Highlights (paragraph); Outcomes (bullets); Conclusion; an Attachments checklist. Public display in `template/app/(general)/event-reports/[id]/page.tsx`, which also has a "Download Word report" link. `.docx` export (decision 72, superseding decisions 51–52's from-scratch `docx`-js generation) → `template/app/api/event-reports/[id]/docx/route.ts` fills the real, literal reference `.docx` file at `template/lib/jules/templates/event-completion-report.docx` (tagged with `docxtemplater`/`pizzip` placeholders — simple `{tag}`s plus `{#coordinators}`/`{#objectives}`/`{#outcomes}` paragraph-loops) rather than reconstructing the layout in code; the path is listed in `proxy.ts`'s `PUBLIC_PATHS` alongside the page route — a page and its supporting API route are two separate entries, not one.
- Real Adani University logo (decision 52, relocated by decision 53) → `template/public/brand/adani-university-logo.png` (background keyed transparent, now shown only in the general public site's footer — not the nav headers, not the admin panel) and `template/public/brand/adani-university-logo-print.jpg` (original opaque source, for embedding inside generated `.docx` files where the page background is white).
- Light theme tokens (decision 53, "warm ivory") → `template/app/globals.css`'s `:root` block — same token *names* as always (`--background`, `--card`, `--gold`, etc.), only the hex values changed; do not hardcode a new hex anywhere else, read from these tokens.
- Event registration + reminders (decision 54, built on Phase 2's backbone — decision 47) → student event detail page `template/app/(general)/events/[eventId]/page.tsx` (full info + Register/Cancel, reused by Dashboard's event rows via `Link`) and its `registration-client.tsx`; admin event **edit** page (new capability) `template/app/admin/(protected)/grid/[eventId]/edit/page.tsx` + `editEventAction` in `grid/actions.ts`; Dashboard's live-computed "Reminders" section in `template/app/(node)/dashboard/page.tsx`. `events.registration_form_url` and `event_registrations.location_at_registration` (migration 0025) are the two new columns; `public_events()` (migration 0026) is the security-definer RPC every public-reachable event read must go through, never a raw `.from('events')` select (RLS only grants `authenticated` reads on the raw table).
- Tier/CSV/QR domain logic → `template/lib/jules/*` (now also `format-date.ts`, decision 30)
- Shared blocking screen → `template/lib/components/pwa-required.tsx` (used by both gate layers)
- Launch splash → `template/lib/components/launch-splash.tsx` (wired inside `pwa-gate.tsx`'s allowed branch, decision 33)
- Winner-moment beat + final-standings reveal → `template/lib/components/winner-burst.tsx` + `template/lib/components/reveal-scoreboard.tsx` (decision 34), used by `surge/[surgeId]/matrix/matrix-client.tsx` and both Live Round client screens
- Sound + haptics helpers → `template/lib/jules/sound.ts` + `template/lib/jules/haptics.ts`; real audio files go in `template/public/sounds/` (see its `README.md`; six now needed, not yet supplied)
- Tier-up celebration → `template/lib/components/tier-up-celebration.tsx` (decision 36), wired into `app/(node)/dashboard/page.tsx`
- Shared empty/no-data pattern → `template/lib/patterns/empty-state.tsx` (decision 38 migrated ~8 more call sites onto it)
- First-Owner bootstrap → `scripts/bootstrap-owner.ts`
- Project docs 01–11 (not generated — see Resume note) → `docs/`
- Live deployment → `https://jules-purven-s-projects.vercel.app` (Vercel project `purven-s-projects/jules`, linked from `template/`, see decisions 21–23); also aliased as `https://jules-delta-nine.vercel.app` (same deployment, several aliases on this project)
- **4-tier RBAC (decision 78)** → `super_admin`/`professor`/`committee_member` on `admins.role`; `is_super_admin()` + the redefined `can_manage_club(club_id)` (the one scoping primitive `can_manage_event()`/`can_manage_surge()` delegate to) in `db/migrations/0038_rbac_super_admin_qr_window.sql`. Club-scoping fixes for gaps this migration missed live in `0039_ledger_club_scoping.sql`, `0040_fix_admin_club_engagement_ambiguity.sql`, and `0042_event_reports_public_rpc.sql`. Institution Settings' role selector + optional custom-password field → `app/admin/(protected)/settings/{actions.ts,roster-section.tsx}`.
- QR scan window (event start → +20 min, decision 78) → inline in `redeem_event_scan()`/`current_qr_token()`, `0038_rbac_super_admin_qr_window.sql`; the "opens at X"/"window closed" UI states → `app/admin/(protected)/grid/station-client.tsx`.
- Simplified System Ledger for Professor/Committee Member (event picker + registered-students list + CSV download, no charts) → `app/admin/(protected)/ledger/{simple-ledger-view.tsx,ledger-event-picker.tsx,registered-students-list.tsx}`; `page.tsx` branches on `admin.role` and renders Super Admin's original dense dashboard unchanged otherwise.
- Audit trail (decision 82) → `log_admin_action()` RPC + widened `audit_log_entries.action` CHECK constraint, `0041_audit_log_expansion.sql`; shared caller helper `lib/jules/audit.ts`; viewer page `app/admin/(protected)/audit/page.tsx` (Super Admin only).
- Mobile admin navigation (decision 84) → `MobileAdminNav` in `app/admin/(protected)/admin-nav-client.tsx`, wired into the mobile header in `app/admin/(protected)/layout.tsx`.
- Server Action body size limit (decision 83, real photo uploads on the Event Report form) → `experimental.serverActions.bodySizeLimit` in `next.config.ts`.
- 40 disposable test accounts for manual walkthroughs (20 students, 10 Committee Members — one per real club, 10 originally-professor-now-super_admin) all use the password `test1234`, email pattern `synergy-test-{role}{NN}@gmail.com`; one additional real Professor test account, `synergy-test-prof-cityscape@gmail.com` (same password), scoped to Cityscape Club specifically for cross-role walkthrough testing.

## Known open items (do not invent)

- **★ Only one placeholder club ("General Club") exists** — the real 12-13 club roster for the MBA program hasn't been supplied. Add them via Institution Settings → Clubs (Professor-only) once known; every existing surge/event is currently attached to the placeholder.
- **★ RESOLVED — Committee Member/Professor read scoping now covers everything, not just writes** (decision 78). `monthly_engagement()`/`event_engagement_totals()`/`admin_dashboard_summary()`/`admin_tier_distribution()`/`admin_club_engagement()`, the full Joule ledger, `surge_answers`, `live_round_answers`, `event_reports`, `event_registrations`, and `gallery_images` are all club-scoped now; only Super Admin sees platform-wide data anywhere in the admin app.
- **Live Round's blanket "any staff" read/manage policy on `live_rounds`/`live_round_answers` was left unscoped at the RLS layer** — club-scoping for Live Round is enforced at the RPC level instead (`host_create_round`/`host_advance_round` both check `can_manage_surge`), since a round's club lives on its surge, not on `live_rounds` itself. A Committee Member could still technically read another club's live round row directly (low severity — Live Round content is already a "Kahoot PIN"-trust model, decision 24's own reasoning), but couldn't host/advance it.
- **★ The Dashboard's new "Upcoming events"/"My registered events" sections (decision 47) were never directly browser-verified** — they sit behind the mobile-PWA gate's Layer 1 (decision 29), which this session's desktop-UA browser tooling can't pass, same limitation as every earlier Node-route slice. The underlying business logic (registration validation, attendance stamping, the streak formula) was instead proven directly against production via `SET LOCAL request.jwt.claims` RPC calls — a stronger bar of proof for this kind of feature than a screenshot, but a real phone/PWA check of the actual rendered sections is still the next real proof.
- **Event registration has no admin-side visibility yet** — there's no "who registered for this event" view for a Committee Member/Professor (only the student's own Dashboard shows their registrations). A per-event attendee list (registered vs. attended vs. no-show) would be a natural Grid Station or event-detail addition later, not scoped in Phase 2.
- **The attendance streak can currently only ever go up via events the student registered for in advance** — there is deliberately no path to backfill a streak credit for a walk-in scan (matches the plan's own "only a matched, attended registration counts toward streak" framing), so a student who only ever walk-ins to events (never pre-registers) will always show a streak of 0, which is working as designed but worth being able to explain if a student asks.
- **★ Catalyst Records' `/catalyst` page was never directly browser-verified** for the same reason as the Dashboard sections above (mobile-PWA gate, decision 29) — the `season_leaderboard()` RPC's actual correctness (zero-Joule students included, pagination windows correctly, `total_count` stays accurate across pages) was instead proven directly against production. A real phone/PWA check of the rendered page — especially the Previous/Next pagination controls with a season that has more than 50 students — is still the next real proof, and this environment's live roster (4 students) couldn't exercise a genuinely multi-page scenario.
- **The public Leaderboard page (Phase C) deliberately exposes student names + point totals to anonymous, logged-out internet visitors, not just other students** — `public_season_leaderboard()` is the first RPC in this project granted to `anon` that returns real per-student data (every earlier public RPC returns event/club metadata, never a student's own name attached to their points). This was the explicit intent of the Phase C plan item ("so non-students can see it too"), not an oversight, and any student could already see every other student's name+points via the pre-existing authenticated `/catalyst` page — but it's worth a deliberate, informed decision (not a silent default) if this ever needs to be narrowed later, e.g. behind a login wall or with names abbreviated.
- **Real ties are unbroken beyond SQL's default `rank()` semantics** — two students with identical Joule totals both correctly get the same rank number (e.g. two students both at 0 J both show rank 2), and the next distinct rank skips accordingly (no rank 3 if two students tie at rank 2 and there's no one else). This is standard, correct leaderboard behavior, not a bug — noted here only because it's worth knowing before someone asks "why did rank 3 disappear."
- **★ The group-registration screen (decision 49) was never directly browser-verified** for the same mobile-PWA-gate reason as every other Node-route slice this pivot — the actual pooled-scoring math (the real risk in this phase) was instead proven via a real RPC test with an independently-reimplemented-in-JS formula, diffed against actual database output for a constructed 4-student scenario, all 8 resulting ledger rows matching exactly. A real phone/PWA walkthrough of forming a group, playing a Surge as a team, and watching the admin close it is the next real proof.
- **Async Surge Mode's Joule credit is now deferred until an admin closes the Surge** (decision 49) — students no longer see their Dashboard/season total update live as they answer during an ongoing async Surge; the quiz UI still shows instant per-question correct/incorrect + a running local tally, but the real ledger write (and everything downstream of it — season_joules, tier, streak) only lands once `complete_surge()` runs. This is a deliberate, necessary consequence of pooled group scoring (you can't split a group's pool before everyone's answered), not an oversight — but it's a real behavior change from how async Surge Mode worked before this phase, worth remembering if a student asks "why didn't my Joules show up yet."
- **No per-event/per-surge admin view of "who's in which group" exists yet** — an admin creating/running a Surge can't currently see the quiz-group rosters from the admin side (only students see this, via their own group-registration screen). A read-only admin panel showing groups + members for a Surge would be a natural addition later, not scoped in Phase 4.
- **The Surge Builder's existing status-transition UI wasn't re-designed for the new `complete_surge()` semantics** — marking a Surge complete now runs real scoring logic (can fail, e.g. if already complete) instead of a trivial status flip; the admin-facing button/flow itself is unchanged and still says whatever it said before, so a `complete_surge()` error (e.g., a double-click race) would surface as a generic thrown-error message rather than a polished "already complete" UI state. Worth a small UX pass later, not required for correctness.
- Real college email domain(s) for `allowed_domains` (decision 10).
- Real registrar season/term dates (decision 9).
- Real club membership size, to confirm the 500-concurrent sizing assumption (spec §11) still holds.
- Push notification service choice — explicitly deferred to Phase 4, non-blocking.
- **★ Supabase project region is `ap-northeast-1` (Tokyo), not `ap-south-1` (Mumbai).** Flagged to the user live during setup (Jules is very likely an Indian college per the Vishwambhari Stuti reference — decision 9). User chose to keep Tokyo for now. Region is cheap to set at creation, expensive to move later (a real Postgres migration, not a config flip) — revisit before real student PII accumulates if DPDP data-residency matters.
- `database.types.ts` is hand-written, not generated — `supabase gen types --db-url` needs a `supabase login` / `SUPABASE_ACCESS_TOKEN` (the CLI version in this environment requires auth even for direct-db-url mode); the hand-written file matches the applied schema and was validated by `npm run build` + a live signup/login round trip, but a real regen is still worth doing once someone can `supabase login` interactively.
- **★ RESOLVED — a real scannable QR image now exists in all three places it was wanted** (decision 43): QR check-in Grid Station, Live Round room codes, and the `/get-app` install page, via `react-qr-code`.
- **★ The General site's header (`app/(general)/layout.tsx`) has zero responsive handling** — a flat flex row (`Home`/`Events`/`Afterglow`/`Gallery`/`Get the App`), no breakpoint, no mobile nav — confirmed cramped on a narrow viewport via a live screenshot. User explicitly deferred this: fix it as part of a dedicated mobile-responsiveness pass later, not now. **Do not build a hamburger menu until that pass is explicitly started.**
- **The General homepage's "moto"/mission copy is derived from `docs/project-spec.md` §2 (the Joule/Shakti conceptual foundation already in the spec), not fabricated marketing copy** — but it hasn't been reviewed by the actual club, and there's no real founding date, member count, or other concrete club facts anywhere on the site yet.
- **★ RESOLVED — the mobile-PWA gate's Layer 2 (standalone-mode check) is now confirmed on a real device.** The user personally tested it: the PWA installed and worked correctly on a real phone, and opening the student links in a real laptop browser correctly bounced to the "get your phone" screen instead of exposing any student/quiz content. This was the single most important remaining unverified piece from decision 29 — it's real now, not just reasoned through from an accessibility tree.
- **Afterglow's real-stats display** (`public_event_stats()`) only aggregates `joule_transactions.event_id`-scoped rows (QR check-in events) — a recap of a Surge-type event wouldn't show meaningful attendance numbers unless that Surge's `event_id` is also set. Not an issue for the common case (recapping a meeting/session/volunteer task), flagged as an edge case.
- Docs 01–11 + App PRD + Data Model & Security were never generated (skipped at the user's request) — the Data-Model-&-Security equivalent lives as comments in the migration files instead of a standalone doc.
- **Secrets hygiene:** the Supabase publishable key, secret key, and DB password were shared directly in chat (not avoidable given the environment) and are now sitting in this session's transcript. They're correctly gitignored (`template/.env.local`) and not committed, but the user may want to rotate the DB password once real student data is in play, purely because it transited a chat log.
- **Live Round's projector screen shows the room code as large text only, no rendered QR image** — matches the existing Grid Station approach (decision 18) and the same one-dependency-rule constraint; a real QR image is one flagged, deliberate follow-up covering both features at once.
- **`qr_secret` was rotated** (not just the code fix, decision 31) — it sat exposed to any anonymous caller for the period between the original build and this retrospective; standard practice after a real secret exposure is to rotate the secret too, not just close the hole. Done via a direct `update app_secrets ...`, invalidating every token computed from the old value. No evidence anyone actually called it during the exposure window, but no reason not to rotate regardless.
- **`events.geofence_lat`/`geofence_lng` are readable by any authenticated student** via the broad `events` SELECT policy (decision 31's retrospective), even though no client code actually reads them (the scan flow sends the student's own coordinates to the server for internal comparison). Lower severity since geofence is already a soft flag, not a hard block, but real over-exposure worth closing — likely fix is a public-safe view/RPC students actually query instead of the raw table, same pattern as `public_events()`.
- **The reported "10s to change tab/page" lag could not be fully reproduced or root-caused from this environment** — real measurements against the live production site (Navigation/Resource Timing APIs, not guesses) showed TTFB varying 48ms–2.4s across pages in one session and a ~3–4.5s cold-cache first load, but nothing matching a literal 10s in isolation; this environment's own sandboxed network is also a real confound that can't be ruled out. Four genuine sequential-await page loads were found and parallelized (`/clubs/[slug]`, `/leaderboard`, both Live Round play/host pages) — real, safe wins, but modest in magnitude. **One undecided trade-off flagged, not silently changed**: this session's own Phase A service-worker fix (network-first for page navigations, to kill the stale-cache bug) means every navigation now always waits for a full network round-trip with no benefit from cache on a merely-slow (not failed) connection — a stale-while-revalidate strategy would get both speed and freshness, but rewriting the SW risks reintroducing the staleness bug just fixed if done carelessly. A real device on the actual reported-slow network is the next real diagnostic step; Vercel's own function logs/analytics (not checked from here) would show real cold-start timings.
- **★ The service worker's `/_next/static/*` cache-first rule (decision 57) assumes content-hashed filenames, which is true for a real production build but NOT true for `next dev` with Turbopack** — dev chunk filenames are stable regardless of content, so a browser tab that already has the SW installed can keep serving a pre-edit chunk indefinitely, surviving even a full dev-server restart with `.next` cleared (discovered while verifying decision 60 — confirmed by fetching the same chunk URL directly via `curl`, which returned the current code, proving the server was fine and the browser's own SW cache was stale). Fix for a suspicious "my fix isn't taking effect" moment during local dev: unregister the service worker and clear Cache Storage in the tab (`navigator.serviceWorker.getRegistrations()` + `caches.keys()`), not just reload or restart the server. Not a production bug — only a local-dev-testing trap, worth remembering before concluding a fix is broken.
- **The login/signup rate limiter (`lib/security.ts`) is explicitly in-memory and single-process** — its own docstring already discloses this resets per instance, which matters more than usual here since this is deployed to Vercel's serverless functions specifically (multiple instances, no shared memory) — brute-force protection on login is weaker in production than the code make it look. Its own docstring recommends Upstash/Redis for real multi-instance protection.
- **★ Live Round re-verified on real, separate hardware over production** (not same-machine browser tabs this time): a real laptop hosted (advancing all 20 "Season Opener Surge" questions) while a real phone joined as a team and played the full round to completion, live over the actual deployed site — the strongest real-world proof this feature has had. Still unproven beyond one team: multi-team behavior (the "N of M teams answered" live counter, correct scoreboard ranking/ties across teams) is reasoned to be correct from the RPC logic but hasn't been exercised with two simultaneous teams yet.
- **`npm run db:types` still can't run** (same `supabase login` gap as before) — `database.types.ts` was hand-updated again for the new tables/RPCs and validated the same way: `typecheck` + `build` + real RPC calls against the live DB, not a real `gen types` diff.
- **No custom domain yet** — live on the default `jules-purven-s-projects.vercel.app`. Fine for testing; get a real domain before promoting beyond soft launch (and update `lib/site.ts`'s `url` + `NEXT_PUBLIC_SITE_URL` in Vercel to match).
- **Git → Vercel auto-deploy is wired up but unproven** — repo connected + Root Directory set to `template` via the API (decision 23 updated). The next real push to `main` is the first real test; if it doesn't auto-deploy, check Vercel dashboard → Project → Settings → Git.
- **"Season Opener Surge" was imported via a script, not the actual Surge Builder UI** — the data is real and correctly shaped (verified via SQL), but the CSV-import *code path itself* (the actual button click → parse → review table → confirm flow) hasn't been exercised live yet, only reasoned to be equivalent. Worth clicking through once the preview browser tooling is available again.
- Not yet done: regenerate `database.types.ts` for real, a real QR image, docs 01–11.
- **★ RESOLVED — all 6 real sound files are in place** (`template/public/sounds/{tick,correct,incorrect,drumroll,winner,tier-up}.mp3`), confirmed on disk. `lib/jules/sound.ts`'s own docstring already reflects this ("the club's own real recordings, replacing the original synthesized placeholder tones"). Not independently re-confirmed how they sound on a real device from this session, but playback is wired and the files are genuinely present.
- **The launch-splash's `prefers-reduced-motion` skip path was verified by code review + the logically-identical `seenToday` skip path (same early-return shape, same code), not by directly forcing the media feature** — this environment's preview tooling doesn't expose a reduced-motion emulation override (unlike `colorScheme`, which `preview_resize` does support). Worth a quick real-device or DevTools Rendering-tab check.
- **Verified via a temporary throwaway route (`app/dev-preview/*`, deleted after use, plus a momentary `proxy.ts` PUBLIC_PATHS entry, reverted after use)**, not the real gated routes directly — a desktop preview browser's User-Agent can't pass the mobile-PWA gate's Layer 1 (decision 29), so `/dashboard`, `/live/*`, and `/surge/*/matrix` couldn't be driven end-to-end in this session's browser tooling. The components themselves (`launch-splash.tsx`, `winner-burst.tsx`, `reveal-scoreboard.tsx`) were confirmed working correctly in isolation; a real phone (or a session with real mobile-UA tooling) is the next real proof for the actual Live Round/Surge Matrix screens using them.
- **Goal 2's explicit asks are now substantively closed**: a full audit found the color-consistency pass and "major feature identity" work both done (decision 40 — active-nav highlighting in both nav systems, the one real remaining gap; the palette itself was already consistent). Any further Goal 2 visual work now would be net-new scope, not closing an open item. Goal 3 (load/concurrency simulation) and Goal 4 (11-club replication, full vocabulary re-skinning) are both established with the user but have zero implementation yet.
- **★ Passing a component reference (e.g. a Lucide icon) as a prop from a Server Component to a Client Component throws at runtime** ("Only plain objects can be passed to Client Components from Server Components") — TypeScript does NOT catch this (a component type checks fine as a prop type), so it's invisible to `typecheck`/`lint`/`build` and was only caught by loading the page in a real browser (decision 40). If a future Server Component needs to hand icon choices to a Client Component, either render the icon as JSX first (a React element serializes fine) or have the Client Component own the icon set itself and pass down only plain data (a string key, a role) — the pattern now used in `admin-nav-client.tsx`.
- **This phase's live-verification note**: the tier-up celebration, rotating splash verse, and empty-state migrations were confirmed via the same temporary-throwaway-route technique as before (`app/dev-preview/*`, deleted after use). The Live Round kumkum-chrome change and several admin empty states (Ledger, Vault) were instead confirmed on the REAL routes directly, since admin routes aren't behind the mobile-PWA gate (decision 29) — a real Live Round was created and screenshotted live, then its test data cleaned up. Only the student-side Live Round/Surge Matrix screens still rely on the throwaway-route workaround for verification; a real phone remains the next real proof for those.
- **A real testing-methodology footgun, worth remembering for next time**: `document.querySelector('button')` on an authenticated admin page can silently match the sidebar's "Log out" button instead of the intended form-submit button if it appears earlier in DOM order — this caused two confusing false "session expired" moments during this phase's live verification before being traced. Always match admin buttons by their visible text content when more than one button exists on the page, not a bare tag selector.
- **The audio spec sheet (6 files: `tick`/`correct`/`incorrect`/`drumroll`/`winner`/`tier-up`) was delivered to the user in chat this session, not written into a repo doc** — worth transcribing into `public/sounds/README.md` if it needs to survive independently of this conversation.
- **The new app icon (`public/icons/icon.svg`) was only visually spot-checked at normal/large size in this session's browser tooling** — a true small-scale (favicon/32px) rendering check wasn't possible with the available preview tooling (data-URL navigation and DOM injection into the live Next.js page both got overridden/blocked). The design uses thick, high-contrast strokes specifically because that survives downscaling well, but a real favicon-tab check is still worth a quick look next time a browser is available for it.
- **No real photos exist for the Gallery yet** — the placeholder illustration (decision 41) is a deliberate stand-in, not a fix; swap it out the moment the club has real event photos to upload.
- **★ `motion/react`'s `animate` prop and `style={{x, y}}` transform binding silently do nothing on nested SVG `<g>`/`<motion.svg>` elements** — no error, no warning, typechecks and builds clean, but `getComputedStyle` confirms no transform is ever actually applied (decision 42). If a future feature needs animated SVG transforms via `motion`, verify it actually renders in a real browser before trusting it — don't assume parity with `motion.div`/HTML elements. Plain CSS `@keyframes` + React state/CSS transitions worked reliably instead.
- **The homepage hero's pointer/touch parallax (`hero-atom.tsx`) is unproven on a real device.** This session's browser tooling can dispatch a synthetic `PointerEvent` that reaches a plain `addEventListener`, but it does not reliably trigger React's `onPointerMove` synthetic handler — confirmed empirically, not just assumed. The handler logic itself (relative pointer position → `setOffset` → CSS transform) was verified correct by code review, matching the confidence level already used elsewhere this session for untestable reduced-motion paths, but a real mouse/touch test on an actual device is the next real proof.
- ~~**★ The Profile avatar's live 3D canvas (`avatar-scene.tsx`) has an unresolved rendering gap...**~~ **RESOLVED BY REMOVAL** — the entire avatar system (decision 44) was deleted in the Synergy pivot (decision 45); this item and every other avatar-specific open item above no longer apply.
- **★ Real device confirmation still needed for the pivot's rebranded PWA**: the mobile-PWA gate itself (decision 29) was already confirmed working on a real phone before the pivot, but that was under the old "Jules" name/icon/colors — worth a quick real-device re-check that the installed PWA still shows correctly (new icon, new `#071620` theme color, "Synergy" as the app name) next time a phone is available, since the manifest/icon/theme-color values all changed in Phase 0.
- **★ RESOLVED — all five approved pivot phases (0–4) are now built and live-verified**: multi-club data model + 3-tier RBAC (decision 46), event registration + attendance streak (decision 47), universal leaderboard (decision 48), group/individual quiz + pooled/negative/time-based scoring (decision 49). See each phase's own Build Log entry and Known Open Items for what's still open *within* a shipped phase (e.g. club-scoping for read-only reports, real-device checks) — nothing from the original 11-point feedback remains unbuilt.
- **★ RESOLVED — the Event Report template now matches the real Adani University reference document** (decision 50), not the Phase 0 guess. A contrast check on the maroon-vs-danger-red pairing (flagged back in the original Phase 0 plan) has still not been run with a real contrast tool — low priority, the two hues are already deliberately far apart (~340° vs ~14°), but worth a quick pass if this ever needs formal AA/AAA sign-off.
- **★ RESOLVED — the Event Report `.docx` download now fills the user's real, literal reference file** (decision 72), not a from-scratch reconstruction. Photo/attachment image embedding into this real template is still an open follow-up — the user asked for it once before the pivot to the real-template approach ("so we need to add picture table border") but it wasn't re-confirmed as in-scope for the docxtemplater rewrite specifically, and needs its own approach (likely docxtemplater's official image module) rather than reusing the old `docx`-js `ImageRun` helpers that were removed along with the rest of the from-scratch generation code.
- **`lib/jules/*`, the `jules` git repo name, and other internal identifiers were deliberately NOT renamed** in the Phase 0 rebrand (decision 45) — only user-visible copy and the product name changed. Don't rename these as drive-by cleanup in a later phase; it's pure churn with no user-facing benefit and real regression risk (import paths, migration history references, etc.).
- **A cleanup pass removed the unused `docx` dependency (superseded by `docxtemplater`/`pizzip`, decision 72) and orphaned IDP payments/shipping scaffolding** (`lib/integrations/payments/`, `lib/integrations/shipping/` — leftover template boilerplate, never imported, no e-commerce concept exists in this app). Also fixed stale "Owner"-era wording in the Vault (the enforced role was already correctly `super_admin`/professor-only progression, just the comments/copy still said "Owner") and a real scoping gap in `editEventAction` where the app-layer club filter had never been updated when Professor became club-scoped (RLS backstopped it, but the code was stale).
- **★ RESOLVED — the real 9-club roster from decision 64 is now the basis for testing accounts too** (decision 82) — 40 disposable test accounts (20 students, 10 CM one-per-club, 10 super_admin) plus one real club-scoped Professor test account, all documented above under "Where things live."
- **The `.docx` download route's "Download Word report" link doesn't check club match before rendering** — it's shown to any logged-in admin (`getAdmin()` truthy), and clicking it now correctly 404s for a cross-club admin (RLS blocks the raw read post-decision-80), but the link itself doesn't know to hide/disable in advance. A minor UX polish item, not a security gap — the actual data access is already correctly blocked.
- **The Server Action body-size-limit fix (decision 83) was found from one real submission attempt with a handful of real phone photos** — not stress-tested at the true edge of the new 25MB ceiling (e.g. many large photos across all 4 attachment categories at once). Worth a heavier real-device retest if reports start failing again at a larger volume.
- **The audit trail (decision 82) only logs 5 new action types plus the original 4** — event delete, report edit/delete, and Live Round phase-advance are all deliberately NOT logged (the first two don't have UI/actions built yet at all; the third would be too frequent to be a useful signal). Extend the same `logAdminAction()` pattern to any new mutating Server Action added later, matching the precedent set in `grid/actions.ts`/`event-reports/actions.ts`/`gallery/actions.ts`/`live/actions.ts`.

---

## Build log

*Newest last. One entry per phase: what was built, what was verified, any deviation.*

### Phase 0 — Setup
- Cloned from the Website IDP (`github.com/PB515/IDP`) into its own folder, git history started fresh (this is a frozen, independent copy — improvements to the IDP later don't touch this site).
- Placed `docs/project-spec.md` (canonical V3+V4 merged spec), `docs/project-spec-v4-addendum.md` (historical, superseded), and `docs/starter-question-bank.csv` (real seed content).
- Filled this file's Decisions section directly from the spec's own §9 + earlier sections — nothing invented.

### Phase 1 — Foundation (security-first: schema → RLS → auth → features)
- **Skipped the planned doc-gen step** (docs 01–11) at the user's explicit interruption — see Resume note above. Went straight from spec to schema to code.
- **Schema + RLS + RPCs** (`0003`–`0009`): every sensitive write (Joule ledger, surge answers, admin roster, audit log) goes through a `SECURITY DEFINER` RPC, not a direct client insert — deny-by-default achieved by simply not granting `authenticated` any write policy on those tables. Found and fixed one real cross-user leak before it shipped (decision 15).
- **Auth**: student signup ("The Connection," connection-strength meter) with pre- and post-signup domain validation, student login, admin login (separate, role-checked), `proxy.ts` route protection, a `next=` redirect param so a QR deep-link survives a login detour, Supabase-native password reset.
- **Node**: dashboard (count-up hero numbers, tier badge + progress, Power Grid, recent activity), QR check-in (`/scan`, deep-link + manual fallback), Live Surge Mode (prefetch-once, per-question timer, lock/reveal), Surge Matrix (staggered reveal + personal result card for everyone, not just top 10), Catalyst Records (season-browsable, static), profile (view/edit, logout).
- **Admin**: role-gated shell, Grid Station (rotating token display + live metrics, Volunteer-scoped-to-one-event access), Surge Builder (CSV import with duplicate/range flagging + manual add + inline edit), System Ledger (month-by-type engagement chart + filterable event timeline), Student Data Vault (search, Force Reset via service-role + audit log, manual Joule adjustment, lock/unlock), Institution Settings (allowed domains, seasons, admin roster).
- **Verified**: `npm run typecheck`, `npm run lint`, `npm run build` (production), and `npm run test` (pre-existing template suite) all pass clean in `template/`. **Not verified**: anything requiring a live Postgres (no Docker here) — the cross-user-denial gate itself still needs to be run for real, not just reasoned through.
- Added `scripts/bootstrap-owner.ts` + `docs/runbooks/jules-setup.md` to make the "no real secrets yet" gap concrete and unblock the next session.
- Not yet done: run `migrate:up` against a real DB, regenerate `database.types.ts` for real, import the starter question bank into a live Surge, a real QR image, docs 01–11.

### Phase 1 — Live verification (git + hosted Supabase)
- **Git:** repo pushed to `git@github.com:PB515/Jules.git` (`main`). No SSH key existed on this machine — generated one, user added it to their GitHub account, verified with `ssh -T git@github.com` before pushing. Pre-commit hook (`lint:ai-tell`) caught 24 real em-dashes across the build; fixed all of them (not bypassed) before the commit landed.
- **Supabase:** user created a hosted project (Tokyo region — flagged, see Known open items) and supplied the new-format keys (`sb_publishable_...` / `sb_secret_...`, the current replacements for anon/service-role) plus a direct Postgres connection string. Wrote `template/.env.local` (gitignored).
- **Migrations:** all 9 applied via `npm run migrate:up` (run with cwd=`template/` so it picks up the right `.env.local` and resolves `db/migrations` directly). One real failure caught and fixed: `hmac(text, text, unknown) does not exist` — hosted Supabase's `pgcrypto` lives in the `extensions` schema, not `public`, and `hmac` needs `bytea` args, not `text`. Fixed in `0004_jules_functions.sql` (decision 16 updated).
- **First Owner bootstrapped** via `scripts/bootstrap-owner.ts` (email chosen by the user).
- **Live end-to-end verification in a real browser** (not just build/typecheck): Owner login at `/admin/login` → real redirect to `/admin` → System Ledger rendered with correct real empty states. Student signup at `/signup` (after discovering Supabase Auth rejects the placeholder `yourcollege.edu` domain as undeliverable — expected, since it's a fake domain; temporarily allow-listed `gmail.com` to prove the mechanism, then reverted it and deleted the test account) → `/dashboard` rendered with the real name, Ember tier, `0`/`0` Joules, and a correct `159`-day season countdown computed from the seeded placeholder calendar.
- **Found and fixed a real bug live**: `/admin/login` redirect-looped because it lived inside the guarded `app/admin/layout.tsx`. Restructured to `app/admin/(protected)/*` + unguarded `app/admin/login/` (decision 20). Re-verified `typecheck` + production `build` clean after the restructure.
- Not yet done: Vercel deployment (next per the user's stated order), real domain/season calendar, starter question bank import, real QR image, docs 01–11.

### Phase 1 — Vercel deployment
- **Vercel CLI** installed and authenticated (device-code flow) as the same account already holding the user's other projects. Created a new project (`jules`, `purven-s-projects` team) and linked `template/` to it.
- **Env vars** (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`) added to Production, Preview, and Development on Vercel; `NEXT_PUBLIC_SITE_URL` added to Production once the real domain was known.
- **Two real deployment-layer bugs found and fixed** (both invisible from build logs alone — see decisions 21/22): the team's default "Standard Protection" SSO wall was blocking every deployment including production (disabled via the API for this project); the project's Framework Preset was stuck on "Other" from being created empty (fixed to `nextjs` via the API + a forced redeploy) — until fixed, the app was serving as a static site from `public/`, 404ing on every real route despite the build itself succeeding.
- **Live-verified via curl** post-fix: `/` → `307` (proxy.ts redirect working), `/login` and `/admin/login` → `200` with correct `X-Nextjs-Prerender`/`X-Matched-Path` headers. Raw `curl` shows an empty body for `/login` because that page bails out to client-side-only rendering (`useSearchParams`) — expected, matches local dev behavior, confirmed not a regression.
- **Live URL:** https://jules-purven-s-projects.vercel.app
- Not yet done: custom domain, git-based auto-deploy (decision 23), real domain/season calendar, starter question bank import, real QR image, docs 01–11.

### Phase 1 — Git auto-deploy + starter content
- **Git → Vercel auto-deploy wired up**: `vercel git connect https://github.com/PB515/Jules.git --yes` (the plain `vercel git connect` failed first — it needs `template/` to be its own git repo, which it isn't; passing the URL explicitly worked around that). Then `rootDirectory: "template"` set via the same project-patch API used for decisions 21/22, since no CLI flag exposes it. **Proven for real**: the very next `git push` auto-triggered a production deploy that went `Building → Ready` on its own.
- **Starter question bank imported for real**: `docs/starter-question-bank.csv` (20 questions) loaded into a new "Season Opener Surge" (draft, `points_per_question: 20`, linked to the currently-active seeded season) via a one-off Node script that replicates `parseQuestionsCsv` + `importQuestionsAction`'s exact logic against the live Postgres directly. Verified by querying Postgres directly: 20/20 rows clean, 0 duplicates, 0 errors.

### Phase 1 — Live Round (Kahoot-style host-paced mode) + three real bugs found live
- **Scoped with the user first**, not built blind: the professor's Kahoot comparison bundled three distinct asks (team play, live projector scoreboard, offline-resilient quiz) — the third was already solved by the existing architecture; the first two needed real design decisions, so this went through `EnterPlanMode` and `AskUserQuestion` before any code, confirming: one-phone-per-team, new mode alongside the existing async Surge, reveal after every question, one device for host+display (decision 24).
- **Built**: migrations 0010 (`live_rounds`/`live_round_teams`/`live_round_answers` + RLS + `host_create_round`/`join_live_round`/`host_advance_round`/`submit_live_answer`/`live_round_scoreboard`, all mirroring the existing SECURITY DEFINER/deny-by-default patterns from 0006 exactly) and 0011 (`live_round_question`, since students have no direct SELECT on `questions`). Host screen (`/admin/live/new`, `/admin/live/[roundId]`) and team screens (`/live`, `/live/[roundId]`) wired to Supabase Realtime — first real use of it in this project, requiring the three new tables to be explicitly added to the `supabase_realtime` publication (easy to miss; RLS alone doesn't make a table broadcast).
- **`npm run typecheck`/`lint`/`build` all passed clean before live testing even started** — and then live testing found three real bugs none of those tools could see:
  1. **`live_round_question()` ambiguous-column bug** — the team screen went blank after every phase change. Root cause: `returns table (id uuid, ...)` implicitly declares `id` as a PL/pgSQL variable for the whole function body, making a later unqualified `where id = p_round_id` genuinely ambiguous against `live_rounds.id`.
  2. **Same bug, audited into existence in `start_surge()`** — meaning the *original* async Surge Mode has never been able to start a Surge since it was built. Fixed both in migration 0012, after auditing every other `returns table` function in the project for the same shape (all others were safe — see decision 25).
  3. **The critical one: `_bump_streak()`'s `trusted_write` escape hatch was never actually implemented** in the students self-update-restriction trigger (0005) — only `is_owner()` was checked. Every correct answer, through *any* path (QR scan, async Surge, Live Round), has silently failed to award Joules since the app was built, rolling back the whole transaction. Fixed in migration 0013.
- **Also fixed**: the team screen's scoreboard didn't fetch on a cold page load into `reveal`/`leaderboard`/`complete` (only on a live realtime transition) — decision 26.
- **Live-verified after all four fixes, in a real browser against the live DB**: full round lifecycle (lobby → question → reveal → leaderboard → next question → complete) with a real team, Realtime confirmed working in both directions (host and team screens react to phase changes with zero manual refresh), a correct answer awarding 20 J and updating `/dashboard` (Season Joules, tier, Power Grid, Recent Activity all correct), and the scoreboard aggregating and ranking correctly.
- **Also found and fixed a data-hygiene issue during testing** (not a code bug): a leftover `students` row had gotten attached to the Owner's own auth id from earlier manual testing without a full logout between roles (decision 28) — cleaned up; the live DB is back to just the Owner account + seed data (one Surge, 20 questions, seasons, institution settings).
- Not yet done: multi-team live testing (two simultaneous teams), a rendered QR image for the room code, real college domain/season calendar, docs 01–11.

### Phase 1 — General public site + mobile-PWA-only gate
- **Scoped with the user first**, same discipline as Live Round: `AskUserQuestion` settled PWA strictness (must be genuinely installed, not just a mobile browser tab), the blocked-on-desktop experience (a calm message, not a silent redirect), the calendar's data source (auto-fed from real `events`, not hand-managed), and organization (flat lists, no season/event grouping). Mid-plan, the user corrected a real misunderstanding — "Reports" as a file-upload page was the wrong shape entirely; it became "Afterglow," a written recap article linked to a real event, before any code existed for the wrong version — caught at the right time, in planning, not after building the wrong thing.
- **Built**: migration 0014 (`afterglow_posts`, `gallery_images` + RLS, the `gallery` Storage bucket + `storage.objects` policies — first use of Supabase Storage in this project — `public_events()` and `public_event_stats()` narrow public RPCs, never a broad public policy on `events` itself). New route group `app/(general)/*` (homepage, `/events`, `/get-app`, `/afterglow` + `/afterglow/[id]`, `/gallery`), admin authoring pages (`/admin/afterglow`, `/admin/gallery`), and the mobile-PWA-only gate (`proxy.ts` Layer 1 + `pwa-gate.tsx` Layer 2, decision 29). Root `app/page.tsx` deleted; `app/(general)/page.tsx` is the real homepage now.
- **`npm run typecheck`/`lint`/`build`/`test` all passed clean** — and live testing then found two more real bugs:
  1. A hydration mismatch in the Afterglow admin form's event dropdown (locale/timezone-dependent date formatting in a Client Component) — and the *identical* pre-existing bug, latent since the original build, in the Grid Station's `event-picker.tsx`. Both fixed with one shared `formatDateUTC()` utility (decision 30).
  2. (Caught and avoided, not shipped): the Afterglow list page's first draft used an embedded `events(name)` join, which would have silently returned null event names for anonymous public visitors since `events` has no public SELECT policy — fixed before it ever ran, by fetching `public_events()` separately and mapping client-side.
- **Live-verified end-to-end**: `curl` with real mobile vs. desktop `User-Agent` strings confirms Layer 1 works exactly as designed (desktop → `/mobile-required`, mobile → normal `/login` flow); all five General pages render real content publicly (no session); admin and General routes are completely unaffected by the gate. As the Owner, created one real event, wrote one real Afterglow post (with correct live-pulled attendance/Joules stats), and uploaded one real photo via a canvas-generated test image — all three appeared correctly on the public pages and the Storage URL served the image with the right content-type. All three test artifacts cleaned up afterward (this was fabricated test content, not real club data, unlike the Season Opener Surge).
- Not yet done: a real device test of Layer 2 (standalone-mode can't be forced via UA-spoofing in devtools), a real club moto/mission review, a rendered QR image for `/get-app`, docs 01–11.

### Phase 1 — Security retrospective on Goal 1 (harsh, by request)
- **The user explicitly asked for an adversarial re-read of the whole backend** — not a normal build/test pass, a deliberate hunt for gaps, security concerns, and inconsistencies before moving to Goal 2. Re-read every migration (0003–0014) and every admin Server Action fresh, with fresh skepticism rather than from memory.
- **Found a live, actively-exploitable CRITICAL vulnerability in production**: `qr_secret()` had no grant/revoke statements at all (every other internal helper in the same file has them) and was confirmed, live against the hosted project, callable by a fully anonymous caller — returning the actual HMAC secret behind QR token rotation. Fixed immediately (migration 0015), before continuing the rest of the audit, given the severity. Re-verified the legitimate call path (`current_qr_token`, the real Grid Station's own RPC) still works. Also rotated the secret's actual value afterward, since code-fixing the hole doesn't undo a possible prior exposure.
- **Found and fixed three more real issues** (migration 0016): `submit_live_answer()`'s missing current-question check (a team could bank a correct answer for an already-revealed earlier question during any later question's window — live-verified the exploit is now rejected and the legitimate flow still works); Force Reset's missing student-only check (an Owner could have silently reset another admin's password); and the RBAC helpers' (`is_admin`/`is_owner`/`is_officer_or_owner`/`is_volunteer_for_event`) own missing grant/revoke pair, fixed carefully to match `has_role.sql`'s exact pattern (revoke from `public`, re-grant to `authenticated`) since getting this wrong would have broken RLS across nearly every table — re-verified live (Vault, surges, institution_settings all still readable correctly for the Owner) after the change.
- **Disclosed, not fixed**: `events.geofence_lat/lng` over-exposure to any authenticated student (no client actually reads them; lower severity since geofence is already a soft flag); the in-memory rate limiter's known serverless limitation (already disclosed in its own docstring, re-flagged here since it's specifically relevant to this Vercel deployment).
- All fixes migrated to the live DB and re-verified with real RPC calls against production, not just reasoned through — same standard as every other fix this session.

### Phase 1 — Goal 2, first slice: launch splash + escalating quiz drama
- **Scoped with the user first**: the user's stated philosophy driving Goal 2 ("every vibe coder can build the backend — the frontend feel is what leaves an impact") led to two concrete asks rather than an abstract polish pass: a once-per-day PWA launch sequence, and an escalating drama curve across a quiz ending in a real "Winner Declaration." Went through `EnterPlanMode` + two rounds of `AskUserQuestion` before any code: splash frequency (once per day, not every open), synced haptic vibration on the finale (confirmed), and adding `motion` + `canvas-confetti` as new dependencies (confirmed) rather than hand-rolling either.
- **Built**: `lib/jules/sound.ts` + `lib/jules/haptics.ts` (silent-fail sound/vibration wrappers, since real audio files don't exist yet); `lib/components/launch-splash.tsx` (decision 33); `lib/components/winner-burst.tsx` + `lib/components/reveal-scoreboard.tsx` (decision 34), wired into `matrix-client.tsx` (rank-1 row only, ranks 10–2 untouched) and both Live Round client screens (`host-client.tsx`, `team-client.tsx` — the latter needed a new `totalQuestions` prop threaded from its page server component, since the team's own `live_round_question()` RPC doesn't return a total count).
- **`npm run typecheck`/`lint`/`build` all passed clean** (two `react-hooks/set-state-in-effect` lint errors surfaced and were fixed with the project's existing documented-disable pattern, same as `count-up.tsx`).
- **Live-verified in a real browser**, but not through the real gated routes directly — they sit behind the mobile-PWA gate (decision 29), which this session's preview browser's desktop User-Agent can't pass, and resizing the viewport doesn't change the UA (confirmed empirically). Instead, built a temporary throwaway route (`app/dev-preview/page.tsx`) with a one-line, temporary `proxy.ts` PUBLIC_PATHS entry, mounted the three new components directly with mock data, drove them via `preview_eval`, and confirmed: the splash's exact phase timing (jolt ~0–650ms, moto ~650–1950ms, return+fade ~1950–2250ms, matching the design), the once-per-day skip (a second launch with today's date already stored shows the dashboard content immediately, no overlay, no blank screen), the `WinnerBurst`'s confetti canvas + card render, and the `RevealScoreboard`'s worst-to-best reveal with rank 1 arriving last. Deleted the throwaway route and reverted the `proxy.ts` line immediately after, then re-ran `typecheck`/`lint`/`build` clean to confirm nothing was left behind.
- **One real, unrelated infra hiccup found and fixed**: running a production `npm run build` while a dev server was still pointed at the same `.next` directory corrupted the dev server's Turbopack manifests (`ENOENT`/`MODULE_NOT_FOUND` on next request) — fixed by clearing `.next` and restarting the dev server; not a code bug, just don't run `build` and `dev` against the same `.next` concurrently.
- Not yet done: real audio files (user sourcing separately), a reduced-motion direct-emulation check (verified by code review + the equivalent `seenToday` path instead), a real-phone confirmation of the actual gated screens (only the components in isolation were browser-verified), the rest of Goal 2, Goal 3, Goal 4.
- **This slice was committed, pushed, and confirmed live**: the new production deployment finished `Ready` and both `/` and `/admin/login` returned live `HTTP 200` on `jules-delta-nine.vercel.app` (one of this project's Vercel aliases). Also set up a full professor-demo kit at the user's request: 4 real test student accounts (`jules.demo.{volt,amp,ohm,watt}@gmail.com`), `gmail.com` temporarily added to `allowed_domains` for them, and a walkthrough script (laptop admin as host/projector, phones as teams, plus a "open the same link on a laptop" beat proving the phone-only rule live).

### Phase 1 — Goal 2, second slice: tier-up celebration, empty-state sweep, color fidelity, Shakti touches
- **Started from a real design-alignment check, not a build request**: after a professor demo, the user flagged a QR-vs-alphanumeric-code idea (discussed, no change made — the existing rotating token is already effectively a 10-char alphanumeric code, decision 7/16, just branded "QR"; kept as-is) and a real mobile-nav gap in the General site header (deferred on purpose — logged as a "do not build until a dedicated mobile-responsiveness pass starts" item, not fixed now). Then asked directly: are we following the moodboard, and are we leaving the Joule/Shakti concept behind? Verified against `docs/project-spec.md` §2/§3 directly rather than just reassuring (see Current status) — confirmed both are intact, and that spec §6's tier-up feature and Phase-4's two "deeper Shakti" ideas were simply never built yet, not abandoned.
- **Research first**: two parallel Explore-agent audits (empty/loading-state coverage across every screen; the tier-color system and whether a light/dark toggle exists anywhere) before any planning, per the plan-mode workflow — found zero light/dark toggle footprint anywhere, confirming "theme transitions" could only sensibly mean the tier system, and a full inventory of ~9 ad hoc empty-state duplicates plus a couple of genuine gaps. Confirmed via `AskUserQuestion` before finalizing the plan: build the tier-up celebration, do the full empty-state sweep, and (after the user asked for "all 3" on moodboard scope) also do a hardcoded-hex audit, pull the two Phase-4 Shakti touches forward, and give Live Round its own color identity.
- **Built**: decisions 35–39 (see Decisions section) — the two hardcoded-hex fixes, `lib/components/tier-up-celebration.tsx`, "the first spark" naming + the rotating Stuti verse, the empty-state sweep across ~11 files, and Live Round's kumkum chrome. `lib/components/winner-burst.tsx` was generalized (optional `colors`/`sound`/`vibration` props) rather than duplicated, so the quiz Winner Declaration and the tier-up celebration share one confetti/sound/haptics implementation.
- **`npm run typecheck`/`lint`/`build` all passed clean** after every step (one type-inference fix needed: `useState(site.tagline)` was inferring a too-narrow literal type once a second, differently-typed string could be passed in, fixed with an explicit `useState<string>`).
- **Live-verified more directly than the previous slice**, since admin routes aren't behind the mobile-PWA gate (decision 29): logged into the real Reactor Command Center and screenshotted the migrated empty states on System Ledger ("No engagement data yet," "No events in this filter") and Student Data Vault ("No matches" on a real search), and created a real Live Round to screenshot the kumkum room-code border and eyebrow text live, then deleted that test round afterward. The tier-up celebration, splash rotation, and remaining components were verified via the same temporary-throwaway-route technique as the previous slice (mock tier transitions across three scenarios: promotion fires with the right tier's confetti color, first-ever visit stays silent, same-tier reload stays silent — all three confirmed correct).
- **One real testing mistake made and caught**: `document.querySelector('button')` on an authenticated admin page matched the sidebar's "Log out" button instead of a form's submit button (it appears earlier in DOM order), causing two confusing false "session expired" moments before being traced via server logs showing `adminLogoutAction()` firing instead of the expected create-round action — not an app bug, a test-harness mistake, logged in Known open items so it isn't repeated.
- Not yet done: real audio files (`tier-up.mp3` now also needed), a real-phone confirmation of the student-side Live Round/Surge Matrix screens, the rest of Goal 2 (broader color-identity work beyond Live Round, a fuller consistency pass), Goal 3, Goal 4, the deferred mobile-responsiveness pass (General site header nav).

### Phase 1 — Goal 2, third slice: active-nav identity + final color consistency
- **Scoped from a fresh audit, not assumption**: a research-agent color-usage sweep across every Node/Admin/General screen (the user asked to close out "give major features their own identity" and the "fuller consistency pass" left open from the previous slice) found the palette already applied consistently almost everywhere — the one real, concrete gap was that neither the admin sidebar nor the public site's nav had any active-route highlighting at all. Confirmed with the user before building: reuse kumkum's existing meaning for "you are here," not a new accent color per feature area (keeps the palette disciplined, doesn't dilute what gold/kumkum already mean everywhere else).
- **Built**: decision 40 — `app/admin/(protected)/admin-nav-client.tsx` (new) + a trimmed `app/admin/(protected)/layout.tsx`; `app/(general)/layout.tsx` converted to a Client Component for `usePathname()`; the Vault search-box border fix; two documentation-only comments on the audit's confirmed-deliberate findings (`engagement-chart.tsx`, `winner-burst.tsx`).
- **A real runtime bug found and fixed via live verification, invisible to typecheck/lint/build**: the first version of the admin sidebar split passed pre-built nav items — including Lucide icon component references — as a prop from the Server Component layout to a new Client Component. Next.js threw "Only plain objects can be passed to Client Components from Server Components" the moment the page actually loaded in a browser; TypeScript has no concept of this constraint, so `LucideIcon` typechecked fine as a prop type and the build was clean. Fixed by having the Client Component own the full nav array (icons included) itself and receive only a plain `role: string` down from the server, never a component reference across the boundary.
- **Live-verified directly on the real routes** (admin routes aren't behind the mobile-PWA gate, decision 29 — no throwaway-route workaround needed): logged in as Owner, clicked through the sidebar and confirmed via computed styles (not just a screenshot) that exactly the current section renders `text-accent`/`border-accent` (`rgb(227, 66, 52)` = `#E34234`, kumkum) while every other item stays neutral; same check on the public site's nav (`/events` correctly kumkum-highlighted, others neutral). Confirmed the Vault search box now uses `border-border` via its rendered className.
- **One more testing-methodology note**: a screenshot briefly showed "Live Round" looking highlighted after a click — turned out to be a lingering `:hover` artifact from the click position in the headless browser (confirmed via computed style: its text color was `--foreground`, not `--accent`, and its class list had no `text-accent`), not a real bug. Read computed styles/classNames directly rather than trusting a single screenshot when a highlight looks ambiguous.
- Not yet done: real audio files, a real-phone confirmation of the student-side Live Round/Surge Matrix screens, Goal 3, Goal 4, the deferred mobile-responsiveness pass.

### Phase 1 — Goal 2, asset-generation slice: icon refresh + Gallery placeholder
- **Scoped from the user's own sequencing plan for the rest of Goal 2** (assets → mobile-responsiveness pass → final polish → next goal): delivered an audio spec sheet in chat (type/length/style/trigger for all 6 sound files), confirmed the moto/mission copy stays as-is (already flagged), and resolved the static-art question directly — no Midjourney/GPT-Image tool exists in this session, so static art is hand-coded SVG, confirmed with the user rather than assumed or faked.
- **Built** (decision 41): `public/icons/icon.svg` — replaced the placeholder lightning bolt with an atom-orbit + spark mark (feeds PWA manifest icons, favicon, and the launch splash's logo beat); `lib/components/gallery-placeholder.tsx` — a hand-coded abstract "energy grid" illustration for the public Gallery's empty state, deliberately not a mockup pretending to be a real event photo, wired into `app/(general)/gallery/page.tsx` in place of the generic `ImageIcon`.
- **`npm run typecheck`/`lint`/`build` all passed clean.**
- **Live-verified directly** (General routes aren't behind the mobile-PWA gate): confirmed the new icon renders correctly at normal size and the Gallery placeholder illustration displays with the right "Real photos coming soon" copy and on-brand colors; also incidentally re-confirmed the previous slice's active-nav highlighting is still working ("Gallery" correctly kumkum-lit in the public nav). A true small-scale/favicon check of the new icon wasn't possible with the available browser tooling this session (see Known open items) — the design deliberately uses thick, high-contrast strokes to survive downscaling regardless.
- Not yet done: real photos to replace the Gallery placeholder, a favicon-scale spot-check of the new icon, the audio files themselves, the deferred mobile-responsiveness pass, Goal 3, Goal 4.

### Phase 1 — Goal 2, interactive homepage hero
- **Scoped through direct back-and-forth, not built from an open-ended idea**: the user shared a reference site's animated hero (rotating wireframe shape + connected floating particle background) and asked whether the same treatment could apply site-wide. Talked through it area by area before building anything — agreed it belongs on the public homepage only (the mobile PWA is deliberately distraction-free for quiz integrity per the professor's own requirement, decision 29; admin needs to stay fast for daily operational use, not a landing-page moment) — then confirmed the specifics: idle drift as the default, but pointer/touch-reactive specifically in the hero, and the particle background running as a continuous full-page layer, not just behind the hero.
- **Built** (decision 42): `lib/components/energy-field.tsx` (full-page ambient particle network, deterministic, pure CSS drift, reusing the exact visual language already established by `gallery-placeholder.tsx`) and `lib/components/hero-atom.tsx` (a bigger, animated version of the new app icon's atom-orbit mark, decision 41, with auto-rotation and pointer/touch parallax), wired only into `app/(general)/page.tsx`.
- **A real readability bug found and fixed via live screenshot, not assumed away**: the first pass set the hero atom's opacity high enough (60%) that it visually collided with the description paragraph text sitting on top of it — dropped to 10% opacity, confirmed via a second screenshot that it now reads as a faint watermark, not a competing foreground element.
- **A real, instructive `motion/react` bug found and fixed**: `animate`/`style={{x, y}}` on nested SVG elements silently did nothing (no error, clean typecheck/build, but `getComputedStyle` showed no transform ever applied) — switched both the rotation and the pointer parallax to plain CSS/React-state techniques already proven reliable elsewhere in this codebase, rather than continuing to debug the library's SVG-specific behavior.
- **A real Tailwind arbitrary-value bug found and fixed**: `opacity-[0.12]` was silently not generating any CSS rule at all (`getComputedStyle` confirmed the parent's opacity stayed `1` despite the class being present in the DOM) — switched to the standard `opacity-10` utility, which worked immediately. Not yet root-caused why the arbitrary-bracket syntax failed specifically in this project's Tailwind v4 setup; flagged as worth avoiding (prefer standard utilities) rather than chasing further right now.
- **A real PWA-cache trap hit repeatedly during this exact verification pass, now documented for next time**: the app's own service worker (`public/sw.js`) uses a cache-first strategy that pre-caches `/` on install — every code edit needed the service worker unregistered AND its Cache Storage cleared before a reload would show current content, otherwise the browser silently kept serving a stale snapshot from before the edit. This is also a latent concern for real users of the installed PWA (a stale shell could persist indefinitely without a cache-version bump) — noted, not fixed, since it's out of scope for this feature.
- **A real testing-tooling limitation surfaced and disclosed rather than glossed over**: a script-dispatched synthetic `PointerEvent` reaches a plain `addEventListener` (confirmed) but does not reliably trigger React's `onPointerMove` synthetic handling in this session's browser tooling, so the hero's pointer/touch parallax couldn't be end-to-end verified live — the handler logic was instead verified correct by careful code review.
- `npm run typecheck`/`lint`/`build` all passed clean after every fix.
- Not yet done: a real mouse/touch verification of the hero parallax, the deferred mobile-responsiveness pass, Goal 3, Goal 4, and the still-outstanding asset items (real photos, audio files, moto review).

### Phase 1 — Real-device verification (user-run) + QR images
- **The user personally ran two real-device tests this phase, closing out the two biggest remaining unknowns from earlier sessions**: the mobile-PWA gate installed and worked correctly on a real phone, while a real laptop browser was correctly blocked from the student experience (confirmed explicitly, not assumed); and a full 20-question Live Round was completed live across two separate real devices (laptop hosting, phone playing as a team) over the actual production deployment — the strongest real-world proof this feature has had, beyond same-machine browser-tab testing.
- **Closed the QR-image gap** (decision 43): added `react-qr-code`, confirmed with the user first. Wired a real scannable QR into the Grid Station, the Live Round host lobby (plus a `?code=` query-param prefill on the join page so scanning actually skips typing the code), and `/get-app`. Deliberately kept the QR black-on-white rather than theme-colored, for real-world scan reliability.
- **`npm run typecheck`/`lint`/`build` all passed clean.**
- **Live-verified directly**: confirmed `react-qr-code` renders correctly as a Server Component (`/get-app` still prerenders statically) and that its SVG path contains real encoded module geometry, not an empty render. The Grid Station and Live Round host lobby QR call sites use the identical library call and weren't separately re-verified with live event/round data, since duplicating the same already-proven library check wasn't worth the setup cost this pass.
- Not yet done: real photos for the Gallery, the six real audio files, a real club moto/mission review, the deferred mobile-responsiveness pass, Goal 3, Goal 4.

### Phase 1 — Goal 2, solarpunk avatar expansion: design system, wireframes, Stage 1 build
- **Scoped through an extended, deliberately visual planning pass before any code**: the user pushed for a "full frontend revamp" beyond the original two Goal-2 ideas, wanting real impact rather than incremental polish. Rather than reason about this in text, built the plan visually: an Artifact-based design system (frozen tokens honored as-is, two new proposed cosmetic-rarity colors, a resolved Space Grotesk + DM Sans type pairing via `next/font` self-hosting, real computed contrast ratios rather than assumed ones, an imagery/video treatment section scoping video to exactly one placement), then a second Artifact with wireframes for Profile (the avatar stage + "Your Collection" tech-tree), Dashboard (a small teaser card), the quiz-answer button redesign, and the cosmetic-unlock celebration storyboard — all reviewed and adjusted with the user before Stage 1 implementation began.
- **A large asset-generation prompt set was also produced at the user's explicit request** (`docs/prompts/asset-generation-prompts.md`, 93 prompts across 13 groups) for the user to run through their own image/video tools (Higgsfield, Gemini) — including, per an explicit override of `docs/prompts/image-gen.md`'s own "icons stay icons, logo must be vector" gate, real prompts for the brand/icon system too, clearly logged as a deliberate exception rather than a silent drift. Two format fixes were needed after real generation failures: every prompt had to be fully self-contained (no "same as above" shorthand, which one tool misread as an image-edit instruction) and every batch needed an explicit "generate N separate standalone images, not a collage" instruction for a different tool.
- **Built Stage 1** (decision 44): migration 0017 applied live with diversified seed colors (two new tokens, `--cosmetic-rare`/`--cosmetic-epic`, added to `globals.css`), `avatar-scene.tsx` (R3F placeholder scene with every optimization lever from the plan), `avatar-backdrop.tsx`, `collection-tree.tsx` (GSAP unlock reveal), `dashboard-teaser.tsx` (GSAP ScrollTrigger reveal), wired into `app/(node)/profile/page.tsx` and `app/(node)/dashboard/page.tsx`.
- **Two real bugs found only by actually building and running the code, both fixed**: `next/dynamic(..., {ssr:false})` rejected from a Server Component in Next 16 (fixed with `avatar-scene-loader.tsx`, a one-line Client Component boundary); and R3F's own resize detection never initializing the canvas at all in this session's specific browser tooling, root-caused all the way into R3F's source and fixed with its documented `resize={{ polyfill: ResizeObserver }}` escape hatch — though this session's tooling couldn't independently confirm the fix visually (see Known open items). `npm run typecheck`/`lint`/`build` all pass clean; confirmed via `grep` that the R3F/Three.js bundle is scoped to `/profile` only.
- Not yet done: a real device check of the live 3D canvas's actual size/aspect ratio, sourcing the real 3D character model (Stage 2), the Afterglow video schema addition, the season-recap shareable card (flagged as a new feature, not scoped), the deferred mobile-responsiveness pass, Goal 3, Goal 4.

### Platform pivot, Phase 0 — Remove the avatar system, rebrand to Synergy
- **Triggered by a live professor review** of the "Jules" build (well received), which surfaced 11 pieces of feedback amounting to a full platform pivot (decision 45): from a single energy-club app to the shared engagement platform for all 12-13 clubs in the program. Name (Synergy) and color direction (Adani-inspired direction "A") were resolved through direct back-and-forth with the user, not invented, before any code — same discipline as every other major direction change this project. A full phased plan (Phase 0 here, then multi-club data model + RBAC, registration/streak, leaderboard, quiz scoring) went through `EnterPlanMode`/`ExitPlanMode` and was approved before starting.
- **Removed entirely**: the 3D avatar/cosmetic-unlock system from decision 44 — `lib/components/avatar-3d/*` (6 files), `lib/jules/avatar-items.ts`, `lib/components/demo-controls.tsx`; `three`/`@react-three/fiber`/`@react-three/drei`/`gsap`/`@gsap/react` uninstalled (57 packages removed, confirmed via `grep` first that nothing else imports them). Migration 0018 drops `avatar_items`/`avatar_items_catalog()`, undoing 0017 cleanly (full symmetric `migrate:down`). `profile/page.tsx` reverted to a plain listing (name/phone form, stats, tier, email, logout); `dashboard/page.tsx` reverted to the plain season card + `TierUpCelebration`, dropping `DashboardTeaser`/`DemoControls`.
- **Rebranded**: `lib/site.ts` (name/tagline/description → Synergy, multi-club framing); `app/globals.css` token *values* swapped under the same variable *names* already used everywhere (`--gold` → X11 Maroon `#BD3861`, `--accent` → a deliberately distinct warm orange-red `#E0521F` so brand and danger colors never collide, new `--background`/`--card`/`--border` family off a dark Honolulu-Blue-adjacent base) — a value swap, not a rearchitecture, confirmed via `grep` this required zero component-file changes; manifest/layout theme-color and the app icon (`public/icons/icon.svg`) updated to match.
- **Afterglow → Event Report** (per explicit professor naming): migration 0019 renames `afterglow_posts` → `event_reports`, `body` → `summary`, adds `highlights text[]`; both route groups renamed (`app/(general)/afterglow` → `event-reports`, `app/admin/(protected)/afterglow` → `event-reports`) via `git mv`; nav labels updated in both the admin sidebar and the public site nav. The authoring form (`new/form.tsx`) rewritten from one freeform textarea into a template — event dropdown (auto-pulls attendance/Joules stats client-side via `public_event_stats()`), Title, a repeatable Highlights list (add/remove), one Summary paragraph — matching the professor's own framing, "fill in the gaps, the report is ready."
- **A thorough "Jules"-copy sweep across user-visible text**, done incrementally as pages were touched then closed out with a final exhaustive `grep`: fixed the homepage's separately-hardcoded "The idea" section copy (not sourced from `lib/site.ts`, so the earlier site.ts edit alone had missed it), `get-app` page/install button, admin layout eyebrow, admin login eyebrow, forgot-password copy, `pwa-required.tsx`, and finally student login's "New to Jules?" line — 7 files total. Internal-only identifiers (`lib/jules/*` folder name, a code comment referencing "Jules-specific" icon needs, the `jules` git repo itself) were deliberately left alone — cosmetic-only churn with no user-facing benefit, flagged in Known open items so it isn't attempted later as drive-by cleanup.
- **A real bug found via `ExitPlanMode`'s returned plan text, not code**: the plan-mode file's rewrite had left ~190 lines of the *old*, now-contradictory avatar-build plan appended after the new plan's ending, because the rewriting `Edit` call's `old_string` only matched the file's opening section. Caught by re-reading the approved-plan text rather than trusting the edit, and fixed with a second `Edit` truncating the stale tail — a process lesson: always verify a large file-rewrite actually replaced the old content to its true end, not just prepended new content before it.
- **`npm run typecheck`/`lint`/`build` all passed clean** (a stale `.next` cache briefly produced phantom errors referencing the deleted `afterglow` routes after the `git mv` — fixed with `rm -rf .next`, a now-familiar gotcha in this project whenever `build` and a running `dev` server share the same `.next` directory).
- **Live-verified in a real browser**: the public `/event-reports` list (correct empty state), the admin `/admin/event-reports` list and its templated `/admin/event-reports/new` form (event dropdown, Title, repeatable Highlights, Summary, all present and rendering), and — via `getComputedStyle` on `document.documentElement` — confirmed the new token values are actually live in the rendered page (`--gold: #bd3861`, `--accent: #e0521f`, `--background: #071620`), not a stale cache artifact. `/profile` and `/dashboard` weren't directly loaded in this pass — they sit behind the mobile-PWA gate's Layer 1 (decision 29), which this session's desktop-UA browser tooling can't pass (same known, previously-disclosed limitation as every earlier Goal-2 slice) — but both were reverted to known-good pre-avatar code structure and share the exact same verified token system.
- Not yet done: Phases 1–4 of the pivot (multi-club data model + 3-tier RBAC, event registration + attendance streak, universal leaderboard, group/individual quiz + pooled/negative/time-based scoring) — all approved, none started. Also still open from before the pivot: real photos for the Gallery, the six real audio files, a real club moto/mission review, the deferred mobile-responsiveness pass, a real-device recheck of the rebranded PWA install (new icon/name/theme-color).

### Platform pivot, Phase 1 — Multi-club data model + 3-tier RBAC
- **Checked the live data before writing any SQL, rather than assuming**: queried production directly and found exactly 3 admin rows (all `owner`), 0 events, 3 surges — meaning the role-value backfill was low-risk (no real `officer`/`volunteer` data to reconcile) but the `club_id` backfill on surges was real and needed a deterministic placeholder to backfill onto.
- **Built** (decision 46): migration 0020 — a new `clubs` table (seeded with one ★ placeholder "General Club", same convention as decisions 9/10); `club_id` added to `events` and `surges` (backfilled, then `NOT NULL`); `admins.club_id` added and `admins.role` collapsed from `owner`/`officer`/`volunteer` to `professor`/`committee_member` (Volunteer dropped entirely — a deliberate simplification, the professor named exactly 3 tiers). New RBAC helpers `is_professor()`/`is_committee_member()`/`is_committee_member_or_professor()` replace `is_owner()`/`is_officer_or_owner()`/`is_volunteer_for_event()`; new scoping primitives `can_manage_club()`/`can_manage_event()`/`can_manage_surge()` back every club-owned write policy. Every policy and RPC that used the old functions was re-audited, not just renamed — events/surges/questions writes are now club-scoped, `log_csv_import`/`host_create_round`/`host_advance_round` are scoped via their surge's club, and `admin_adjust_joules` stays Professor-only (the one power nothing else gets). New admin UI: a "Clubs" section in Institution Settings (Professor-only, `clubs-section.tsx`), a club picker on both the New Surge and New Event forms (converted from bare Client Components to Server-Component-fetches-clubs + Client-Component-form, matching the Event Report pattern from Phase 0), and the Grid Station/Surges list pages now filter to a Committee Member's own club at the query level.
- **Two real bugs caught by the migration tool's own transactional rollback, not by review beforehand**: (1) the role-value `UPDATE`s ran before the old check constraint was dropped, so writing `'professor'` violated the still-live `('owner','officer','volunteer')` constraint — fixed by reordering the drop before the writes. (2) `CREATE OR REPLACE FUNCTION` refused to rename `admin_create_admin`/`admin_set_role`'s `p_volunteer_event_id` parameter to `p_club_id` even with an identical type signature ("cannot change name of input parameter") — fixed with an explicit `DROP FUNCTION` first; the migration's `down` path was checked afterward and needed a fix too (it only dropped these two functions, never recreated the original-signature versions — added them back so a rollback wouldn't leave the app broken).
- **`npm run typecheck`/`lint`/`build` all passed clean.**
- **Live-verified the actual security boundary, not just the UI rendering**: applied migration 0020 to production, confirmed via direct SQL that all 3 admins became `professor`, the placeholder club was seeded, and all 3 existing surges backfilled to it. Then ran a real cross-club RLS test using `SET LOCAL request.jwt.claims`/`SET LOCAL ROLE authenticated` (the same mechanism Supabase's own PostgREST layer uses per request) — a temporary Committee Member (scoped to the placeholder club) correctly had 0 rows affected when trying to rename a surge belonging to a freshly-created second test club, correctly succeeded renaming their own club's surge, `can_manage_surge()` returned `false`/`true` for the two cases respectively, a Professor's rename of the other club's surge correctly succeeded (platform-wide), and `log_csv_import` correctly raised "not authorized" for a cross-club surge id. Also logged into the real Reactor Command Center as a Professor and confirmed Institution Settings renders the new Clubs section and shows all admins as "Professor" in the roster. All test fixtures (the second club, its test surge, the temporary admin row) were cleaned up immediately after, confirmed via a follow-up query that the live DB is back to exactly its pre-test state.
- Not yet done: Pivot Phases 2–4 (event registration + attendance streak, universal leaderboard, group/individual quiz + pooled/negative/time-based scoring), the real 12-13 club roster (only the placeholder exists), club-scoping for read-only reports (System Ledger, full Joule ledger — flagged as a deliberate follow-up, not an oversight, in Known Open Items).

### Platform pivot, Phase 2 — Event registration + attendance-based streak
- **Built** (decision 47): migration 0021 — a new `event_registrations` table; `register_for_event()`/`unregister_from_event()` RPCs (reject surge-type events, already-ended events, double-registration; cancellation is blocked once attendance is stamped); `redeem_event_scan()` extended to stamp a matching registration's `attended_at` alongside its existing Joule credit, with walk-in scans (no prior registration) unaffected. The old daily-activity streak (`_bump_streak()`, decision 13) is removed entirely, along with its call sites in `submit_surge_answer`/`submit_live_answer` — quizzes no longer move the streak. A new computed function, `student_attendance_streak()`, replaces it: same "computed, never stored" philosophy as tiers/Joule totals (decision 11), walking a student's concluded registrations backward from most-recent and counting consecutive attended ones until the first no-show. The now-dead `students.streak_days`/`last_active_date` columns are dropped, and the field is renamed `streak_days` → `streak` everywhere (`my_totals`, `admin_student_totals`, and every frontend display — Profile, Dashboard, the Vault) since it no longer means "days," it means "consecutive attended events." Dashboard gained two new sections: "Upcoming events" (Register/Cancel buttons, a small client component using the same `useTransition`/server-action pattern already established in the Surge Builder) and "My registered events" (Registered/Attended/Missed status per row).
- **Two real Postgres migration bugs, both the same class already hit in decision 46's migration**: `CREATE OR REPLACE FUNCTION` refused to change `my_totals()`/`admin_student_totals()`'s return-table column name (`streak_days` → `streak`) with "cannot change return type of existing function" — fixed with an explicit `DROP FUNCTION` first, in both the `up` and `down` directions (the `down` path needed the same fix, not just the `up`). Worth remembering as a pattern now, not a one-off: this project's migrations have hit this exact Postgres restriction twice in two consecutive phases, for two different kinds of signature change (parameter rename in Phase 1, return-column rename in Phase 2) — any future `CREATE OR REPLACE FUNCTION` that changes anything about a function's declared shape, not just its body, should be treated as a likely `DROP FUNCTION` case from the start.
- **`npm run typecheck`/`lint`/`build` all passed clean** — one real lint catch: two inline `Date.now()` calls inside the Dashboard's render body (computing "is this event upcoming" and "has this registration concluded") tripped the project's own `react-hooks/purity` rule. Fixed with a standalone `hasConcluded()` helper, following the exact same pattern this same file's pre-existing `daysUntil()` helper already used for the season countdown.
- **Live-verified the actual logic directly against production**, not the UI (the Node routes sit behind the mobile-PWA gate, decision 29, same limitation as every earlier Node-route slice this session's desktop browser tooling hits). Applied migration 0021, confirmed via direct query that `streak_days`/`last_active_date` are gone and `event_registrations` has the right shape. Then, using `SET LOCAL request.jwt.claims`/`SET LOCAL ROLE authenticated` (same technique as Phase 1's RLS proof): confirmed `register_for_event` succeeds for a live not-yet-ended event and correctly rejects a second registration and an already-ended one; confirmed `redeem_event_scan` credits Joules and stamps `attended_at` on the matching registration in the same call; confirmed `unregister_from_event` deletes an unattended registration but leaves an attended one alone. Separately proved the streak formula itself with a constructed sequence of four/six past registrations (attend, attend, **miss**, attend[, attend, attend]) — computed streak was `1` right after the miss (correctly discarding the two attendances before it) and `3` once two more consecutive attendances were layered on top, exactly the "reset to 0, recount from the next success" behavior the plan called for. All test fixtures (events, registrations) were created and cleaned up directly against the live DB, confirmed clean afterward with a follow-up query.
- Not yet done: Pivot Phases 3–4 (universal leaderboard, group/individual quiz + pooled/negative/time-based scoring), a real phone/PWA check of the new Dashboard sections, an admin-facing "who registered/attended" view per event (not scoped in Phase 2), the real 12-13 club roster, club-scoping for read-only reports.

### Platform pivot, Phase 3 — Universal leaderboard
- **Started from a real finding, not an assumption**: read `season_leaderboard()`'s actual SQL (0008) before touching anything and found it was never capped at the old top-10 the professor was reacting against — but it did have a `having ... > 0` filter silently excluding every zero-Joule student, which is the literal gap "even if there are 400 then all 400 will be on points" calls out. Fixed the existing function in place (decision 48) rather than adding a separate `platform_leaderboard()` RPC, since Joules were never club-scoped (decision 46) — this function already was the platform-wide leaderboard, just with a filtering bug.
- **Built**: migration 0022 drops the `having` filter and adds real pagination (`p_limit`/`p_offset`, default page size 50), with `total_count` riding along via `count(*) over ()` — computed before `LIMIT`/`OFFSET` apply in SQL's logical processing order, so it's always the true total regardless of which page was requested. Catalyst Records (the existing season-browsable screen — reused per the plan's own instruction, no new screen built) gained a `page` query param, Previous/Next links, and an "X–Y of N students" caption; the empty state changed from "Nobody had earned Joules" to "No students yet," since the list can now only be empty if there are literally zero students in the system.
- **A third instance of the same Postgres restriction hit in decisions 46 and 47's migrations**: adding new trailing parameters changes a function's identity for Postgres's overload resolution, so `CREATE OR REPLACE FUNCTION` would have created a second, separately-callable overload rather than truly replacing the old one — fixed with an explicit `DROP FUNCTION public.season_leaderboard(uuid)` first. Three consecutive phases hitting a variant of this same rule (parameter rename, return-column rename, parameter count change) is now treated as a standing pattern, not something to rediscover each time: any `CREATE OR REPLACE FUNCTION` that changes a function's declared shape gets an explicit `DROP FUNCTION` first, by default.
- **`npm run typecheck`/`lint`/`build` all passed clean.**
- **Live-verified directly against production**: applied migration 0022, then queried the real (small) live roster's active season and confirmed all 4 students appear ranked — including the 3 with zero Joules, correctly tied at rank 2 (standard SQL `rank()` tie semantics). Confirmed pagination windows correctly across two pages (limit 2/offset 0, then limit 2/offset 2) with `total_count` staying `4` on every row of both pages — proof the window function computes over the whole result set before `LIMIT` trims it. The `/catalyst` page itself wasn't directly browser-loaded (same mobile-PWA-gate limitation as decision 47), but the RPC's actual correctness — the real risk in this phase — was proven at the database layer directly, the stronger bar of proof for this kind of feature.
- Not yet done: Pivot Phase 4 (group/individual quiz registration + pooled/negative/time-based scoring — the last approved pivot phase), a real phone/PWA check of the Catalyst Records pagination UI (this environment's 4-student roster can't exercise a genuinely multi-page scenario), an admin-facing "who registered/attended" view per event, the real 12-13 club roster, club-scoping for read-only reports.

### Platform pivot, Phase 4 — Group/individual quiz registration + pooled scoring + negative/time-based marking
- **Four explicit design questions from the plan were resolved with the user via `AskUserQuestion` before any code**, all "Recommended" picked: pooled points that don't divide evenly round down with the leftover going one point at a time to the fastest answerers; the time bonus is a continuous formula, not tiered brackets; negative marking is a flat, per-Surge-configurable penalty (0 = off); Committee Members can configure all of it themselves, same access as any other Surge.
- **Scope confirmed against the plan's own contrast with Live Round**: group registration + pooled scoring apply only to async Surge Mode; Live Round keeps its existing real-time "one device per team" model (decision 24) untouched, picking up only the new scoring formula.
- **Built** (decision 49): migration 0023 — `quiz_groups`/`quiz_group_members` (the latter denormalizes `surge_id`, giving a real `unique(surge_id, student_id)` constraint that closes a race condition an RPC-level check alone couldn't fully prevent); two new `surges` columns (`participation_points_per_question`, `negative_points_per_wrong_answer`); a shared `surge_answer_points()` formula function used by both the instant per-answer preview and the finalize-time aggregation, so the math lives in exactly one place; `create_quiz_group()`/`join_quiz_group()`/`leave_quiz_group()` (locked to `status = 'draft'`, matching the existing prefetch-once architecture, decision 8); and `complete_surge()`, the new finalization RPC that replaces a raw status update for the "mark complete" transition — computes each group's participation/earned pools from every *registered* member (not just those who answered), splits both via the confirmed floor-plus-remainder-to-fastest rule, and credits the ledger atomically under a row lock. `submit_surge_answer()` no longer writes `joule_transactions` directly — Joule credit for async Surge Mode is now fully deferred to `complete_surge()`, though the quiz UI still gets an instant correct/incorrect + points preview. `joule_transactions.type` value `'surge_correct_answer'` is renamed to `'surge_earned'` (10 existing live rows converted) and a new `'surge_participation'` type added; `surge_leaderboard()` updated to sum both.
- **The same Postgres migration-ordering bug from decision 46's migration, hit again**: the data-migration `UPDATE` renaming existing rows to `'surge_earned'` ran before the old check constraint was dropped, violating the still-live constraint. Second occurrence of this exact mistake in this project — now a standing checklist item for any future enum-value-renaming migration: drop the constraint, then write the data, then add the new constraint, never the reverse.
- **`npm run typecheck`/`lint`/`build` all passed clean.**
- **Verified with an independently-reimplemented formula, not hand arithmetic**: the same `surge_answer_points()` math was rewritten in a standalone JS test script, which drove the real RPCs for a constructed 4-student scenario (a 3-person group with mixed correct/wrong/timed answers and one member who skipped a question, plus one solo player), called `complete_surge()`, and diffed the actual database output against the JS-computed expectation — all 8 resulting ledger rows matched exactly, including the correct fastest-two-get-the-remainder split and the solo player receiving their full undivided pool. Separately confirmed: zero `joule_transactions` exist before `complete_surge()` runs (proving deferred crediting); every group-formation guard (one group per student per surge, no forming/joining/leaving once live); and Live Round's `submit_live_answer` now correctly inserts a real negative ledger row for a wrong answer under negative marking, where before it credited nothing at all. All test fixtures were created and fully cleaned up directly against the live DB. The admin-facing New Surge form (not gated behind the mobile-PWA wall) was directly browser-verified showing all three new scoring fields; the student-facing group-registration screen sits behind the same gate as every other Node route this pivot has hit, so it relies on the RPC-level proof instead of a screenshot.
- **This closes out the pivot as scoped**: all five approved phases (0–4) from the professor's 11-point feedback are now built and live-verified. Not yet done: a real phone/PWA walkthrough of the group-registration screen and pooled-scoring quiz flow end-to-end, an admin view of "who's in which group" per Surge, real content (the 12-13 club roster, real audio files, real photos, a real club moto review), and whatever comes next — Goal 3/Goal 4 from before the pivot, or fresh direction after another professor review.

### Event Report — rebuilt to match the real Adani University reference template
- **The user shared the actual "AU Event Completion Report" template and a filled example ("Bid to Build"), revealing the Phase 0 guess (Title/Highlights list/Summary) didn't match the real institutional format** — the real one has Event Name/Date/Time/Venue/Organised by/Coordinator, Introduction (paragraph), Objectives (bullets), Event Highlights (a paragraph, not bullets — the Phase 0 guess had this backwards), Outcomes (bullets, missing entirely before), Conclusion (its own paragraph, previously conflated with Summary), and an Attachments checklist. Confirmed with the user before rebuilding: Date/Time/Venue/Organised-by-club auto-fill from the linked event, read-only — matching the "fill the gaps" philosophy directly rather than retyping data already in the app.
- **Built** (decision 50): migration 0024 restructures `event_reports` to the real shape (confirmed 0 live rows first, so a clean restructure not a data migration) and extends `public_events()` with the organising club's name (`DROP FUNCTION` first, the now-standing rule from decisions 46–48). Rewrote both the admin authoring form and the public detail page to match the real template's exact section order.
- **A real, previously-latent bug caught during this pass**: the public detail page's event date/time used locale-dependent formatting with no timezone pin in a Server Component — rendering in whatever timezone the deploying server happens to run in, not the event's real intended time. Live-verified concretely (an 11:00 AM UTC event showed as "4:30 p.m." before the fix) and fixed with the project's existing `formatDateUTC()`/`formatTimeUTC()` helpers.
- **Live-verified with the actual real-world example**: created a real test event matching "Bid to Build" exactly, filled out the real admin form with the real document's content, confirmed auto-fill pulled Date/Time/Venue/Club correctly, confirmed the saved data matches via direct query, and confirmed the public page renders every section (including bullets and the attachments checklist) in the template's real order. All test fixtures cleaned up. `npm run typecheck`/`lint`/`build` all passed clean throughout.

### Event Report .docx download rebuilt to match the real template's visual layout + real Adani logo (decisions 51-53)
- Covered above in decisions 51-53 — .docx export shipped, then rebuilt as a real bordered masthead table matching the paper template's actual layout, then the real Adani logo (extracted from the reference docs already on hand) was added and later relocated to the public site's footer only, and the whole site's canvas flipped from dark navy to a warm-ivory light theme per direct professor feedback. See decisions 51-53 for the full detail — not duplicated here.

### Event registration via external form + in-app reminders + QR-confirmed attendance (decision 54)
- **Built on Phase 2's existing registration/attendance/streak backbone (decision 47) rather than rebuilding it** — `event_registrations`, `register_for_event()`/`unregister_from_event()`, `redeem_event_scan()`'s attendance stamp, and the computed attendance streak were all already correct and untouched. What was missing: an external-form link on events, an actual event **detail** page (only a list existed), any way to **edit** an event after creating it (didn't exist at all), and any reminder surface.
- **Two design decisions resolved with the user via `AskUserQuestion` before any code, both "Recommended" picked**: registering in the app records our own database row AND opens the club's external Google/Microsoft Form in the same click (the form is the club's own supplementary data collection — team size, dietary needs — not our source of truth for "is this student registered," so no fragile Apps-Script-webhook integration was built); and reminders are in-app only, computed live on the Dashboard, not real Web Push (no VAPID keys, subscription table, or scheduled job — this project has zero push infrastructure today, and building it would have been a materially bigger, unrequested lift).
- **Built**: migration 0025 — `events.registration_form_url` (nullable) and `event_registrations.location_at_registration` (a snapshot of the event's location taken by `register_for_event()` at the moment of registration, so a later venue change can be detected by comparing the snapshot to the event's current location — simpler than a separate edit-history table). New admin capability that plain didn't exist before: an event **edit** page (`admin/grid/[eventId]/edit`) + `editEventAction`, club-scoped the same way creation already is. New student-facing event detail page (`(general)/events/[eventId]`) with full info and Register/Cancel; Dashboard's event rows became links into it (single action surface, not a duplicate inline button). A live-computed "Reminders" section on the Dashboard surfaces "tomorrow" (UTC day-boundary math, not local time — same reasoning as `formatDateUTC`/`formatTimeUTC`, so it doesn't flicker depending on the server's timezone) and "venue changed."
- **A real bug found during live verification, not caught by typecheck/lint**: the new event detail page did a raw `.from('events').select(...)` for what needed to be a logged-out-viewable public page — but `events` only has an RLS policy for `authenticated` reads (0005), so an anonymous request silently returned nothing and 404'd. This is exactly why `public_events()` exists as a security-definer RPC (decisions behind 0014/0018/50/52) — fixed by extending it with `end_date`/`joule_value` (migration 0026, `DROP FUNCTION` first per decisions 46-48) and routing the new page through it instead of the raw table.
- **Live-verified past the RPC layer, through the real UI, not just reasoning**: created a real test event/registration directly against the database, faking `auth.uid()` via `request.jwt.claims` to call the actual `register_for_event`/`redeem_event_scan` RPCs (not raw table writes), confirmed the location snapshot, simulated a venue change, confirmed the reminder-detection query returns `venue_changed: true`, computed a genuine QR token via `qr_token_for_epoch` and ran the real scan end-to-end (`attended_at` stamped, Joules credited). Then logged into the real admin UI in a browser, opened the new edit page, confirmed every field — including the registration form URL — was genuinely prefilled (not just showing its placeholder), changed the venue through the actual form, and confirmed the database reflected it. All test fixtures cleaned up, confirmed via a follow-up query. `npm run typecheck`/`lint`/`build` all passed clean (one transient Windows `EPERM` build error from a stale `.next` cache still held by the running dev server — this project's known "stale `.next`" gotcha, resolved by clearing it, not a real code issue).

### Pre-deployment feedback backlog, Phase A — real bugs, grounded in the code
- A 13-screenshot real testing-feedback document (gathered from actually using the deployed site ahead of a professor review) surfaced four grounded bugs, all fixed in one pass: the homepage's "Upcoming Events" list had no `<Link>` at all (fixed to match `/events`'s already-correct pattern); `loginAction` (student login) had no `students`-table membership check, so admin credentials silently redirected into `/dashboard` and hit the mobile-PWA gate's "Use your phone" screen instead of failing with a clear error (fixed to mirror `adminLoginAction`'s existing check); the service worker (`public/sw.js`) was pure cache-first with a hardcoded, never-bumped cache name, meaning old deploys could be served forever — rebuilt as a Route Handler (`app/sw.js/route.ts`) whose cache name derives from `VERCEL_GIT_COMMIT_SHA`, with network-first for page navigations and cache-first only for hashed `/_next/static/*` assets; and Grid Station's event picker didn't filter out concluded events, which would become unusable once the real 50-60 event calendar exists (fixed with the same `hasConcluded()` check the Dashboard already used).
- `npm run typecheck`/`lint`/`build` all passed clean; live-verified via `curl` (homepage links, service worker route) and direct Supabase REST queries against real seeded events (Grid Station filter) — the login fix was verified by code-reviewing it against the already-proven `adminLoginAction` pattern it mirrors, not a live login attempt.

### Pre-deployment feedback backlog — dev-only bypass for the mobile-PWA gate
- **The single biggest recurring limitation across this project's history**: every Node-route feature had to be verified via direct RPC calls instead of an actual look, because nothing in this session's browser tooling can satisfy either layer of the mobile-PWA gate (decision 29). Explicitly confirmed with the user before building, since it touches a security-relevant gate. `proxy.ts` gained `isDevMobileBypass()` — a `?devMobileBypass=1` query param or `dev_mobile_bypass` cookie, double-gated on `process.env.NODE_ENV !== 'production'`, which is structurally unreachable in any real `next build`/`next start` (all Vercel ever runs) regardless of any other env var. `pwa-gate.tsx` (Layer 2) reads the same cookie.
- **A real gotcha, not a code bug**: the bypass appeared not to work on the first attempt even though the cookie was genuinely set — React Fast Refresh had preserved `PwaGate`'s already-computed `standalone = false` state from before the edit hot-swapped in. A full page reload fixed it immediately.
- **Live-verified for real, the first genuine look at rendered student PWA content this whole project has had**: logged in as a real seed student account through the actual login form and reached a real Dashboard (Season Joules, tier, Power Grid, Upcoming Events, Recent Activity all rendering correctly, not inferred from RPC output).

### Pre-deployment feedback backlog, Phase B — bulk student account provisioning
- **The real process change behind "no self-signup," confirmed with the user via `AskUserQuestion`**: credentials go out as a CSV the institution sends themselves (email/WhatsApp), not an automated email-sending integration. Reused `createAdminAction`'s own exact pattern (service-role `auth.admin.createUser()` per row, then a direct `students` row insert via the service-role client) rather than `complete_onboarding()`, which only works in the new user's own session. New `BulkStudentsSection` in Institution Settings: paste "Name, email" one per line (up to 500), each validated against `is_email_domain_allowed()`, partial failures reported per-row, a client-side CSV built from the actual results. Added genuine self-service password change to Profile (`changePasswordAction`) — the other half of the deal. The public `/signup` route was kept working but de-emphasized (its link removed from the login page).
- **Live-verified genuinely end-to-end**: created real test rows through the actual admin UI (valid + deliberately malformed), confirmed per-row pass/fail reporting and that both valid rows exist in `students`. **Did not read back the real generated temp passwords or CSV contents** — the safety classifier correctly blocked an attempt to capture the CSV blob's contents as credential materialization; verification instead used a disposable, self-chosen test password set directly via the service-role Admin API, then a real login through the actual form via the new dev-mobile-bypass, reaching a genuine Dashboard. Self-service password change was verified the same way (changed, logged out, logged back in with the new value). All test fixtures deleted afterward. `npm run typecheck`/`lint`/`build` all passed clean.

### Pre-deployment feedback backlog, Phase C — events & club presentation redesign
- **Built**: event cover image upload (`event-covers` Storage bucket, same public-read/committee-write policy pattern as `gallery`) displayed on both the event card and detail page; `/events` redesigned as a card grid (cover image or a `BookOpen` placeholder, name/club/type) replacing the old plain list; per-club public pages (`/clubs`, `/clubs/[slug]`) with social links (new `instagram_url`/`linkedin_url`/`x_url` columns on `clubs`, editable from Institution Settings) via a new `public_clubs()` RPC; a public Leaderboard page (`/leaderboard`) reusing the exact season-picker + pagination pattern already proven in Catalyst Records, via new `public_seasons()`/`public_season_leaderboard()` wrapper RPCs (the originals are `authenticated`-only, so this follows the established `public_events()` narrow-wrapper pattern rather than opening RLS on `seasons`/`season_leaderboard()` itself); and real uploaded images for Event Report Attachments (each `attachment_*` boolean became an `attachment_*_paths text[]`, checked-ness now derives purely from the array being non-empty) plus a simple registered-vs-attended bar graph on the report page, fed by a new `total_registered` field on `public_event_stats()`.
- **A real bug caught only by live browser verification, not typecheck/lint/build**: `/clubs` and `/leaderboard` — this phase's own new public routes — were both missing from `proxy.ts`'s `PUBLIC_PATHS` allowlist, so both silently redirected to `/login` instead of rendering. Fixed before committing; a reminder that a route living under `app/(general)/*` is not automatically public — `proxy.ts`'s allowlist is a separate, must-update-by-hand list.
- **Live-verified directly in a real browser** (all of Phase C's new routes are on the public General site, not gated behind the mobile-PWA wall): the Leaderboard page renders all students including the zero-Joule tie-at-rank-2 students correctly; `/clubs` and the club detail page render correctly; the Event Report attendance bar graph's computed widths were confirmed via `getComputedStyle`; the attachment-image upload/display path was verified by uploading a real test PNG directly via the service-role client (the Browser pane's tooling can't drive a native OS file picker) and confirming it renders as a real `<img>` with the correct `naturalWidth`/`naturalHeight` on the public page — all test fixtures (the uploaded file, the report's temporarily-attached path) cleaned up immediately after. `npm run typecheck`/`lint`/`build` all passed clean throughout.

### Live Round — real multi-student teams + pooled/split scoring (decision 60)
- **Triggered by real-device testing**: the user shared annotated screenshots from the actual installed PWA showing Live Round's join screen had no group-formation UI at all, and then explicitly clarified — after being shown that group/pooled scoring already existed on async Surge Mode (decision 49) — that **only Live Round will actually be used in practice**, so the mechanic needed to move into Live Round itself, not just be documented as living elsewhere. Went through a full `EnterPlanMode`/`ExitPlanMode` plan before any code, given the size of the rearchitecture.
- **Built** (decision 60): migration 0032 rearchitects `live_round_teams`/`live_round_answers` and adds `live_round_team_members`, mirroring async Surge's `quiz_groups`/`quiz_group_members` pattern; new RPCs `create_live_team`/`join_live_team`/`leave_live_team` replace `join_live_round`; `submit_live_answer` defers Joule crediting to a new `complete_live_round()`, called automatically inside `host_advance_round`'s terminal transition; `live_round_scoreboard`/`live_round_question` rewritten around the new membership table. Client-side: `/live` is room-code-only, a new `TeamFormationClient` handles create/join/leave, and the host lobby shows both team and member counts via a new realtime subscription.
- **A genuine environment migration blocker, root-caused and fixed**: the direct-Postgres host (`db.<project>.supabase.co:5432`) resolves only to IPv6, and this sandbox has no IPv6 route at all — switched `DATABASE_URL` to Supabase's connection pooler (`aws-0-ap-northeast-1.pooler.supabase.com:6543`, transaction pooler mode), which resolves via IPv4 and is now the live migration-runner connection string (the old direct host is kept as a commented-out reference, not deleted).
- **A real, previously-latent bug in shared code, found while testing this feature (not caused by it)**: `lib/components/reveal-scoreboard.tsx`'s reveal-animation effect had an empty dependency array, permanently freezing the reveal at zero rows on any cold page load landing directly on Live Round's `complete` phase (before the parent's async scoreboard fetch resolves) — invisible in ordinary continuous play, since the preceding phase's own earlier fetch already populates the state well before the terminal transition. Fixed by depending on `rows.length` instead of `[]`.
- **A real, previously-undocumented local-dev-only tooling trap, discovered while chasing what first looked like the fix above not working**: the service worker's cache-first rule for `/_next/static/*` (decision 57) assumes Next.js content-hashes those filenames — true in a real production build, not true for `next dev`'s Turbopack chunk names, which stay stable across content changes. An already-SW-installed browser tab kept serving a pre-fix chunk indefinitely, surviving even a full dev-server restart with `.next` cleared, until the service worker was explicitly unregistered and Cache Storage cleared in that tab. Confirmed via `curl` (bypassing the browser/SW entirely) that the server was serving current code the whole time — the browser's own cache was the sole problem. Documented as a Known Open Item so a future session doesn't mistake this for a broken fix again.
- **Verified the pooled-scoring math with an independently-reimplemented JS formula** (same bar as decision 49): a throwaway script drove the real RPCs end-to-end for a 2-person-team-plus-solo-player scenario with mixed correct/wrong/timed answers, diffed actual `joule_transactions` against hand-computed expectations — exact match for all three students. Script and its fixtures were deleted immediately after.
- **Live-verified in a real, genuinely fresh browser tab** (SW cleared, dev server restarted, `.next` wiped) that the `complete`-phase scoreboard renders correctly. `npm run typecheck`/`lint`/`build` all passed clean. Cleanup: the one leftover test round and its team/members were deleted (cascading via FK); the pre-existing "Example" test surge those rounds belonged to was deliberately left untouched, since removing a whole shared surge was outside this task's stated cleanup scope; the leftover `advance-round.mjs` script was deleted.

### Real-device Professor/Committee-Member walkthrough → 4-tier RBAC rework, ledger redesign, mobile admin nav, audit trail
- **Triggered by the user personally walking through the app as a real Professor and a real Committee Member on a real phone** — the single most productive verification method this whole project has used, catching problems no amount of code review or RPC-level testing could have. What started as "let me walk through the CM-creates-event / Professor-tracks-it flow" turned into a full RBAC rework (decision 78) once the user described the real operating model directly: Professor per club, not platform-wide; Committee Member never touches the QR or Live Round; a new Super Admin tier holds what Professor used to.
- **Built**: the full 4-tier RBAC (decision 78, migrations 0038–0040/0042), a simplified event-picker-based System Ledger for club-scoped roles replacing the dense charts dashboard (decision 79's own scoping fix made this redesign possible — confirmed with the user via `AskUserQuestion` before building: same nav link, Super Admin's view untouched), Live Round narrowed to staff-only (decision 81), custom admin passwords + a genuinely traceable audit log with its first-ever viewer page (decision 82), and a mobile hamburger nav for the admin shell (decision 84).
- **Real bugs found from actually using each fix live, not just RPC-testing it, at a rate higher than almost any earlier phase**:
  1. The System Ledger's three newer RPCs (0031, added after 0038's original audit list was drawn up) were completely missed and stayed platform-wide — a Cityscape Professor saw all 9 clubs' numbers (decision 79).
  2. The fix for #1 had its own bug: `admin_club_engagement()`'s `RETURNS TABLE` column named `club_id` collided with a same-named local variable (decision 25's exact bug class, hit a fourth time in this project — 0040).
  3. `event_reports`' blanket public-read policy from 0019 silently defeated every club-scoping policy layered on top of it (decision 80) — a Cityscape CM could read Shastra/StratEdge Club reports, found from the user's own live testing, not from re-reading the migration.
  4. The admin shell had zero mobile navigation below `sm:` — a real Committee Member on a real phone could not reach Event Reports at all (decision 84).
  5. The Event Report form crashed on submit with a raw browser-level error page when uploading real phone photos — root-caused via `npx vercel logs` (after first accidentally linking to a brand-new, wrong Vercel project instead of the existing one, caught and fixed immediately) to Next's default 1MB Server Action body limit (decision 83).
  6. A process mistake, not a code bug: the user ran a migration's `-- migrate:up` and `-- migrate:down` sections back-to-back once, cleanly reverting the whole change — caught immediately, no data lost, the up-section was just re-applied.
- **`npm run typecheck`/`lint`/`build` clean after every fix.** Live-verified via a mix of: 12+5+2 RLS/RPC cross-club checks against disposable fixtures (all passing, fully cleaned up), direct re-querying of the exact raw queries each affected page runs (confirmed before/after values), and the user's own real-device re-tests after each deploy. A real Vercel CLI mistake was caught and corrected mid-session (`vercel link` without an explicit project name created a new project called "template" instead of linking the existing "jules" one — reverted the stray `.gitignore`/`.env.local` changes it made and re-linked correctly).

**Local script-based testing report for this segment** (every verification script below was disposable, run once, and fully cleaned up afterward — none left running or committed):
| What was tested | Method | Result |
|---|---|---|
| Cross-club RLS after the RBAC rework (0038) | 12 checks via `SET LOCAL request.jwt.claims` against disposable club/professor/CM fixtures | 12/12 passed |
| System Ledger club-scoping (0039/0040) | Direct RPC calls as a disposable Professor fixture, before/after values compared | Confirmed platform totals (31/9/10) → correctly scoped (1/1/2) |
| `event_reports` leak fix (0042) | Same raw query the admin list page runs, run directly as the real Cityscape CM | Confirmed 3 leaked cross-club reports → 0 (correctly none of their own yet); public RPCs re-confirmed still working for anonymous callers |
| Audit log (0041) | 5 real actions logged as a disposable Professor fixture + custom-password login + invalid-action rejection + non-admin rejection | 5/5 checks passed |
| Server Action body limit (0083) | Root-caused via real Vercel runtime logs (`npx vercel logs`), not a local script | Confirmed exact error/digest match, fixed, redeployed |
| Mobile admin nav | Preview-browser accessibility-tree read at a 375px viewport, real `<Link href>` hrefs confirmed (not a JS-only click handler) | Menu renders + role-filters correctly; a live click-through hit a tooling coordinate-scaling artifact unrelated to the app itself, not re-attempted given the stronger href-level proof already in hand |

Every disposable test/verification script (club fixtures, admin RPC-check scripts, the account-creation helper) was deleted immediately after use, per this project's standing fixture-cleanup discipline — confirmed via a follow-up query after each one, not just assumed.
